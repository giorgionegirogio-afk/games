# Pulizia dei banchi #108 — piano di esecuzione (voce #121)

Progetto: `docs/superpowers/specs/2026-09-19-pulizia-108-design.md`.
Base: `main` = `48921bd`. Ramo: `voce-121-pulizia-108`.
Cantiere di manutenzione degli STRUMENTI: non si tocca il gioco. Tre compiti,
un commit per compito, piu' revisione finale + fusione.

## Vincoli globali (a ogni compito)
1. **Non si tocca `CALCETTO-il-gioco.html`** (resta a `48921bd`). Si editano solo
   file `strumenti/*.js` (sono strumenti, si editano direttamente — nessun
   attrezzo-a-ancore, quello e' per il gioco).
2. **La correzione e' meccanica**: `setCpuVsCpu(true)` va spostato DOPO
   `startMatch(...)` (modello corretto: `_eventi.js`, `_posa.js:posaFerma`). Non
   cambiare NIENT'ALTRO: stessi semi, stessa taglia, stessa logica di prova.
3. **Leggere OGNI riga dell'esito**, non solo il codice d'uscita: la correzione
   cambia lo scenario (squadra 0 da immobile a CPU vera), quindi i NUMERI cambiano
   e una prova che passava solo grazie all'artefatto potrebbe scoprirsi rossa.
4. **Nessuna partita del banco deve restare in `freekick`**: correggere l'ordine
   elimina l'hang (diagnosi #119, esperimento A: 0/31). Se una resta bloccata, la
   correzione non e' stata applicata a tutti i siti.
5. I riferimenti (numeri di taglia/sorteggi) che cambiano nei commenti/header del
   banco o nel MANUALE vanno RI-ANCORATI ai nuovi numeri, dichiarando il cambio
   (mai lasciare un riferimento stantio spacciato per attuale).
6. Un commit per compito; messaggi italiani; commenti senza accentate.

## Compito 1 — `_q-battute.js` e `_q-regole.js` (basso rischio)
**Obiettivo.** Correggere i 20 siti a basso rischio nei due banchi.
- `_q-battute.js`, 5 siti: :325-326, :348-349, :370-371, :396-397, :451-452
  (le prove 2 RIMESSA, 3 FONDO-ANGOLO, 4 FONDO-RINVIO, 5 GABBIA, 6 ANTI-STALLO).
  I 3 gemelli gia' corretti (prove 9/10/11) sono il modello.
- `_q-regole.js`, 15 siti: :801-802, :1039-1040, :1106-1107, :1130-1131,
  :1153-1154, :1193-1194, :1223-1224, :1270-1271, :1305-1306, :1336-1337,
  :1375-1376, :1405-1406, :1513-1514, :1548-1549, :1586-1587.
**Cancelli.** `node strumenti/_q-battute.js` → 11/11, LETTO riga per riga.
`node strumenti/_q-regole.js` → 16/16, LETTO riga per riga (attenzione alle prove
3-16, margine di incertezza dichiarato dalla ricognizione). Aggiorna l'header di
`_q-regole.js` (:218-233 documenta il difetto e il censimento come APERTO) per
dichiararlo CHIUSO su questi due banchi, coi numeri nuovi se cambiano.
**Definizione di fatto.** I 20 siti corretti, entrambi i banchi verdi letti riga
per riga, nessuna partita in freekick, header di _q-regole aggiornato. Un commit.

## Compito 2 — `_q-umore.js` (rischio concreto)
**Obiettivo.** Correggere i 5 siti (:335-336, :636-637, :715-716, :850-851,
:886-887). La squadra 0 diventa CPU vera: piu' eventi, partite diverse.
**Cancelli.** `node strumenti/_q-umore.js` → 30/30, LETTO riga per riga:
- i **tetti** (prova a-tetti) devono ancora REGGERE (il massimo osservato puo'
  cambiare — ora la squadra 0 gioca e campiona di piu' — ma NON deve superare il
  tetto dichiarato; se il massimo osservato cambia, ri-ancora il numero
  nell'header/commento e nel MANUALE, dichiarando che ora e' CPU-CPU vera);
- **nessuna** delle 5 partite di TETTI ne' la partita di TESTIMONE resta in
  freekick (correggere l'ordine lo elimina — verificalo);
- REGISTRO/STATI/SPECCHIO/INPUT-SACRO/NIENTE-FALSI-BANNER restano verdi.
- Rigenera i bugiardi e conferma che il banco li condanna ancora
  (`_crit-mind-tetto.js`, `_crit-mind-muto.js`).
Se un numero di riferimento del compito 6 di #117 (gia' in MANUALE) e' stato
misurato con la squadra 0 congelata e ora cambia, ANNOTA la rettifica (studi a
edizioni: il vecchio numero era su scenario squadra-0-immobile, il nuovo su
CPU-CPU vera).
**Definizione di fatto.** 5 siti corretti, 30/30 letto riga per riga, tetti che
reggono, zero freekick bloccati, riferimenti ri-ancorati. Un commit.

## Compito 3 — la prova anti-regressione
**Obiettivo.** Impedire che un quarto banco nasca rotto (come `_q-umore`).
- Aggiungi a `_posa.js` (dove chi scrive un banco guarda gia' `semeFisso`) una
  funzione/nota che documenta l'ordine giusto, e/o una verifica riusabile: dopo un
  avvio CPU-CPU, `G.cpu` deve essere `[true,true]`. Preferisci ESPORRE la funzione
  corretta gia' esistente (`posaFerma`) come la via raccomandata, con un commento
  che dice perche' (l'artefatto #108).
- Aggiungi un CONTROLLO in `strumenti/tutti.js` (o un piccolo banco dedicato
  `_q-cpu-ordine.js` registrato in batteria, conta:true): verifica che dopo
  `startMatch` + `setCpuVsCpu(true)` risulti `G.cpu===[true,true]`, e — piu' forte
  — che una partita CPU-CPU a seme fisso NON resti in `freekick` oltre un tetto di
  fotogrammi (il sintomo #108/#119). NASCE ROSSA su un gioco/banco che simula
  l'ordine sbagliato (dimostra che condanna), VERDE sul giusto.
**Cancelli.** Il nuovo controllo verde; registrato in `tutti.js`; nasce rosso
sull'ordine sbagliato (dimostra che misura). Batteria intera rilanciata verde.
**Definizione di fatto.** La prova esiste, condanna l'ordine sbagliato, e la
batteria intera e' verde. Un commit.

## Chiusura
Revisione finale del ramo (modello piu' capace): riproduce i banchi verdi letti
riga per riga, conferma che il gioco NON e' toccato (`git diff` di
CALCETTO-il-gioco.html vuoto), che la prova anti-regressione condanna davvero.
Verbale in MANUALE (voce #121): #108 chiuso sui tre banchi di batteria, la prova
anti-ricaduta, i riferimenti ri-ancorati, gli ~22 strumenti d'archivio dichiarati
fuori. Se «Ready to merge: YES»: fast-forward, smoke (precedenza 9/9), push, ramo
eliminato, ledger aggiornato.

# Dove siamo, e cosa manca

Aggiornato al **23 settembre 2026**. Questo file serve a riprendere senza
rileggere niente altro.

## La giornata del 23 settembre, in breve

| | cosa |
|---|---|
| 0 | **#145 CANTIERE CHIUSO — LA MISURA DELLA RETE E DEL TRASPORTO, secondo bivio dell'ONDA E, E IL VERDETTO È NO** (cinque compiti dal merge-base `55267d0`; spec `docs/superpowers/specs/2026-09-23-misura-rete-design.md`, piano `.../plans/2026-09-23-misura-rete.md`, referto `_analisi/MISURA-RETE-145.md`, dati grezzi `_analisi/misura-rete-145.json`). **Il gioco non è stato toccato: `MOTORE_V` resta 4.** **PRIMA DI MISURARE si è sciolto il nodo che avrebbe comprato il NO a credito**: il #141 aveva scritto «D_gioco = 0 tick» applicando la regola dell'estremo alto, e preso alla lettera `D_rete ≤ D_gioco` è falsa senza misurare niente. La lettura onesta del gradino è che **da 50 a 300 ms il gioco costa lo stesso**, quindi la soglia che decide è `D_rete ≤ 18 tick` — l'estremo dell'intervallo che il #141 ha davvero guardato. **LE SEI SOGLIE SONO COMMITTATE PRIMA (`53d5ffa`).** **E UNA DERIVAZIONE CHE NESSUN DOCUMENTO DI CASA AVEVA SCRITTO**: lo stallo non si sceglie, si calcola — 600 invii al minuto, meno di uno stallo al minuto, quindi **D deve coprire il p99,83**, non il p95. **I NUMERI** (una macchina sola, connessione FISSA italiana, e c'è scritto ovunque): relay WebSocket veri, 3.000 e 800 pacchetti, **D_rete 30,6 e 23,3 tick**, **D_stallo 38,4 e 30,4 tick**, IC del p95 a semiampiezza 19,5 e 22,5 ms (tetto 25). **IL CONTROLLO DEL BANCO È LA MISURA PIÙ IMPORTANTE**: lo stesso client contro un relay WebSocket scritto a mano sul loopback dà **p99,83 = 2 ms, massimo 3** — le code da 300-600 ms sono della rete, non dell'anello degli eventi di Node, e il verdetto non è mio. **E UNA MISURA HA CAMBIATO IL VERDETTO MENTRE LO SCRIVEVO**: la ridondanza di §2.4 del progetto d'onda faceva scendere D_stallo a **8,4 tick** (un SÌ), ma quel conto assume ritardi indipendenti — misurato, **il 78,4% e il 66,7% dei pacchetti sopra il p95 ne ha un altro sopra il p95 subito prima**, contro il 5% atteso: la coda arriva a raffica (blocco in testa alla fila di TCP) e le R copie stanno tutte nella stessa fila ferma. **IL BANCO SI È ANCHE CONDANNATO DA SOLO**: la prima sonda WebRTC apriva tre `RTCPeerConnection`, una per STUN, confrontava le porte mappate, ne trovava tre diverse e stampava «NAT SIMMETRICO, SERVE UN TURN» — condannava il P2P dell'intera onda E **misurando il proprio numero di socket**. Rifatta interrogando i tre STUN dallo STESSO socket, con la guardia che tutti e tre rispondano (o concluderebbe «a cono» misurando il silenzio): **una sola porta distinta, tre giri su tre → mappatura indipendente dall'endpoint, da rete fissa il P2P con solo STUN passa**. **E UN FATTO CHE NESSUN VERBALE AVEVA MAI VERIFICATO: non esiste nessun progetto Supabase** — zero riferimenti nel repo, zero variabili d'ambiente, **zero variabili sul progetto Vercel `calcetto-rete`**, e il deployment di produzione risponde **503 `DEPLOYMENT_PAUSED`**. **E LA STESSA DOMANDA DETTA DALLA PARTE DI CHI GIOCA**: col ritardo più generoso che il gioco tollera (18 tick, 300 ms) la partita si fermerebbe **17,2 e 9,0 volte al minuto** contro una soglia di **meno di una**; e la durata del singolo stallo starebbe dentro i 250 ms dichiarati (111-157 ms in media), quindi **non è la lunghezza a uccidere il lockstep, è la frequenza**. **IL VERDETTO: S1 NON TIENE, S2 NON TIENE, S3 NON TIENE, S4 tiene a metà, S5 NON MISURATA, S6 tiene → NO.** E il NO non è «la rete è lenta»: le mediane stanno benissimo (67 ms a due gambe verso un relay pubblico lontano; AGCOM dà 28 ms di RTT medio sul mobile italiano, marcato letteratura). **Il NO è la coda**, che vale 2,7-5,5 volte la mediana, arriva a raffica, e non sta ferma nemmeno fra due corse della stessa sera (p99 da 335 a 493 ms in mezz'ora). **LA MISURA FAVORISCE LA TERZA VIA** (§5.3 del progetto d'onda, il 1v1 fatto di duelli) **per una ragione misurata**: il danno dello stallo è `600 invii/minuto × P(coda)`, cioè scala con la FREQUENZA del canale — dieci scambi per duello danno **0,017 stalli per duello** invece di tre al minuto, i verbi del duello sono già semantici nel nastro, il duello ha già il suo orologio, e il polling di oggi ci sta dentro i freni (30-60 richieste al minuto) senza un servizio nuovo, senza una chiave nuova e senza bolletta. Il server autoritativo invece **non toglie la coda**: la tollera in un altro modo, e il gioco non ha né estrapolazione né riavvolgimento. **CANCELLI**: `rete-latenza` **15/15** e `rete-falsi` **6/6** in batteria `conta:true` (offline, deterministici); la campagna NON è in batteria ed esce **3** se non può misurare. **RETTIFICA A EDIZIONI**: `rete/LEGGIMI.md:167-173`, tre affermazioni non misurate presentate come fatti, corrette in chiaro accanto al testo vecchio. **CIÒ CHE RESTA NON MISURATO**: due telefoni italiani su rete mobile — e la misura da fare per prima è scritta (un pacchetto ogni 100 ms per un'ora, 36.000 campioni, p99,83 e quota di raffica, più il tasso di riuscita del DataChannel con solo STUN fra quei due telefoni, che è S5). Verbale: `MANUALE.md` §A, voce #145. |
| 1 | **#144 CANTIERE CHIUSO — IL COMANDO SENZA SCHERMO: due debiti pagati in una volta, e l'onda E ha i suoi primi due prerequisiti** (cinque compiti dal merge-base `c71a83e`; spec `docs/superpowers/specs/2026-09-23-comando-senza-schermo-design.md`, piano `docs/superpowers/plans/2026-09-23-comando-senza-schermo.md`, progetto d'onda `…/2026-09-23-onda-e-architettura.md` §3.1). Chiude **il seguito piu' grosso del progetto** (#133, i tocchi in coordinate di schermo, aperto dal 22 settembre) e **dissolve il prezzo dichiarato dal #139** («un nastro con la finestra mossa resta aperto per sempre»). **IL DIFETTO E' PEGGIORE DI COM'ERA SCRITTO**: dove finisce un pixel lo decidono TRE cose che nel nastro non ci sono — la finestra, il POLLICE (scala 85-150%, spazio 100-140%, mancino) e la TACCA (`env(safe-area-inset-*)`) — e i due canali gemelli **non stavano in nessun verbale**. Una sfida vera registrata a 915x412 e dichiarata **2-3** rigioca **0-5** a 800x360, **0-5** a 844x390, **0-1** a 1280x720, **0-0** col pollice al massimo e **0-2** con la tacca; e su quest'ultimo lo schermo e' IDENTICO, quindi il giudice non si asteneva nemmeno: **diceva NON TORNA a un onesto che aveva solo un telefono con la tacca**. **LA CURA NON E' «NORMALIZZARE LA RISOLUZIONE», E' TRASPORTARE L'ATTO RISOLTO**: `Touch5.start` si spacca in `risolvi` (pura) + `applica` (il corpo di prima riga per riga) + `avvia` (la porta che il dito vero e il nastro attraversano insieme), e fra loro passa un atto di cinque numeri — **squadra**, esito, disco per INDICE, e il punto di posa **in unita' della geometria dei comandi**. La normalizzazione non e' inventata: e' `d/(r+10)` e `d/(r+18)`, quelle che il gioco usa gia' per decidere, quindi la distanza normalizzata e' invariante **per costruzione** perche' il denominatore e' la geometria LOCALE. **Il verbo non viaggia** (si rilegge da `touchBtnLayout(t)[slot].act`: dipende dal possesso, che e' simulazione). **I trascinamenti viaggiano come scostamenti** dal punto di posa, ed e' l'unica forma corretta perche' TUTTE le soglie che li leggono sono in pixel assoluti e non scalate (6, 12, 46, 70, 96). **Due tipi di riga nuovi (12 e 13), e i tipi 0 e 1 restano LEGGIBILI.** **LA SQUADRA NEL COMANDO** e' il secondo prerequisito dell'onda E: `teamOf` e' avvolto dall'esterno e in rilettura **non ricalcola** — misurato (prova F, l'unica in modalita' 2) che un tocco a x=500 registrato a 1280 e' della squadra 0 e prima della cura, riletto a 800, muoveva la levetta della squadra **1**. **IL BANCO `_q-schermi` 8/30 → 30/30**, sei bracci e **due misure per braccio** (il verdetto, e il punteggio rigiocato con l'astensione AGGIRATA — senza il secondo misurerebbe la guardia invece del canale). **E IL BANCO SI E' CORRETTO DUE VOLTE PERCHE' ATTESTAVA**: la tacca a 34 px spostava i dischi di dieci pixel e quel braccio era verde anche PRIMA della cura (adesso porta gli inserti veri, 44/59/21/59, 35 px di spostamento, e un dito sul BORDO della presa); e la prova **G** non esisteva — senza di lei il banco **promuoveva due falsi su cinque**, perche' una volta che l'atto porta esito e disco il punto ricostruito non decide piu' il punteggio e un gioco che rigiocasse dal PIXEL dava 21 prove su 24 **identiche alla cura onesta**. **CINQUE FALSI, tutti bocciati, bite list misurata su 30 prove**: `pixel` G1 G2 G4 G5 G6 (25/30), **`mezza` — LA MEZZA CURA — G5 e G6 soli (28/30), cioe' esattamente i due bracci gemelli, con le quattro finestre tutte verdi**, `ricalcola` F1 F2 (28/30), `mossa` quattordici prove (16/30), `grana` il solo A2 (29/30). **`MOTORE_V` 3 → 4, MISURATO NEI DUE VERSI** (`_t-144-motorev.js`): quattro nastri del merge-base rigiocati sul curato sono **identici 4/4** (la cura di suo non cambia nessuna partita), ma quattro nastri del curato rigiocati sul gioco di ieri finiscono in una partita **diversa 4/4**, tutti al primo campione, con **170 righe lette su 2749** — il gioco vecchio non ha un ramo per i tipi 12 e 13 e rigioca una partita in cui nessuno ha toccato lo schermo. Senza il numero, un telefono con la copia in cache direbbe NON TORNA a un onesto; **prezzo dichiarato**: i nastri v3 diventano ingiudicabili, e costa poco perche' v3 e' di **ieri** (#143). **LE TRE ASTENSIONI DELLO SCHERMO NON SI TOLGONO, SI CONDIZIONANO**: il nastro si astiene **se e solo se porta un PIXEL** (`nastroHaPixel()`, una funzione sola letta dal giudice e dalla staffetta) — non una data, non una versione, una proprieta' del nastro. La riga 10 resta scritta: non decide piu' niente, ma un referto deve poter dire su che telefono si e' giocato. **LA STAFFETTA SI SEMPLIFICA** e continua a funzionare: tre etichette invece di due — `qualunque` (i nastri di atti, che una finestra non la chiedono), `ignota` (i nastri di prima del #133) e la misura vera — piu' una rettifica a edizioni sul commento di `MISURA_SERIE`, che dal #144 e' la casa della maggior parte delle righe e non l'angolo dei casi persi. **RESIDUO DICHIARATO**: la riadozione della levetta dopo una pausa chiede «questo dito e' su un pulsante?» sulla posizione CORRENTE, cioe' dopo il trascinamento; il punto di POSA e' invariante per costruzione, quello dopo un trascinamento no, e la differenza e' limitata dalla differenza dei raggi. Oggi non morde. **IN BATTERIA**: `schermi` (`conta:true`, `lento:true`). **E due falsi che il #143 aveva lasciato indietro tornano a costruirsi** (`_crit-finestra`, `_crit-giudice-sbadato`: pretendevano `MOTORE_V = 2` alla lettera e sarebbero morti a ogni incremento). Verbale completo in `MANUALE.md` §A registro, voce #144, IN CIMA |
| 2 | **#142 CANTIERE CHIUSO — IL MOTORE NEL NASTRO: un onesto non viene piu' accusato perche' ha un telefono di un'altra marca** (cinque compiti dal merge-base `fc25184`; spec `docs/superpowers/specs/2026-09-23-motore-nel-nastro-design.md`, piano `docs/superpowers/plans/2026-09-23-motore-nel-nastro.md`). Cura un **CRITICO IN PRODUZIONE DELL'ONDA D** scoperto dal #141, che l'aveva letto solo come un blocco dell'onda E. **LA CONDANNA E' MISURATA SULLA CATENA VERA, non ragionata**: otto sfide oneste registrate su WebKit (due telefoni, server finto, nastro preso dalla riga che il server ha ricevuto) e rigiudicate con la finestra che il nastro dichiara danno **WebKit TORNA 8/8, Chromium NON TORNA 7/8, Firefox NON TORNA 1/8** — e al banco definitivo a sei nastri, 5 accuse su 6 e 2 su 6. E' **peggiore dell'ipotesi**: fra un iPhone e un Android il motore JavaScript e' **sempre** diverso, e `staffetta.js` apriva `chromium.launch()` a riga fissa. NON TORNA e' l'unico verdetto che muove punti — li toglie a DUE persone, alza un sospetto che non decade mai e chiude la riga per sempre. L'unico nastro salvo su Chromium si era salvato **per fortuna** (gia' divergiuto a 3-3 contro lo 0-2 dichiarato, fermato da un duello dal dischetto senza comandi: un'astensione per la causa sbagliata). **LA CAUSA** e' quella del #141: ECMA-262 lascia le trascendenti approssimate e `Math.hypot` sbaglia l'ultimo bit su 100 valori su 200; il nastro portava lo SCHERMO (riga 10, #133) e non il MOTORE. **LA CURA E' LA DOTTRINA DEL #133 E DEL #139 — ci si astiene, non si accusa**: una riga di tipo 11 con l'impronta FUNZIONALE del motore. **Non lo userAgent** (dato personale, cambia a ogni versione, si falsifica da una console): il motore si dichiara FACENDO IL CONTO — 64 ingressi irrazionali fatti di sole moltiplicazioni e addizioni (esatte per IEEE-754, cosi' si misura la funzione e non il valore), sette trascendenti ciascuno, 448 risultati letti A BIT e ridotti con FNV-1a a 32 bit, che usa solo xor e imul. **pow e sqrt escluse APPOSTA e misurato perche'**: sono correttamente arrotondate, e un'impronta fatta con loro vale **1634607669 su tutti e tre i motori**. L'impronta vera separa tutti e tre — chromium **3274447767**, webkit **4281245088**, firefox **1495105755** — ed e' **stabile** su contesti freschi. **IL GIUDICE SI ASTIENE** in coda ai controlli dello schermo: impronta diversa -> `INCOMPLETO/motore-js-diverso` **col numero nel referto**, impronta assente -> `INCOMPLETO/motore-js-ignoto`, **e ci si astiene lo stesso** (correzione di revisione che il #133 ha gia' pagato, IMPORTANTE-1). **L'ESITO: da 5 accuse su 6 a ZERO, e su firefox da 2 a 0; e la copertura non si e' mangiata niente — sullo stesso motore TORNA 6 su 6.** 11 controlli su 11. **IL PREZZO DEI NASTRI VECCHI E' DICHIARATO E NON STIMATO**: tutti i nastri di ieri diventano ingiudicabili, e **quanti siano non si puo' contare da qui** (il database di esercizio non sta nel repo). Si e' misurato l'effetto: la prova E porta un nastro vero da TORNA a INCOMPLETO, e l'unica fixture di nastro del repo non ha la riga 11 (1 su 1). Si paga perche' **l'alternativa all'astensione non e' «verificarli», e' ACCUSARLI**, e perche' la riga resta a `verificata = 0`, cioe' torna giudicabile da se'. **E IL PREZZO SI E' FATTO SENTIRE SUBITO**: TRE reti che usano quella fixture per misurare ALTRO (il duello in `_q-giudice`, i cinque verdetti in `_q-staffetta`, la finestra che cambia in `_q-finestra` — e **la terza l'ha trovata la batteria**, che e' la ragione per cui la si rilancia intera a ogni compito) si completano **a runtime** con l'impronta di chi giudica invece di rigenerare la fixture con un numero fisso, che si spegnerebbe da solo su un'altra macchina. **LA STAFFETTA APRE IL MOTORE GIUSTO**, cosi' l'astensione e' una complicazione operativa e non una resa: raggruppamento per la COPPIA (misura, impronta), e l'impronta si **chiede** a ogni motore invece di indovinarla dal nome. **Misurato sul giro vero** (`_t-142-staffetta-motori.js`, 8/8): con tutti e due i motori **due contesti, due TORNA, ognuna sul suo**; **con un motore solo** la riga che chiede l'altro **non si giudica su quello che c'e'** — resta a 0, nessuna parola parte per lei, e il referto la grida. **E I REPLAY NON SI SPENGONO, MISURATO e non dedotto dal codice** (`fuori/_sonda-142d.js`): `Sfida.guarda` intercetta quattro cause PER NOME e queste due non ci sono — la stessa sfida vera guardata tre volte dal telefono del difensore (nastro intatto, nastro con un motore che non e' quello di chi guarda, nastro senza riga 11) parte tutte e tre le volte, `registroModo = 2`, scena kickoff, cinque uomini in campo, mentre il giudizio sugli stessi tre dice TORNA, `INCOMPLETO/motore-js-diverso` e `INCOMPLETO/motore-js-ignoto`. **QUATTRO FALSI NEL CASO PEGGIORE, 14/14**, bite list misurata: `muto` (scrive e non guarda) cade su A2, E, F, F2; `accusa` (il difetto del #133) su A2, A3, F, F2; `piatto` (pow e sqrt) su C, A2, F, F2; `pauroso` (si astiene sempre) su A1b e B **e passa tutte le altre otto**. Piu' il controllo positivo: il vero 11 verdi su 11. **E DUE FALSI HANNO CORRETTO IL BANCO**: `pauroso` lo faceva uscire **3 invece di 1** (zero accuse perche' i nastri tornano e zero perche' il giudice si e' astenuto sono due cose diverse, e un 3 non accusa nessuno) — da li' A1b e la prova nulla spostata in fondo; e lo stesso falso ha scoperto che il lettore di etichette filtrava `[A-Z0-9]+`, quindi la **b minuscola di A1b spariva** e il falso risultava non morso — da li' V2. **A1, IL CONTROLLO DI ESERCIZIO**, e' la meta' che manca a quasi tutti i banchi: maschera l'impronta del nastro con quella di chi giudica, cosi' il giudice rigioca e dice quel che avrebbe detto senza la cura (5 accuse su 6); se non ne trova nessuna il banco **esce 3** invece di dichiararsi verde. **MOTORE_V RESTA 2, MISURATO**: 30 nastri su 30 registrati sul gioco di main e rigiocati sul curato danno la stessa partita. Diff del gioco **216 righe aggiunte, 2 tolte**. **DUE ROSSI CHE NON ERANO REGRESSIONI**, misurati tre volte su due versioni prima di scrivere la parola: `verbi-ritardo` e `audio` sul gioco di oggi 3/3 verdi, e sul gioco di main `verbi-ritardo` NO-OK-OK col banco che si dichiara da se' «RUMOROSO OGGI». **IN BATTERIA**: `motore-nastro` e `motore-falsi`, tutti e due `conta:true` e tutti e due **`solo:true`** — e non per il cronometro: misurato che in compagnia di altri tre cancelli il `goto` scade a 30 s (tre motori veri e un file da 2,7 MB a sei contesti) e il banco esce **2**, cioe' si dichiara cieco per il carico invece di misurare; da soli sono verdi, e dentro il banco i tempi sono saliti a 120 s come seconda rete. (`motori` del #141 resta a `conta:false`, e' rosso per un guasto vero e aperto.) **SEGUITI**: la **matematica scritta in casa** (il #141 ha misurato che il solo `hypot` fa convergere 7 semi su 8, non 8: serve tutto il resto, e muove MOTORE_V); **l'impronta e' NECESSARIA, non sufficiente** — due motori che la danno uguale potrebbero divergere su un valore non campionato, quindi il rischio residuo e' ridotto e non azzerato, mentre la direzione dell'errore e' sicura (un'astensione in piu', mai un'accusa in piu'); e il conto vero dei nastri vecchi in esercizio. Verbale completo in `MANUALE.md` §A registro, voce #142, IN CIMA |
| 3 | **#141 CANTIERE CHIUSO — IL METRO DEL RITARDO, primo dell'ONDA E, E IL VERDETTO E' NO** (sei compiti dal merge-base `2e728d6`; spec `docs/superpowers/specs/2026-09-23-metro-ritardo-design.md`, piano `docs/superpowers/plans/2026-09-23-metro-ritardo.md`, progetto d'onda `docs/superpowers/specs/2026-09-23-onda-e-architettura.md`). Non costruisce niente: **e' la misura che decide l'architettura**, per la regola del committente «lockstep prima; il server autoritativo solo se LA MISURA DICE che il lockstep non basta». **Le sei soglie sono state scritte e committate PRIMA che un banco girasse** (`df4da67`). **IL NO ESCE DALLA GAMBA D, e non e' il ritardo: e' IL MOTORE JAVASCRIPT.** `_q-determinismo` diceva «due telefoni vedono la stessa partita» facendo **un solo chromium.launch()** e due contesti della stessa istanza: la parola telefono era un'inferenza. `strumenti/_q-motori.js` apre Chromium (V8), WebKit (JavaScriptCore) e Firefox (SpiderMonkey): **8 semi su 8 divergono al secondo 1**, punteggi `1-1` contro `2-0`, `0-0` contro `0-2`, sorteggi 5.123 contro 4.284. **La causa e' `Math.hypot`**, che ECMA-262 lascia approssimata: V8 sbaglia l'ultimo bit su **100 e 103 valori su 200**, e il gioco la chiama 33 volte — una e' `const len=(x,y)=>Math.hypot(x,y)` a `:8710`, la distanza. **E OTTO SEMI HANNO CORRETTO TRE SEMI**: con `hypot` riscritto come `sqrt(x*x+y*y)` (lecito per norma, `sqrt` e' correttamente arrotondata) a tre semi i motori tornavano identici e il verbale avrebbe detto «il guasto e' una riga»; a otto, sette convergono e uno no (seme `20260929`, secondo 44). Restano `sin` 7/200, `cos` 3/200, `tan` 9/200, `exp` 2/200, che il gioco chiama 160, 104, 2 e 44 volte. **Prerequisito nuovo, non previsto dal progetto d'onda: LA MATEMATICA SCRITTA IN CASA** (trascendenti in JS dalle sole operazioni IEEE-esatte) **prima del #145**. **GAMBA A — 120 nastri, partite intere**: gol della squadra comandata 1,01+-0,07 a K=0, 0,78+-0,08 a K=12; specchio 1,27+-0,09 e 1,03+-0,10; **danno a 200 ms 23,1%+-20,7% (estremo alto 43,8%)**, D_gioco misurato **0 tick** contro i 12 richiesti. **MA LA CURVA E' UN GRADINO, NON UNA RAMPA**: danno a 50 ms 29,8%, a 300 ms 28,9%, scarto 0,01+-0,22 — **non distinguibili**. Il costo si paga tutto nei primi 50 ms, quindi **scegliere D fra 3 e 18 tick e' quasi gratis e il margine contro il jitter si compra senza pagarlo**; e non esiste un D piccolo che eviti il danno. La **fedelta'** invece cala davvero, 0,786 -> 0,174. **Il banco si e' condannato da solo tre volte**: su 3 nastri da 15 s con zero gol e zero tiri stampava «SOGLIA TENUTA» misurando il nulla; a 24 nastri l'errore della media (18%) era quasi la soglia (25%) e adesso il danno si stampa col suo intervallo e chiede **119 nastri**; e porta tre prove che guardano il BANCO (la traslazione trasla nel campo giusto? il metro varia? il controllo negativo — senza NESSUN comando — morde?). **GAMBA B — nessuno dei cinque verbi muore a 200 ms** (6/6 ciascuno, semi appaiati), e **la carica NON e' il punto fragile che si temeva**: 6/6 in finestra, valori 0,63-0,65, perche' **un ritardo uniforme non cambia la durata di un gesto tenuto** (ritarda pressione e rilascio della stessa quantita'). Il 95% **non e' deciso** da 6 tentativi e il banco lo dichiara: ne servirebbero 60 per verbo e per K. **GAMBA C — NON ESEGUITA, RICHIEDE IL COMMITTENTE**: pronti l'aggancio `__test.ritardo(K)`, lo strumento `_prova-umana-ritardo.js` (K in cieco, sigillo dell'ordine scritto prima della prima partita, riscaldamento non votato) e il protocollo `docs/superpowers/plans/2026-09-23-protocollo-prova-umana.md`. **Si gioca col dito, non coi tasti**: la tastiera entra dritta in `Keys[e.code]` e non subisce il ritardo, e lo strumento **scarta il voto** se trova righe di tastiera nel nastro. **TRE FALSI, 7/7**, ognuno nel caso peggiore e ognuno morso dalla prova che il suo file dichiara: `traslazione-sorda` (muove metadati e il tick del dischetto, i soli campi che nessuno legge) -> 0a; `traslazione-cieca` (ritarda solo i touchmove: la levetta sbanda, i verbi partono in orario) -> 0a; `ritardo-attestatore` (traslazione onesta, legge i numeri della CPU come quelli del comandato — passa 0a e 0b) -> 0c. Piu' il **controllo positivo**, la meta' che manca a quasi tutti i banchi di falsi. **DUE AGGANCI ADDITIVI** (`_toppa-141-ritardo.js`, 2 ancore): la coda del ritardo avvolge **piu' fuori** del registratore, cosi' il comando si registra al tick in cui ESEGUE; **`MOTORE_V` resta 2, MISURATO** (gioco nuovo e vecchio giocano la stessa identica partita, stesse 627 righe). **DUE RETTIFICHE A EDIZIONI**: la prova C di `_q-determinismo` era **inerte** (`__test.dita` non esisteva) e prometteva la misura dell'INGRESSO che nessuno faceva — adesso il banco fa **11/11**; e INV-14 di `_q-invarianti` non e' N/A, e' **parzialmente vera** (il DELETE che consuma l'impegno, il nastro al posto dell'hash), scoperta solo la firma. **BATTERIA INTERA a cinque gruppi, tutti i cancelli che contano VERDI**; reti di sicurezza a cantiere chiuso: `_q-duello-impronta` 44/44, `_q-giudice` 21/21, `_q-sigillo` 14/14, `_q-carta` 22/22, `_q-amici` 23/23, `_q-sospetto` 39/39, `_q-staffetta` 42/42, `_q-finestra` 20/20, `_q-glicko` 58/58, `_q-invarianti` 12/12, `_q-sfida` 54/54, `rete/prove/tutte.js` 46/46. **E un rosso che NON era una regressione**: al primo giro `sfida` e' uscito 5/6 su «ogni sfida senza duello si lascia rivedere»; misurato **tre volte** prima di scrivere quella parola — da solo sul gioco di oggi 54/54, da solo sul gioco di `main` 54/54, e col gruppo a `--ripetuto 2` 54/54 e 54/54, col banco che dichiara la corsa stabile. Era rumore. **IN BATTERIA**: `ritardo` (conta, modo `--solo-banco`), `ritardo-falsi` (conta, 7/7), `verbi-ritardo` (conta, `solo:true`, porta stretta: «un verbo MUORE», non il 95%), `motori` (**conta:false**, oggi rosso 12/23 per un difetto vero e aperto — metterlo a true tingerebbe di rosso l'intera batteria per un guasto gia' a registro). Verbale completo in `MANUALE.md` §A registro, voce #141, IN CIMA |
| 4 | **#140 CANTIERE CHIUSO - IL RATING NASCOSTO, GLICKO-2 (il punto 13 del programma, l'ultimo dell'onda D)** (cinque compiti dal merge-base `562e62e`; spec `docs/superpowers/specs/2026-09-23-glicko-design.md`, piano `docs/superpowers/plans/2026-09-23-glicko.md`). Cantiere di SERVER: `git diff main -- CALCETTO-il-gioco.html` **vuoto**, `MOTORE_V` resta 2. **LA DECISIONE, presa con una misura e non con una lettura**: il rating nascosto **AFFIANCA** i punti visibili, non li sostituisce. Il mandato si puo' leggere nei due modi e i due modi stanno scritti in due righe diverse dei nostri documenti (`MANDATO-STADIUM-ROAR.md:163-164` contro `MAPPA-MANDATO.md:479`). Si e' fatta la (a) perche' **i punti di CALCETTO non sono un rating e non possono diventarlo**: il difensore perde meta' di quel che l'attaccante guadagna, la serie moltiplica fino a 1,3, c'e' un pavimento a 100 e contro i fantasmi si prende meta'. Tutte e quattro CREANO valore dal nulla ed e' giusto che lo facciano - sono una VALUTA che premia il giocare. Misurato su 400 allenatori e 18.546 sfide simulate: **+1,6% di totale in sessanta giorni**. E la (b) riscriverebbe la classifica di tutti in una notte senza avere il posto dove farlo, perche' il committente ha ESCLUSO ogni azzeramento stagionale. **LA VERIFICA CONTRO L'IMPLEMENTAZIONE DI RIFERIMENTO** (il cancello che il mandato chiede per nome, milestone M9): l'esempio lavorato di Glickman riprodotto **numero per numero** - g 0,9955/0,9531/0,7242, E 0,639/0,432/0,303, phi* 1,1529, phi' 0,8722, mu' -0,2069, **r' 1464,05 contro i 1464,06 del paper**, **RD' 151,52 esatto**. I due valori che non coincidono (v 1,77898 contro 1,7785, Delta -0,48393 contro -0,4834) NON sono stati fatti passare allargando la tolleranza: il banco rifa' il conto **con i g e gli E stampati dal paper** e ritrova esattamente 1,7785 e -0,4834, cioe' dimostra che lo scarto e' l'arrotondamento del paper. **I TRE NUMERI** (`nascosto` 1500, `incertezza` 350, `volatilita` 0,06) piu' `periodo` e `giri` sono **cinque colonne su `punti`**, non una tabella nuova: RLS e' acceso con zero policy su tutte e sei le tabelle, e una tabella nuova sarebbe l'unica porta aperta del database - in silenzio. `periodo` sta apposta accanto a `stagione`, perche' e' li' che le due si confonderebbero: **il giorno nuovo non azzera niente**, il rating resta quello di ieri e a crescere e' solo l'incertezza. **L'ABBINAMENTO USA IL RATING NASCOSTO**, estendendo la finestra del #137 e non riscrivendola: il gradino era un numero, poi una coppia, adesso e' una TERNA. E la terza coordinata **non e' una distanza**: misurato, confrontare i rating a distanza porta lo scarto di abilita' vera da 136 a 122 (cioe' niente, perche' l'incertezza di due assestati vale 124 e si mangia la banda); confrontare l'**ATTESO** di Glicko-2 - dove l'incertezza di tutti e due entra per costruzione - lo porta a **72**. Misurato su 5000 ricerche, e la grandezza non e' lo scarto di PUNTI (sarebbe giudicare un metro con se' stesso) ma lo scarto di **ABILITA' LATENTE**: **400 allenatori 133->72** (p90 355->183, entro 100 dal 40% al **65%**), **60 allenatori 151->94**, **12 allenatori 291->185** (p90 554->391). **Zero ricerche in piu' senza avversario**, su tutte e tre. **IL BANCO HA CORRETTO IL PROGETTO TRE VOLTE**, ed e' la parte che vale piu' del codice: (1) i gradini sono **cinque** e non quattro, perche' con quattro, su una base di dodici, stringere sopra faceva cadere la ricerca sull'ultimo - che per costruzione non ha limiti - e **la coda PEGGIORAVA**, p90 da 554 a 666; il quinto gradino e' un atterraggio, non un muro. (2) il pavimento e' **7/5/4/2/1** e non 8/6/4/1, perche' 8/6/4 **disfaceva una misura del #137** (lo scarto mediano di punti sulla base da dodici da 147 a 213). (3) **7/5/3 e' stato scartato da un SECONDO SEME**: su 20260923 dava quattro avversari possibili e sembrava a posto, su 987654 e 555 ne dava TRE - da li' il banco prende `--seme`, e i suoi 58 controlli sono verdi su quattro popolazioni. **IL RATING NON ESCE DAL DATABASE**, come il sospetto del #137 e per la stessa ragione (la tupla di `trova_avversario` finisce dritta nel corpo della risposta): quindi `ammissibile` non puo' ricontrollare la terza coordinata, e il prezzo e' scritto in chiaro. I miei due numeri non li legge nemmeno l'endpoint: se li prende la CTE `mia`. **SEI FALSI**, ognuno nel caso peggiore, bite list misurata su 58 prove: `ferma` 3, `cresce` 11 (e **passa A9** - il rating resta esatto, sbaglia solo la certezza), `sorda` 2 e **PASSA IL GRUPPO A** (nell'esempio del paper la differenza fra ricalcolare la volatilita' e lasciarla ferma e' di quattro milionesimi, sotto la precisione pubblicata), `visibile` 4 e cade **solo sulla misura** perche' cambia JavaScript e SQL insieme, `stagione` 1, `fantasma` 1. E **`stagione` ha trovato un buco**: passava tutte e 57 le prove, perche' nessuna chiedeva che il rating di ieri sopravvivesse alla notte - E4b e' nata da li'. **`_q-glicko` in batteria** con `conta:true`, 58/58, quattro secondi, non apre il gioco. **LIMITI DICHIARATI**: l'SQL non si esegue (non c'e' un Postgres nel repo, e il gruppo D dice di attestare); **un rating solo, non uno per modo** come chiede il mandato - misurato perche': spezzare in tre la stessa evidenza porta l'incertezza mediana da 63 a 88 su 400 e da 65 a 93 su 12, e un rating piu' incerto abbina peggio; nessun trofeo, nessuna lega, nessun piazzamento contro bot (i nostri bot non sono calibrati); la popolazione e' simulata. |
| 5 | **LO STATO VERO, oggi** (sostituisce la riga 15 della giornata del 22 settembre, che va letta come **RIGA SUPERATA** per il conto dell'onda D). **Onda C CHIUSA** (3/3). **RETTIFICA A EDIZIONI (23 settembre 2026).** «ONDA D CHIUSA», scritto dal #137 in poi e ripetuto dal #138 e dal #139, **era vero sui punti 11 e 12 del programma, non su tutta l'onda**. L'onda D ne ha **TRE** (`_analisi/MAPPA-MANDATO.md:763-768`): 11 il verificatore differito, 12 gli amici, e **13 il Glicko-2**. Il 13 non era fatto, e i cantieri #130-#139 non lo toccavano: nessuno dei tre verbali diceva il falso su quel che aveva fatto, ma tutti e tre chiamavano «chiusa» un'onda a cui mancava un punto su tre. Il testo vecchio resta dov'e'. **ADESSO L'ONDA D E' CHIUSA DAVVERO, 3 punti su 3**: il metro (**#130**), il duello nel nastro (**#131**), i cinque canali (**#132**), il giudice (**#133**), il sigillo (**#134**), la sfida di carta (**#135**), la classifica degli amici (**#136**), l'abbinamento e il sospetto (**#137**), la staffetta (**#138**), la finestra che cambia (**#139**) e **il rating nascosto (#140)**. **Onda E** (live 1v1) non aperta. **Seguiti aperti, nessuno bloccante**: tutti quelli della riga 15 del 22 settembre restano - i tocchi indipendenti dallo schermo (dal #133, il piu' grosso), la sfida di carta a 7 e a 11 e senza giudice (#135), `SAVE.diff` (PRE-ESISTENTE), i due del #136, i tre del #137, i tre del #138, il resize durante un REPLAY (#139) - piu' i **quattro nuovi del #140**: **un rating solo e non uno per modo** come chiede il mandato (misurato perche', e riapribile quando la base cresce), **il periodo di rating e' di una partita e non di un giorno** (la divergenza dalle giornate e' misurata: scarto mediano 25 punti, massimo 50), **la terza coordinata dell'abbinamento non si puo' ricontrollare fuori dal database** (il rating nascosto non esce, ed e' il prezzo dichiarato), e **nessun piazzamento contro bot calibrati** (i nostri bot non sono calibrati: calibrarli e' un cantiere a parte). Piu' INV-06/07, **#118** (MIND v2), **#123**, la rosa canonica nelle sfide, il tetto empirico della fase `power`, la tastiera nel nastro (#131), il tetto delle 40.000 righe, il nome di un uomo di rosa non validato, la rosa che il server rilegge viva (#132), piu' i fuori-mandato **#89, #97, #99, #101-#106, #109, #115** |
| 6 | **#143 CANTIERE CHIUSO — LA MATEMATICA IN CASA: tre motori JavaScript fanno la STESSA partita, e il lockstep dell'onda E smette di essere impossibile** (sei compiti dal merge-base `a2607d0`; spec `docs/superpowers/specs/2026-09-23-matematica-in-casa-design.md`, piano `docs/superpowers/plans/2026-09-23-matematica-in-casa.md`). È **la cura vera del blocco che il #141 aveva misurato** (0 semi concordi su 8) **e che il #142 aveva solo aggirato** curando l'accusa ingiusta da ripiego dichiarato. **LA CAUSA È UNA RIGA DI NORMA**: ECMA-262 lascia le trascendenti «implementation-approximated», e su 200 valori irrazionali fra V8/JSC/SpiderMonkey `hypot` differisce su **103**, `atan2` 24, `exp` 21, `tan` 9, `sin` 7, `log` 6, `cos` 3 — `sqrt` e `pow` **0** (la prima per norma, la seconda per fatto). **IL PERIMETRO È MISURATO, NON DEDOTTO** (`_q-perimetro.js`): **452 siti eseguibili**, contati da uno scanner che salta i commenti perché metà del file parla di `Math.hypot`; **53 dentro la simulazione, 100 solo nel disegno, 74 solo al caricamento, 225 mai accesi**; **243 chiamate per passo, 1187 per fotogramma**; sporcandone una per volta di un ulp: dentro sin/cos/exp/atan2/hypot/pow/sqrt, fuori log, **tan non misurata** (zero chiamate, che non è «innocente»). **LA CURA**: sette funzioni scritte a mano con sole operazioni che IEEE-754 obbliga a essere correttamente arrotondate (riduzione di fdlibm, π/2 in tre pezzi da 33 bit, polinomi minimax) e **370 chiamate su 370 dirottate** — non le 53 viste, perché i 225 mai accesi sono il rigore, la rimessa, la taglia 11 e i verbi che in CPU contro CPU non escono: innestare solo i misurati sarebbe l'elenco troncato già pagato dodici volte. **IL RISULTATO**: `_q-motori` passa da **0/8** a **20/20 semi concordi**, e sul dominio vero della partita la casa dà **zero differenze su 12.242 valori** mentre le native divergono (`hypot` 1120/3000, `atan2` 507/3000 su Firefox), con scarto **≤1 ulp** (2 per hypot) su un tetto di 4. **IL COSTO NON C'È, E TRE STRUMENTI DANNO LO STESSO SEGNO**: alla prima corsa `prestazione.js` **si è dichiarato cieco** (prova (a) −28,6% su un file contro sé stesso, ballo 1164%) e il suo referto è stato **buttato**; rifatta a banco meno occupato la prova (a) è passata con **risoluzione 4,6%** e il confronto appaiato col gioco di `main` dà **fotogramma medio −6,7%, tipico −1,2%, p95 −0,0%** (gli ultimi due sotto la risoluzione: lì non c'è differenza credibile). Il conto per funzione, fatto dove il lavoro dentro il cronometro è costante (200.000 chiamate identiche, minimo su sette ripetizioni), dice perché: **`hypot` in casa costa metà della nativa** e da sola fa 191,7 chiamate per passo sulle 276, così il saldo pesato è **−0,0026 ms a passo, −0,015% del budget di un fotogramma**. La cura **rende**. **`MOTORE_V` 2 → 3 deciso col numero** (`_t-143-motorev.js`): sei nastri di `main` rigiocati sul curato, **6 su 6 in una partita diversa**, tutti entro il quarto campione; i nastri vecchi vengono respinti con **ALTRO MOTORE/motore-diverso**, nessun accusato. **QUATTRO FALSI, tutti morsi** (`_q-casa-falsi.js` 9/9): `solo-hypot` — la cura che il #141 aveva in mano — **bocciata, 4 semi concordi su 8**, e il primo che la smaschera è il terzo (il numero di semi è parte della soglia, non un parametro del banco); `storta` passa la SOGLIA e cade sullo scarto in ULP (28 contro 4); `una-nativa` cade sulla guardia strutturale della toppa e su `_q-motori` (0/8); `impronta` rende piatta l'impronta del #142 (62859382 su tutti e tre) e cade sul confronto fra motori. | `_q-motori` **21/21**, SOGLIA-MOTORE tenuta · `_q-casa` 18/18 · `_q-perimetro` 5/5 · `_q-casa-falsi` 9/9 · `_t-143-motorev` 7/7 |
| 7 | **SETTE PROVE CHE NON POTEVANO PIÙ ESSERE VERDI, E CINQUE ANCORE RITARATE — nessuna era una regressione** (voce #143, compiti 4 e 5). `_q-motori` chiudeva con «21 controlli, 14 passati, **7 falliti**» *seguito da* «SOGLIA-MOTORE TENUTA» e uscita 0: le sette erano le sette native che divergono, rosse **per costruzione** (la norma permette quella divergenza) e per giunta cieche, perché dal compito 3 il gioco non ne chiama più nessuna. Uno strumento che conta fra i propri fallimenti righe che non possono passare insegna a leggere i suoi rossi senza crederci. Adesso **T è una misura dichiarata** più i due verdetti che può dare onestamente (almeno una nativa diverga ancora — altrimenti la prova nuova sarebbe verde per il motivo sbagliato; `sqrt` e `pow` concordi, che sono le due lasciate native apposta), e il cancello che mancava è la **prova H nuova**: le sette **di casa**, lette da `window.Msin` sulla pagina del gioco vero e non da una copia del banco. **21 su 21**, e l'uscita riparata (un rosso fuori dalla prova B adesso è un rosso). **LE CINQUE RITARATURE, ognuna misurata anche su `main` con `--gioco`**: (1) `_q-mira` PIENO-IDENTICO — la traccia diverge di **due ulp** con gli stessi indici, e la prova cambia forma e non severità (identità esatta se il motore è lo stesso, altrimenti stessi bersagli e scarto sotto 1e-9; misurato 2,4e-14, e su `main` l'identità esatta si ottiene ancora); (2) **`_nastro-duello-congelato.js` rigenerato** — portava `MOTORE_V` 2 e quattro banchi in una volta lo respingevano con ALTRO MOTORE (`staffetta` su sette prove): il rifiuto era **giusto**, e il generatore prevedeva già questo giorno — ma il suo modello aveva perso due paragrafi di sapere pagato, corretti prima di rigenerare; (3) `_q-giudice` prova H — col seme storto la rigiocata incontra un calcio piazzato e il giudice **si astiene**, che è la dottrina del #133 al lavoro: adesso si provano **quattro semi** e ne basta uno (misurato `+7` astensione, `+13` NON TORNA); (4) **`_q-sfida` sezione 10, una prova ferma al 21 settembre che nessuno aveva più svegliato** — pretendeva che una partita col dischetto non si facesse vedere *affatto*, ma il **#131** ha messo il duello nel nastro: era rimasta verde perché il suo ramo `else` regala un verde quando nessuna sfida della sessione incontra un calcio piazzato, e sul gioco del #143 ne è capitata una. Riscritta (o il replay parte e finisce col punteggio dichiarato, o non parte e il gioco dice la causa) ha misurato al primo colpo **tabellone 3-2, replay 3-2**: la promessa del #131 verificata da capo a fondo per la prima volta; (5) `_q-motore-nastro` A1 e la bite list di `_q-motore-falsi` — l'esercizio del #142 **non riesce più, ed è la cura**: il nastro mascherato rigioca e **TORNA 6 su 6**, quindi `A2` non può più mordere i falsi `muto` e `piatto` (col nastro che rigioca uguale, **nemmeno un giudice rotto accusa un onesto**) e passa da `cade` a `tiene` per quei due, che restano bocciati da `E/F/F2` e `C/F/F2`. **Il #142 non decade**: restano fuori dalla cura il disegno, `pow`/`sqrt` e tutti i nastri di prima. **`_q-duello-impronta` (44/44), `folla`, `seme`, `_q-volo`, fuzzer, soak, `_q-carta`, `_q-amici`, `_q-glicko`, `determinismo` e `determinismo-11` non hanno avuto bisogno di niente.** | `_q-mira` 5/5 · `_q-giudice` 21/21 · `_q-sfida` 56/56 · `_q-motore-nastro` 11/11 · `_q-motore-falsi` 14/14 · `staffetta`/`finestra` verdi dopo la fixture |
| 8 | **LO STATO VERO, oggi** (sostituisce la riga 3 qui sopra, che va letta come **RIGA SUPERATA** per quel che dice dell'onda E). **Onda C CHIUSA** (3/3). **Onda D CHIUSA, 3 punti su 3** (#130-#140). **ONDA E: il primo ostacolo è tolto.** Il #141 aveva misurato che due telefoni divergono da soli, senza che la rete c'entri niente, e aveva dato **NO** al lockstep puro; il #142 aveva curato il critico di produzione che ne derivava, da ripiego dichiarato; il **#143 toglie la causa**: le trascendenti della simulazione sono scritte in casa, Chromium, WebKit e Firefox vedono la **stessa partita** su venti semi su venti, e `MOTORE_V` è a **3**. Il lockstep dell'onda E **non è più impossibile** — ma non è ancora fatto: restano la rete, il ritardo (il **metro** del #141 esiste già, `_q-ritardo`) e il protocollo. **Batteria intera verde a sei gruppi** (19/19, 14/14, 5/5, 6/6, 15/15, più i cronometrici con `prestazione` verde), e a chiusura una corsa intera dei non-lenti in un colpo solo: **47 cancelli in 706 s, i 46 che contano tutti passati**; `audio` e `avvio-telefono` escono **3** per il banco (niente audio, nessun telefono collegato) e **un 3 non accusa il gioco**. **In batteria da oggi**: `motori` a `conta:true` (la riga che lo teneva informativo diceva «finché il guasto non ha un cantiere»), più `casa`, `perimetro` e `casa-falsi`, tutti `lento`+`solo` perché aprono tre motori veri. **Seguiti aperti, nessuno bloccante**: tutti quelli della riga 3 restano, più i **quattro nuovi del #143** — il **disegno** non passa da casa (1187 chiamate per fotogramma restano native: non spostano la partita, ma il numero è dichiarato), **`pow` e `sqrt` native per misura e non per norma** (con due righe rosse pronte il giorno che cambiasse), **`tan` non misurata** nel perimetro e innestata lo stesso, e il **determinismo pieno vale a taglia 5** (voce #98). | — |

## La giornata del 22 settembre, in breve

| | cosa |
|---|---|
| 1 | **#133 CANTIERE CHIUSO — IL GIUDICE (cuore dell'onda D)** (cinque compiti dal merge-base `e7aa605`; spec `docs/superpowers/specs/2026-09-22-il-giudice-design.md`, piano `docs/superpowers/plans/2026-09-22-il-giudice.md`). Cantiere di MOTORE, tutto per ancore (`_toppa-giudice.js` 4 ancore, `_toppa-giudice-schermo.js` 6, `_toppa-133-motorev.js` 1 di solo commento, e la correzione di revisione del 22 settembre `_toppa-giudice-schermo-ignoto.js` 1); `git diff main -- CALCETTO-il-gioco.html` **384/6** (era 375/6 prima della correzione). **LA DECISIONE D'ARCHITETTURA**: il verificatore differito **vive NEL FILE, non nel server** — un secondo motore in Node divergerebbe per costruzione e ogni divergenza toglierebbe punti a un innocente. `giudica(nastro, atteso, {seme, taglia})` è `Sfida.guarda` resa chiamabile senza schermo. **CINQUE VERDETTI, e uno solo muove punti**: TORNA · **NON TORNA** (l'unico su cui sia lecito agire) · INCOMPLETO (nove cause distinte) · ALTRO MOTORE · NON FINISCE. **IL CONTRATTO**: forza `sponde:'gabbia'`/`miraGuidata:'pieno'`; legge le rose DAL NASTRO e — più stretto di `Sfida.guarda` — si rifiuta invece di ripiegare sul profilo vivo; non muove mai un punto su un verdetto diverso da NON TORNA; è ripetibile. **Tetto = `tettoFotogrammi(taglia)`** (voce #130), stringibile ma non allargabile. **Il salvataggio si fotografa e si rimette com'era** (misurato: una partita che finisce muove `SAVE.lastRes` e `SAVE.inviti` anche senza pagare niente) e `imparaIndole` non gira durante un giudizio. **IL BANCO A TRE PAGINE**: la terza non si collega, non gioca, ha la rosa vergine e le impostazioni locali sbagliate apposta — così la prova B prova il contratto, non solo la funzione. **SETTE FALSI, ognuno passa tutte le prove tranne la sua**: `cieco` C/H/M, `vivo` B/B2/M, `sordo` la sola F, `fisso` la sola K, `locale` B/C/M, `lento` I/M, `sbadato` G/M. **TASSO DI FALSI «NON TORNA» SU PARTITE ONESTE: 0 su 14** (10 sfide vere a taglia 5, 2 a 7, 2 a 11 — **14 TORNA su 14**); nomi e audio non spostano un verdetto. **RETTIFICA A EDIZIONI (22 settembre, correzione di revisione)**: 0/14 è compatibile con un tasso vero fino a **~19%** (regola dei tre) e vale SOLO nella condizione misurata (schermo uguale o dichiarato) — non «il tasso è zero». **MOTORE_V RESTA 2, MISURATO** (`_t-132-motorev.js --prima fuori/gioco-133-base.html`): **30 su 30 identici**, zero nulli, uno dal dischetto; nessun `_t-133-motorev.js` scritto (una copia è un posto in più dove la stessa ferita si riapre). **CANCELLI**: `_q-giudice` 0/16 → 18/18 → **21/21** (correzione di revisione) e **registrato in `tutti.js` con `conta:true` insieme alla cura**; `_t-giudice-schermo` 2/5 → **5/5**; impronta del duello **44/44 a ogni compito**; i quattro cancelli del #132 verdi a ogni compito; batteria intera a sei gruppi a ogni compito, tutti i cancelli che contano VERDI (`audio` 3 in compagnia e **28/28 da solo**, `avvio-telefono` 3 senza telefono, `istantanea` informativo 45/56 come sempre).

**CORREZIONI DI REVISIONE (22 settembre 2026, voce #133), un commit a parte dopo il «Ready to merge: YES previa correzione», due IMPORTANTE e due MINORE, nessun CRITICO.** (IMPORTANTE-1) il giudice rifiutava lo schermo DIVERSO ma non lo schermo IGNOTO (`schermoDelNastro()` torna `null` su un nastro senza riga 10, e `if(sc && ...)` era falso su `null`: PROCEDEVA alla cieca) — misurato: una sfida onesta del gioco `e7aa605` (senza riga 10) rigiocata dal gioco nuovo dava **NON TORNA** su tre schermi su quattro (`1024x460`, `844x390`, `800x360`), un innocente accusato, protetto finora **per accidente**. Cura una riga, `if(!sc) return fermo('INCOMPLETO','schermo-ignoto')`, costo zero sui nastri veri; nuova prova Q. (IMPORTANTE-2) nessuna asserzione condannava i «non lo so» prodotti DURANTE la rigiocata (`duello-senza-righe`, `rigiocata-esplosa`) — due falsi del revisore passavano 18/18. Cura: `_nastro-duello-congelato.js`, una sfida vera con un duello NATURALE (forzare `rigori()` fuori banda rompe la ripetibilità, provato e scartato), che rigiocata intatta TORNA (prova R) e senza le righe del duello dà `duello-senza-righe` **deterministico** (prova S); il falso `_crit-giudice-accusa.js` costruito e **BOCCIATO** (cade solo su S, 20/21). `rigiocata-esplosa` resta dichiarata NON ESERCITATA — nessuna costruzione deterministica trovata in tempo ragionevole. (MINORE-1) «0 su 14» rettificato sopra. (MINORE-2) devicePixelRatio **MISURATO** (1, 2, 3 sulla stessa finestra CSS): verdetto, gol e passi identici — canale confermato innocuo. Verbale completo in `MANUALE.md` §A registro, voce #133, IN CIMA |
| 2 | **IL SESTO CANALE: LO SCHERMO — trovato misurando, ed è il più grosso di tutti** (voce #133, compito 3). Il terzo canale sospetto era la finestra, in lista per la ragione SBAGLIATA (i sorteggi della cosmetica, che la voce #129 ha spostato su `DECO` e che **non c'entrano**: quella cura tiene). **Il canale passa dai PIXEL**: il nastro registra i tocchi in coordinate di SCHERMO, e dove finisce un tocco lo decidono `touchBtnLayout` e `SCALE/OX/OY`, che derivano tutti da `innerWidth`/`innerHeight`. Lo stesso tocco a (841, 342) preme il disco grande su 915x412 e **non preme niente** su 800x360. MISURATO su nove viste, stesso nastro, partita dichiarata 3-4: `915x413`/`916x412`/`930x412`/`915x430` tornano 3-4, `1024x460` **NON TORNA 1-3**, `1280x720` **INCOMPLETO 0-4**, `844x390` e `800x360` **NON TORNA 0-3**. Non è una lama: quindici pixel non spostano niente, settanta spostano tutto. **E C'ERA GIÀ IN PRODUZIONE**: il replay vero fra due telefoni di schermo diverso diverge 3 volte su 3, e il gioco scrive testualmente **«La squadra di chi ti ha attaccato è cambiata da allora»** — l'innocente accusato, la stessa frase che il #132 ha tolto di mezzo cinque volte, detta quasi sempre perché due telefoni con lo stesso schermo sono l'eccezione. **CURA** (non la definitiva): **riga di tipo 10** col formato dello schermo; il **giudice** su uno schermo diverso si RIFIUTA (`INCOMPLETO/schermo-diverso`) **e dichiara quale schermo serve**, così il verificatore riapre il browser di quella misura e giudica davvero; il **replay** il film lo mostra lo stesso (rifiutarlo spegnerebbe la funzione per quasi tutti) ma dà la CAUSA VERA. **SEGUITO APERTO, e non piccolo**: registrare i tocchi in coordinate che non dipendono dallo schermo — finché non è fatto, un replay fra telefoni diversi resta un film approssimato, e solo il giudice se ne accorge |
| 3 | **LO STATO VERO, oggi** (sostituisce la riga 5 della giornata del 21 settembre, che va letta come **RIGA SUPERATA** per la parte «resta dell'onda D»). **Onda C CHIUSA** (3/3). **Onda D**: il metro (**#130**), il P0 (**#131**, il duello nel nastro), i falsi positivi (**#132**, i cinque canali) e **il giudice (#133)** sono chiusi. La capacità di verificare una sfida senza schermo **c'è ed è misurata**: 14 partite oneste su 14 tornano, zero innocenti condannati **nella condizione misurata** (schermo uguale o dichiarato — rettifica a edizioni, 22 settembre, correzione di revisione: fuori da quella condizione, schermo IGNOTO, il giudice PRIMA della cura ne accusava uno, ora curato). **RESTA dell'onda D**: il **lavoratore lato server** — il ciclo che pesca le righe con `verificata=0`, apre un browser headless della misura che il nastro dichiara, chiama `giudica` e scrive `verificata=-1` con `muovi_punti(-delta)` sui soli NON TORNA; e da lì il **punteggio di sospetto continuo** (colonna `sospetto`, oggi inutilizzata). **Onda E** (live 1v1) non aperta. **Seguiti aperti, nessuno bloccante**: **i tocchi indipendenti dallo schermo** (nuovo, dal #133, ed è il più grosso), INV-06/07, **#118** (MIND v2), **#123**, la rosa canonica nelle sfide, il tetto empirico della fase `power`, la tastiera nel nastro (#131), il tetto delle 40.000 righe, il nome di un uomo di rosa non validato, la rosa che il server rilegge viva (#132), più i fuori-mandato **#89, #97, #99, #101-#106, #109, #115** |
| 4 | **#134 CANTIERE CHIUSO — IL SIGILLO** (quattro compiti dal merge-base `6dee72b`; spec `docs/superpowers/specs/2026-09-22-il-sigillo-design.md`, piano `docs/superpowers/plans/2026-09-22-il-sigillo.md`). Quarto cantiere dell'onda D: le tre voci precedenti hanno costruito il GIUDIZIO, questa lo fa arrivare **all'occhio di chi gioca**. Cantiere di MOTORE **e di SERVER**: gioco per ancore (`_toppa-sigillo-vaglio.js` 11 ancore, `_toppa-sigillo-riga.js` 4, `_toppa-sigillo-guarda.js` 6 di cui una a quattro teste), `rete/api/sfida.js` con Edit. `git diff main -- CALCETTO-il-gioco.html` **248/80**; `git diff main -- rete/` **28/1**. **IL MURO ERA ALTO DUE MATTONI, misurati prima di scrivere una riga**: (1) `grep -c verificata rete/api/*.js` = **1**, ed era dentro a un commento — la colonna esiste in `schema.sql:125` con tanto di indice parziale e non era nella `select` del GET; (2) `Sfida.dipingi` stampava quattro cose per riga e nessuna era la verifica. **LA DECISIONE CHE CONTA: una porta sola.** I nove controlli sul nastro stavano scritti DUE VOLTE (in `giudica` e in `Sfida.guarda`) e i due elenchi **non erano identici** — finche' ne usciva un film costava manutenzione, dal momento in cui ne esce un verdetto in lista la riga direbbe «non torna» dove il verificatore differito direbbe «incompleto». `vagliaNastro(righe)` e' quella porta: il giudice si ferma su tutti e nove, `Sfida.guarda` su quattro, e sugli altri quattro (`rose-assenti`, `carattere-assente`, `schermo-ignoto`, `schermo-diverso`) **mostra il film lo stesso ma non da' verdetto**. Comportamento visibile invariato: `_q-giudice` 21/21 e `_q-sfida` 54/54 prima e dopo. **LE CINQUE PAROLE**: `DA VERIFICARE`/`VERIFICATA`/`NON TORNA` dal server, `TORNA`/`NON VERIFICABILE` da questo telefono. **DA VERIFICARE e non «da guardare»** (la parola dello schema): nella riga ci sono gia' il pallino ambra e il bottone GUARDA. **TORNA e non VERIFICATA** quando lo dice il telefono: chi difende e' parte in causa, puo' dire che cosa ha visto, non timbrare la classifica — e infatti **il verdetto locale NON parte per la rete** e si vede **solo** dove il server dice ancora 0. **NESSUN INNOCENTE ACCUSATO ANCHE NELLE PAROLE**: NON TORNA resta l'unica parola che accusa, gli altri tre no del giudice diventano NON VERIFICABILE con la causa vera (`Sfida.causaSigillo`, sei cause in italiano) — e non e' teoria: due telefoni con lo STESSO schermo sono l'eccezione, e la #133 ha misurato 800x360 contro 915x412 che da' 0-3 dove il tabellone dice 3-4. **IL SERVER: una colonna e un freno.** `verificata` entra nella `select`; nessuna tabella nuova, quindi nessuna riga di RLS. E il GET ne era rimasto **senza freno** da quando e' stato scritto (il POST ha `sfida:` 30/60, la classifica `cla:` 60/60): buco pre-esistente trovato perche' la cura toccava la riga accanto, chiuso con `sfl:<id>` 60/60. **LA PIEGA, misurata**: a 800x360 con cinque righe CERCA AVVERSARIO chiude a **220**, la prima riga a **329** (era 308), il primo GUARDA a **308**; la riga passa da **46 a 67 px**, 31 px di margine. La fascia di riepilogo in cima — la tentazione del cantiere, difetto gia' pagato del TORNEO — porterebbe la prima riga a **418** su una piega di 360. **IL BANCO PRIMA DELLA COSA**: `_q-sigillo.js` nasce **0/5** al compito 1 e **5/14** al compito 2, finisce **14/14**; registrato in `tutti.js` con `conta:true` insieme alla cura (~13 s). Il gruppo A carica il modulo **vero** `rete/api/sfida.js` con `import()` dinamico e un `db` di carta che **onora la `select`** come PostgREST — un finto che restituisse la riga intera direbbe verde proprio nel caso da trovare. **CINQUE FALSI, ognuno rosso solo sulla sua prova**: `server-sordo` A1/A2, `muto` B1/B2/C5, `accusa` C3/C4b, `timbro` C2/C5, `fascia` B3. Il piu' importante e' **`accusa`**, costruito nel caso peggiore: dice TORNA quando torna, NON TORNA sul punteggio gonfiato, stampa i sigilli giusti — **passa dieci prove su dodici** e cade su una cosa sola, uno schermo diverso da quello del nastro. **MOTORE_V RESTA 2**: nastro e simulazione non toccati, e la prova sono impronta **44/44** e giudice **21/21** a ogni compito. Batteria intera a gruppi a ogni compito, tutti i cancelli che contano VERDI (`istantanea` informativo **45/56 sia sul gioco di oggi sia su quello di main** — misurato a due versioni, pre-esistente; `audio` 3 in compagnia e **28/28 da solo**; `avvio-telefono` 3 senza telefono). Verbale completo in `MANUALE.md` §A registro, voce #134, IN CIMA |
| 5 | **LO STATO VERO, oggi** (sostituisce la riga 3 qui sopra, che va letta come **RIGA SUPERATA** per la parte «resta dell'onda D»). **Onda C CHIUSA** (3/3). **Onda D**: il metro (**#130**), il duello nel nastro (**#131**), i cinque canali (**#132**), il giudice (**#133**) e **il sigillo (#134)** sono chiusi. Il verdetto adesso **esce dal database, attraversa la rete e si legge sulla riga della lista**, e il replay che si guarda col dito ne produce uno per conto suo — con la regola che una sola delle cinque parole accusa. **RESTA dell'onda D, ed e' l'ultimo pezzo**: il **lavoratore lato server** — il ciclo che pesca le righe con `verificata=0`, apre un browser headless della misura che il nastro dichiara, chiama `giudica` e scrive `verificata=-1` con `muovi_punti(-delta)` sui soli NON TORNA; e da li' il **punteggio di sospetto continuo** (colonna `sospetto`, oggi inutilizzata). Finche' non gira, tutte le righe valgono 0 e la lista dice «DA VERIFICARE»: e' la verita', non un ripiego. **Onda E** (live 1v1) non aperta. **Seguiti aperti, nessuno bloccante**: **i tocchi indipendenti dallo schermo** (dal #133, il piu' grosso — finche' non e' fatto, un replay fra telefoni diversi resta un film approssimato e la sua riga dira' NON VERIFICABILE), INV-06/07, **#118** (MIND v2), **#123**, la rosa canonica nelle sfide, il tetto empirico della fase `power`, la tastiera nel nastro (#131), il tetto delle 40.000 righe, il nome di un uomo di rosa non validato, la rosa che il server rilegge viva (#132), piu' i fuori-mandato **#89, #97, #99, #101-#106, #109, #115** |
| 6 | **#135 CANTIERE CHIUSO — LA SFIDA DI CARTA (il primo pezzo dell'onda D che non ha bisogno del server)** (cinque compiti dal merge-base `602a13e`; spec `docs/superpowers/specs/2026-09-22-sfida-di-carta-design.md`, piano `docs/superpowers/plans/2026-09-22-sfida-di-carta.md`). Cantiere di MOTORE e SCHERMATA, tutto per ancore (`_toppa-carta-codice.js` 8 ancore, `_toppa-carta-rettifica.js` 1 di solo commento, `_toppa-carta-schermata.js` 10); `git diff main -- CALCETTO-il-gioco.html` **604/13**, zero file di `rete/` toccati. **IL CODICE STA IN 79 CARATTERI**, una parola sola (`CARTA` + 70 simboli + 4 di controllo, base32 di Crockford senza I/L/O/U): dentro versione, `MOTORE_V`, taglia, seme a 32 bit, le due posture, l'indice di carattere, il punteggio da battere e i quaranta attributi delle due rose — 350 bit tondi. Ci sta in un SMS (160), misurato identico su 1000 sfide a caso. **IL DIVIETO CHE REGGE IL CANTIERE**: `codiceTrasferimento()` NON e' un codice da amici — chi lo incolla DIVENTA quella squadra — quindi nel codice della sfida non entra niente che dipenda da chi lo scrive (ne' id, ne' segreto, ne' nomi); se ne riusa solo la FORMA del controllo. Due serrature, una per verso, e chi incolla il codice del cambio telefono nel campo sbagliato riceve la frase giusta. **QUATTRO SIMBOLI DI CONTROLLO E NON UNO**, misurati esaustivamente: una cifra cambiata **2294/2294**, due scambiate **2624/2624**, 29.762 mutazioni a caso su 29.762 — con un carattere solo il falso misura **54,5%** sugli scambi. **LO SCHERMO NON ENTRA NEL CODICE, per misura**: sei codici, quattro viste e quattro salvataggi diversi, partita fino al fischio finale, **24 aperture tutte identiche** (punteggio, sorteggi, durata, posizioni a ogni campione, titolari con nomi e numeri). **L'unica cosa che puo' cambiare**: 28 nomi su 240, tutti rincalzi entrati dalla panchina, con gli stessi identici quattro numeri. **CINQUE CONTRO CINQUE**, e la ragione scritta al compito 2 era **sbagliata** — rettificata a edizioni nel gioco e nella spec: a sette i quattordici uomini hanno gli STESSI numeri e la partita finisce uguale; quel che manca e' che a sette e a undici **il codice non descrive tutta la squadra**. **NON PORTA UNA PROVA**, e sta scritto nel pannello: chi riceve il codice puo' dichiarare il punteggio che vuole — per un risultato verificato c'e' la SFIDA di rete, che il giudice ce l'ha. **LA PIEGA**, tre posti misurati prima di scegliere: sopra la lista rompe `_q-sigillo` B3 (prima riga a 375 su una piega di 360), sotto la lista non muove un pixel (CERCA@220, prima riga@329, GUARDA@308) e l'ingresso resta sopra la piega su tutte e tre le viste (347/347/471); il prezzo e' la barra dei tre bottoni 46 px piu' giu'. **ZERO RETE**, contata come delta dopo l'apertura della schermata: 0 richieste nuove in tutto il giro. **IL BANCO NASCE 0/19 e chiude 22/22** (`_q-carta`, in batteria `conta:true` `lento:true`, ~75 s), con **SEI FALSI** ognuno rosso solo sulla sua prova — il piu' importante e' `_crit-carta-segreto`, che infila sessanta bit dell'identificatore e passa **diciotto controlli su venti**, compresa la ricerca di sottostringhe: lo prende solo il confronto fra due telefoni con identita' diverse. `MOTORE_V` resta **2** |
| 7 | **LO STATO VERO, oggi** (sostituisce la riga 5 qui sopra, che va letta come **RIGA SUPERATA** per la parte «resta dell'onda D»). **Onda C CHIUSA** (3/3). **Onda D**: il metro (**#130**), il duello nel nastro (**#131**), i cinque canali (**#132**), il giudice (**#133**), il sigillo (**#134**) e **la sfida di carta (#135)** sono chiusi. Due persone adesso si sfidano **senza server**, con un codice di 79 caratteri che sta in un messaggio. **RESTA dell'onda D, ed e' sempre lo stesso ultimo pezzo**: il **lavoratore lato server** — il ciclo che pesca le righe con `verificata=0`, apre un browser headless della misura che il nastro dichiara, chiama `giudica` e scrive `verificata=-1` con `muovi_punti(-delta)` sui soli NON TORNA; e da li' il **punteggio di sospetto continuo**. **Onda E** (live 1v1) non aperta. **Seguiti aperti, nessuno bloccante**: **i tocchi indipendenti dallo schermo** (dal #133, il piu' grosso), **la sfida di carta a 7 e a 11** (nuovo, dal #135: il codice resterebbe di 79 caratteri, manca la descrizione dei due/sei uomini che oggi il telefono ricostruisce da se'), **la sfida di carta non ha un giudice** (nuovo, dal #135, e' una rinuncia dichiarata: un nastro non sta in un messaggio), **SAVE.diff riscritta a 1 da ogni sfida** (nuovo, PRE-ESISTENTE e non di questo cantiere: `Sfida.gioca` e `apriCarta` chiamano `startMatch(1, SFIDA_DIFF)` con `matchCtx` «friendly», e `persistSave` scrive `SAVE.diff=G.diff` — chi gioca una sfida si ritrova la difficolta' preferita rimessa a NORMALE; misurato leggendo il codice, non ancora su banco), INV-06/07, **#118** (MIND v2), **#123**, la rosa canonica nelle sfide, il tetto empirico della fase `power`, la tastiera nel nastro (#131), il tetto delle 40.000 righe, il nome di un uomo di rosa non validato, la rosa che il server rilegge viva (#132), piu' i fuori-mandato **#89, #97, #99, #101-#106, #109, #115** — **RIGA SUPERATA** dalla riga 9 qui sotto (la #136 ha chiuso, e due seguiti nuovi) |
| 8 | **#136 CANTIERE CHIUSO — LA CLASSIFICA DEGLI AMICI (il giro della sfida di carta si chiude)** (quattro compiti dal merge-base `66b17fe`; spec `docs/superpowers/specs/2026-09-22-classifica-amici-design.md`, piano `docs/superpowers/plans/2026-09-22-classifica-amici.md`). Cantiere di MOTORE, SALVATAGGIO e SCHERMATA, tutto per ancore (`_toppa-amici-codice.js` 9 ancore, `_toppa-amici-schermata.js` 7); `git diff main -- CALCETTO-il-gioco.html` **570/7**, zero file di `rete/` toccati. **IL PROBLEMA**: la sfida di carta (#135) va in un verso solo — mando 79 caratteri, tu giochi la mia partita, e il mio telefono non sapra' mai com'e' finita. **LA CURA**: un codice di **21 caratteri** (`ESITO` + 12 simboli + 4 di controllo, stesso alfabeto del #135) che torna indietro col risultato, e una classifica dei testa a testa che vive nel salvataggio locale. Dentro il codice **60 bit tondi**: versione, `MOTORE_V`, seme a 32 bit e i QUATTRO numeri dei due punteggi. Misurato 21..21 su 1000 risposte a caso, giro impacca-e-spacca **1000/1000**; sta in un SMS, in una riga da 80 colonne, e **si detta al telefono**. `ESITO` ha una I e una O, le due lettere che l'alfabeto butta via: il prefisso si confronta dopo la stessa normalizzazione del corpo, quindi `ES1T0` ricopiato a mano viene capito. **LE SERRATURE SONO TRE** (cambio telefono, sfida, risultato) e ognuna chiama le altre per nome invece di dire «codice sbagliato». **PORTA TUTTI E DUE I PUNTEGGI, ma se il telefono quel seme se lo ricorda VINCE IL RICORDO** (`SAVE.amici.mie`, ultime 20 sfide create): e' l'unica verifica possibile senza un server. **`SAVE.amici` E' ADDITIVA e il salvataggio resta v4**, misurato: su 37 chiavi di un salvataggio vissuto, togliere la chiave nuova non ne perde nemmeno una delle altre 36; venti amici pesano **+5.695 byte**, lo 0,143% del tetto di localStorage. Il prezzo, detto: **riaprire una versione VECCHIA del gioco cancella la classifica** (la whitelist la ignora e riscrive senza). **TRE TETTI E NESSUNO SILENZIOSO**: 20 amici (esce il piu' vecchio, e la riga lo dice per nome), 12 semi per amico (il doppione non conta due volte), 20 sfide create. **QUATTRO SIMBOLI DI CONTROLLO su CINQUANTA codici**: una cifra cambiata **24.800/24.800**, due scambiate **5.788/5.788**, 99,997% su 99.139 mutazioni — e **le fughe sono tutte la coppia 11+15**, cioe' la classe che nessun controllo puo' prendere (un codice valido di un'altra partita), detta invece che nascosta; con un simbolo solo gli scambi scenderebbero al **66,06%**. **LA CLASSIFICA STA SOPRA QUELLA DI RETE** (sotto, a rete piena, il primo amico finirebbe a **876 px**; sopra sta a 114/136) e **la schermata SFIDA non si tocca di un pixel** — CERCA@220, prima riga@329, GUARDA@308, SFIDA DI CARTA@347, e l'attrezzo confronta quella fetta byte per byte. **E UN DIFETTO DEL GIOCO SPEDITO, trovato misurando dove cadevano le aggiunte**: il pannello della sfida di carta e' alto 542 px su pieghe di 412/360 e `align-items:center` gli manda la cima a **-65/-91** dove **nessuno scorrimento arriva** (scrollHeight 493 contro 574) — il titolo stava a -44 e non si poteva leggere. Cura: `align-items:flex-start`, e **solo su quel pannello** (il cambio telefono e' alto 349 e la cima ce l'ha sempre avuta). **IL BANCO NASCE 0/22 e chiude 23/23** (`_q-amici`, in batteria `conta:true`, 17-18 s, ripetibile), e il gruppo C fa giocare i due telefoni **in due modi diversi** (uno CPU, uno col copione del pollice) perche' due CPU finirebbero pari per costruzione e uno specchio di pari e' uguale a se' stesso: misurato ANNA 0-1 contro BRUNO 1-3, «vinta» da una parte e «persa» dall'altra. **OTTO FALSI**, ognuno rosso solo sulla sua prova, e tre lezioni: (1) la ricerca di **sottostringhe non protegge niente** — il falso che fa viaggiare il nome della squadra la passa, perche' «DOPOLAVORO» esce scritto `D0P01A`; (2) **«la riga c'e' dopo un riavvio» non prova che sia stata scritta** — il gioco salva anche mentre la pagina se ne va, quindi C4 guarda il disco SUBITO; (3) **la rete va contata col server acceso**, in tre tacche (0 per il giro, 1 per la classifica di rete che chiede da sempre, 0 a rete spenta). `MOTORE_V` resta **2** |
| 9 | **LO STATO VERO, oggi** (sostituisce la riga 7 qui sopra). **Onda C CHIUSA** (3/3). **Onda D**: il metro (**#130**), il duello nel nastro (**#131**), i cinque canali (**#132**), il giudice (**#133**), il sigillo (**#134**), la sfida di carta (**#135**) e **la classifica degli amici (#136)** sono chiusi. Due persone adesso si sfidano **senza server** e il giro si chiude: 79 caratteri all'andata, 21 al ritorno, e su tutti e due i telefoni nasce la stessa classifica dei testa a testa — senza conto, senza identita' che viaggia, senza una sola richiesta di rete. **RESTA dell'onda D, ed e' sempre lo stesso ultimo pezzo**: il **lavoratore lato server** — il ciclo che pesca le righe con `verificata=0`, apre un browser headless della misura che il nastro dichiara, chiama `giudica` e scrive `verificata=-1` con `muovi_punti(-delta)` sui soli NON TORNA; e da li' il **punteggio di sospetto continuo**. **Onda E** (live 1v1) non aperta. **Seguiti aperti, nessuno bloccante**: **i tocchi indipendenti dallo schermo** (dal #133, il piu' grosso), **la sfida di carta a 7 e a 11** (#135), **la sfida di carta non ha un giudice** (#135, rinuncia dichiarata), **SAVE.diff riscritta a 1 da ogni sfida** (PRE-ESISTENTE, misurato leggendo il codice e non ancora su banco), e i due nuovi del #136: **la classifica degli amici sparisce se si riapre una versione vecchia del gioco** (il salvataggio si rilegge a whitelist e si riscrive senza la chiave: e' il comportamento di sempre, ma qui dentro c'e' un mese di sfide) e **il pannello del CAMBIO TELEFONO ha lo stesso difetto di centraggio**, oggi latente perche' e' alto 349 px e la piega piu' bassa che si misura e' 360 — sotto i 349 la sua cima diventerebbe irraggiungibile come lo era quella della sfida di carta. Piu' INV-06/07, **#118** (MIND v2), **#123**, la rosa canonica nelle sfide, il tetto empirico della fase `power`, la tastiera nel nastro (#131), il tetto delle 40.000 righe, il nome di un uomo di rosa non validato, la rosa che il server rilegge viva (#132), piu' i fuori-mandato **#89, #97, #99, #101-#106, #109, #115** |
| 10 | **#137 CANTIERE CHIUSO — L'ABBINAMENTO PER PUNTI E IL SOSPETTO (ULTIMO CANTIERE DELL'ONDA D)** (quattro compiti dal merge-base `8a9b33d`; spec `docs/superpowers/specs/2026-09-22-abbinamento-punti-design.md`, piano `docs/superpowers/plans/2026-09-22-abbinamento-punti.md`). Cantiere di **SERVER**: `git diff main -- rete/` **+644/-22 su 5 file**, `git diff main -- CALCETTO-il-gioco.html` **VUOTO** — e non per pigrizia: il sospetto non si vede per disegno, e un abbinamento piu' giusto si sente giocando, non si legge in un pixel. `MOTORE_V` resta **2** per costruzione. **IL PROBLEMA**: due colonne esistevano e non le usava nessuno, i **punti** nella tupla di `trova_avversario` e il **sospetto** in `allenatore`. **(a) LA SECONDA COORDINATA, non una finestra nuova**: la finestra che si allarga a gradini (`for (const banda of [8, 20, 99])`) c'era da mesi, e il gradino smette di essere un numero e diventa una coppia. Perche' serviva: forza e punti misurano due cose diverse — quanto hai *giocato* contro quanto *vinci* — e dentro `forza 75 +/-8` ci stanno 203 allenatori su 400 con punti da 402 a 1486. **Misurato su 5000 ricerche, prima e dopo nella STESSA corsa**: su 400 allenatori lo scarto mediano di punti **da 188 a 60**, abbinamenti entro 150 punti **dal 41% al 99%**, oltre 500 **dal 7% allo 0%**, e **zero** ricerche in piu' senza avversario su tutte e tre le popolazioni (l'ultimo gradino della scala **E'** l'ultimo gradino di prima). Prezzo: chiamate al database per ricerca 1,00 -> 1,01 (base 400), 1,11 (60), 2,17 (12), contro un freno `avv:` da 60 al minuto e un uso vero di UNA ricerca per pressione del dito. **IL PREZZO CHE IL PROGETTO NON AVEVA PREVISTO, trovato dal banco, in due tempi**: (1) *il banco misurava se' stesso* — la prova sulla varieta' tirava 200 generatori con **semi consecutivi** e ne usava la prima uscita, e 200 semi consecutivi di quello xorshift danno **16** valori distinti su mille (diceva «4 avversari» dove ce n'erano 99); (2) riparata la misura, il difetto vero — una finestra piu' stretta da' abbinamenti piu' giusti **e meno gente dentro**, e il peggio servito restava con **UN** avversario possibile, cioe' proprio cio' che l'`order by random()` esiste per impedire, arrivato pero' dalla *finestra* invece che dall'ordinamento. La scala ha preso un terzo numero, il **pavimento del mazzo** (`minimo` 6/4/2/1, e l'ultimo gradino ne chiede uno cosi' nessuna sfida si perde): peggio servito su base 400 **1 -> 7**, su base 12 **1 -> 5**, cioe' **meglio dei 3 di oggi**. **(b) IL SOSPETTO NASCE DA UN VERDETTO SOLO, NON TORNA.** INCOMPLETO, ALTRO MOTORE e NON FINISCE sono «non lo so» e non muovono niente; **il ripiego della tavola e' l'innocenza** (22 ingressi storti misurati: vuoto, nullo, minuscolo, verdetto inventato — nessuno accusa). **L'INVARIANTE che rende scrivibile un'accusa**: `allenatore.sospetto` di X **E'** il numero di righe `sfida` con `attaccante = X` e `verificata = -1` — un conteggio di righe, non un'opinione, e ogni riga porta seme, taglia, gol e replay, quindi chi e' segnato lo e' per partite che chiunque abbia la chiave puo' **rigiocare una per una**. Verificato su cento verdetti mescolati, venti conti su venti. Per questo **non decade**: un numero che cala smetterebbe di essere ricostruibile dalle righe. **CHE COSA COMPORTA**: niente punti tolti dal sospetto in se', niente bando, niente che si veda in nessuna risposta ne' in classifica. **Una cosa sola**: da `SOSPETTO_SEPARA = 3` in su ci si abbina solo con chi sta dalla stessa parte della soglia (misurato: un onesto incontra un sospetto il **3,93%** delle volte senza separazione, lo **0,00%** con). I due prezzi detti: un sospetto senza pari riceve un avversario costruito («allenamento, mezzi punti»), e cosi' un onesto in una base dove l'unico altro e' un sospetto. **ZERO endpoint nuovi, ZERO tabelle nuove, ZERO freni nuovi**: `segna_verdetto(s_id, verdetto)` e' una funzione del database, non un endpoint — un endpoint che accetta «questa sfida non torna» e' il modo piu' corto per far togliere i punti a un avversario. Prende la **parola** e non il numero (due porte, e nessuna accetta un -1 passato a mano), ha la guardia `and verificata = 0` contro il doppio conteggio, disfa i punti di entrambi con `delta_a`/`delta_d` (che esistevano dal primo giorno «per poterli disfare»); la `serie` non si disfa, e il perche' e' scritto accanto. **La trappola di Postgres pagata**: `create or replace` con una firma diversa non sostituisce, **affianca**, e la nuova nascerebbe senza `revoke` cioe' aperta — le firme vecchie si buttano prima. **IL BANCO**: `_q-sospetto.js` **39/39**, in batteria con `conta:true`, ed e' **il primo cancello della batteria che non apre il gioco** (misura il server, 4 s, niente Chrome). **OTTO FALSI**, ognuno bocciato dalla sua prova e da nessun'altra: `-incompleto`/`-altromotore`/`-nonfinisce` (A4 A5 B5 B10), `-doppio` (B6 B7 B10), `-spione` (D6), `-largo` (C2 C4 C4b C5: ha la colonna, il parametro, il predicato e i commenti, e non cambia niente), `-ordine` (C7), `-unico` (C7 C7b: **la cura come era scritta nel progetto**, con numeri migliori su ogni grandezza prevista). **QUEL CHE ANCORA NON C'E', detto**: la **staffetta** — il processo che pesca le righe a `verificata = 0`, apre il browser della misura giusta, chiama `giudica` e riporta la parola. La #133 ha fatto la capacita', la #134 il tubo fino all'occhio, la #137 l'altro capo; manca il pezzo in mezzo. |
| 11 | **LO STATO VERO, oggi** (sostituisce la riga 9 qui sopra, che va letta come **RIGA SUPERATA**). **Onda C CHIUSA** (3/3). **ONDA D CHIUSA**: il metro (**#130**), il duello nel nastro (**#131**), i cinque canali (**#132**), il giudice (**#133**), il sigillo (**#134**), la sfida di carta (**#135**), la classifica degli amici (**#136**) e **l'abbinamento per punti e il sospetto (#137)**. Il giudizio della sfida di rete adesso ha tutti e due i capi — la capacita' che produce i cinque verdetti e la conseguenza che ne deriva — e la competizione ha un abbinamento che guarda i punti oltre alla forza. **RESTA, ed e' il pezzo in mezzo**: la **staffetta** del verificatore differito, cioe' il ciclo che pesca `verificata = 0`, apre un browser headless della misura che il nastro dichiara, chiama `giudica` e passa la parola che esce a `segna_verdetto`. Non e' piu' «il lavoratore lato server» per intero: di quel lavoro esistono gia' la capacita' (#133), il tubo (#134) e la conseguenza (#137). **Onda E** (live 1v1) non aperta. **Seguiti aperti, nessuno bloccante**: **i tocchi indipendenti dallo schermo** (dal #133, il piu' grosso), **la sfida di carta a 7 e a 11** e **la sfida di carta non ha un giudice** (#135), **SAVE.diff riscritta a 1 da ogni sfida** (PRE-ESISTENTE), i due del #136 (**la classifica degli amici sparisce riaprendo una versione vecchia del gioco** e **il pannello del CAMBIO TELEFONO ha lo stesso difetto di centraggio**, oggi latente), e i tre nuovi del #137: **il pavimento del mazzo non si puo' ricontrollare fuori dal database** (e' una proprieta' del mazzo, non del candidato, come la separazione dei sospetti), **l'SQL di `rete/` non si esegue in nessun banco** (non c'e' un Postgres nel repo: il gruppo D di `_q-sospetto` legge il testo e **dichiara di attestare invece di misurare**), e **le righe a `verificata = 0` non scadono mai** (un INCOMPLETO resta in lista per sempre — va bene finche' qualcuno non le conta). Piu' INV-06/07, **#118** (MIND v2), **#123**, la rosa canonica nelle sfide, il tetto empirico della fase `power`, la tastiera nel nastro (#131), il tetto delle 40.000 righe, il nome di un uomo di rosa non validato, la rosa che il server rilegge viva (#132), piu' i fuori-mandato **#89, #97, #99, #101-#106, #109, #115** |
| 12 | **#138 CANTIERE CHIUSO — LA STAFFETTA (IL PEZZO MANCANTE, E CHIUDE DAVVERO L'ONDA D)** (quattro compiti dal merge-base `8127f6c`; spec `docs/superpowers/specs/2026-09-22-la-staffetta-design.md`, piano `docs/superpowers/plans/2026-09-22-la-staffetta.md`). **Rettifica alla riga 10**: il #137 si era dichiarato «ULTIMO CANTIERE DELL'ONDA D» — lo era dei cantieri *progettati*, non dei pezzi *mancanti*, e lo diceva lui stesso in fondo («quel che ancora non c'è: la staffetta»). Cantiere di **STRUMENTI**: `git diff main -- CALCETTO-il-gioco.html` **VUOTO**, `MOTORE_V` resta **2**, e di `rete/` cambiano **solo commenti** (due rettifiche a edizioni, perché due file dicevano in chiaro che questa cosa non esisteva). **IL PROBLEMA**: l'onda D aveva tre capi su quattro — la capacità (#133), il tubo (#134), l'altro capo nel database (#137) — e in mezzo niente. Lo dicevano tre file del repo con queste parole: «manca quello, e non manca altro». Finché non esisteva, «la classifica si ripulisce da sola» era una **promessa architetturale**, cioè proprio ciò che il mandato §10.5 chiede di ribaltare. **(a) DOVE VIVE**: `strumenti/staffetta.js`, un processo che si lancia. **Non è un endpoint e non lo diventerà**: una funzione Vercel non ha un browser e il giudice **È** il gioco; e un endpoint che accetta «questa sfida non torna» è il modo più corto per far togliere i punti a un avversario scrivendo il suo identificativo. Endpoint ancora **cinque**, RLS acceso su tutte e sei le tabelle con zero policy, **zero** tabelle/colonne/grant nuovi; le credenziali sono le due di `rete/lib/comuni.js`, **nell'ambiente** e mai nel repo. **(b) IL GIRO IN SEI PASSI**: pesca `verificata = 0` a pagine con un cursore (l'indice parziale c'era dal primo giorno) → allarga il replay → **raggruppa per la misura della riga di tipo 10** → apre **UN contesto per misura, non uno per riga** → chiama `giudica` → manda **la parola** a `segna_verdetto`. **(c) LA MISURA SI LEGGE IN NODE E NON PUÒ ACCUSARE NESSUNO**: va letta prima di aprire il browser perché è lei a decidere quale browser aprire, e se sbagliasse `giudica` la ricontrolla da sé e risponde INCOMPLETO/schermo-diverso — **un «non lo so», mai un NON TORNA**. Può far perdere tempo; non può far togliere punti. **(d) LA STAFFETTA NON TRADUCE**: manda la parola così com'è, tutti e cinque i verdetti sempre, compresi i tre «non lo so» — costano una chiamata che non muove niente e comprano **un cammino solo**. Una staffetta che scegliesse avrebbe dentro un `if` sulla tavola dei cinque, cioè una **terza porta** scritta peggio delle due che ci sono. **(e) DUE STRATI DI IDEMPOTENZA, E NON SONO LA STESSA COSA**: la **struttura** (`and verificata = 0`) protegge dalle **accuse doppie**, il **taccuino** (un file locale) solo dal **lavoro sprecato**, perché i tre «non lo so» restano a 0 per disegno e tornerebbero a ogni giro. **Il taccuino può sparire senza che nessuno venga accusato due volte.** Eccezione misurata: un INCOMPLETO/schermo-diverso su una riga per cui la staffetta aveva chiesto **proprio quella misura** non è un «non lo so» del nastro, è **la finestra negata** dalla macchina che ospita — si grida nel referto e la riga torna al giro dopo. **(f) NESSUN FRENO DEL SERVER TOCCA QUESTO PROCESSO** (i sei `frenato(...)` stanno negli endpoint, e la staffetta non passa da nessun endpoint): quindi **si frena da sé**, con lo stesso meccanismo e nello stesso posto, perché due staffette lanciate insieme non condividono memoria. **LE MISURE**: contesto **1077 ms**, giudizio **773 ms** medi, giro di sei righe **7,9 s**, **34 righe/minuto** contro un tetto di 60 che si impone da sé, **2 chiamate al database per riga** più una per giro. **IL GIRO COMPLETO**: sei sfide, quattro verdetti in una corsa sola (il quinto, NON FINISCE, in un giro a parte con la rigiocata stretta a 300 fotogrammi) — **due righe chiuse a 1, due a −1, DUE RESTANO APERTE a 0**; il sospetto sale **solo** sull'attaccante dei due NON TORNA e **di uno per riga** (B=2, A=0, C=0); i suoi punti tornano indietro per intero (**1038 → 1000**); l'invariante del #137 regge dopo il giro. **LA MISURA GIUSTA, e non si prova con la fixture congelata**: il banco **GIOCA una sfida vera a 1024x460** dentro la corsa e quella riga deve tornare TORNA — quattro righe su sei sono a 915x412, e senza quella riga «apre la misura giusta» sarebbe un racconto verde anche su una staffetta cieca. Lo **stesso** nastro aperto di forza a 915x412 dice INCOMPLETO/schermo-diverso e dichiara quale schermo serve: **mai NON TORNA**. Tre contesti per sei righe. **LA RIPARTENZA IN QUATTRO MODI**: la risposta persa (nessuna riga giudicata due volte, stato finale identico al giro pulito), la chiamata mai partita (**quella riga e solo quella** si rigiudica), il taccuino cancellato (cambia il lavoro, non l'esito), e **due staffette che pescarono insieme** — l'unico caso in cui la guardia della struttura viene davvero esercitata, ed è la ragione per cui esiste. **(g) IL FILO, aggiunto al compito 3 perché era un buco**: la spec dichiarava «PostgREST non si interroga», e da quel buco ci passava un difetto che non si sarebbe visto da nessun'altra parte — l'argomento di `segna_verdetto` chiamato `id` invece di `s_id`: la staffetta pescherebbe bene, aprirebbe la finestra giusta, giudicherebbe bene, riferirebbe verdetti corretti, e **nessuna riga si chiuderebbe mai**. Adesso il gruppo **G** misura il filo contro un `http` che parla la **forma** di PostgREST, e chiude con **il programma vero** — `node strumenti/staffetta.js` con le due variabili d'ambiente — che apre il suo server del gioco e il suo browser, chiude le righe, scrive il taccuino e non stampa la chiave. **IL BANCO**: `_q-staffetta.js` **42/42** in sette gruppi, nato a **29 rossi su 32**, in batteria con `conta:true` (70 s da solo, 393 s sotto la batteria in parallelo: `lento:true`). **DIECI FALSI**, ognuno con la lista **misurata** di ciò che morde. **DUE HANNO RIPARATO IL BANCO PRIMA DI ESSERNE BOCCIATI**, ed è la ragione per cui i falsi si costruiscono invece di raccontarli: `cieca` **passava** C2 (la prova leggeva la **chiave del gruppo** — quel che il nastro dichiara — invece della **finestra aperta davvero**) e `filo` **passava** G7 (la prova lanciava `strumenti/staffetta.js` per percorso fisso, quindi provava sempre quella onesta qualunque cosa le si puntasse contro). Due righe che attestavano invece di misurare, in un banco scritto per non farlo, e a trovarle non è stato chi le ha scritte. **E QUATTRO FALSI SONO NATI DALLA DOMANDA OPPOSTA** — *quale asserzione, qui dentro, non ha un giudice?*, perché **un'asserzione senza falso è un attestato**: `sprecona` per C4 (l'unica che nessun altro condannava), `filo` per il gruppo G, `rassegnata` per C6/C6b (la **finestra negata**, una regola scritta in tre documenti e misurata in nessuno), `avvelenata` per E3b. **E L'ULTIMA HA TROVATO UN DIFETTO VERO, non un buco del banco**: la staffetta scriveva nel taccuino anche durante il giro a vuoto (`--asciutto`). Quel giro giudica e non manda niente, quindi le righe restano a `verificata = 0`: messe nel taccuino, il giro vero del giorno dopo le avrebbe **saltate**, e non le avrebbe guardate mai più nessuno — referto pieno di verdetti giusti, database intatto, nessuno accusato. **Non sbagliava niente: dimenticava.** Curato, con la sua prova e il suo falso. **E L'ALTRA TRAPPOLA, pagata al compito 2**: le scene del banco ricominciano gli id da 1 e il taccuino è indicizzato per id, quindi con **un file solo** la riga 1 di una scena faceva saltare la riga 1 della scena dopo — cinque prove su trentadue avevano un metro di paragone tarato sul niente («giro pulito: zero verificate»). **I LIMITI DICHIARATI** (la stessa dichiarazione del #137): l'**SQL non si esegue** (il lato-database del banco è `applica()` di `rete/lib/verdetto.js`, e la corrispondenza con `segna_verdetto` la guarda **per testo** `_q-sospetto` D5), **PostgREST si interroga ma è finto** (il gruppo G misura il filo, non il Postgres vero: se la funzione cambiasse firma nello schema qui non si vedrebbe), il **ritmo è di questa macchina** e non di un CI, **si gira a taglia 5** (dove il determinismo è pieno, voce #98 — che il *giudice* torni anche a 7 e 11 lo misura `giudice`, 14 su 14, voce #133), e **quattordici asserzioni su quarantadue non hanno un falso che le condanni**, misurate e non stimate (A1 A2 A3 A5, D2 D5, E3 E4, F1-F4, G2 G5: sono guardie di forma e di porte, restano verdi anche senza la staffetta e non devono sembrare di provare il giro). **LA CHIAVE NON È NEL REPO**, e non è un'assicurazione ma un controllo: **zero su 1898 file tracciati** — **rettifica a edizioni, 22 settembre 2026, voce #139**: **1898** è il conto di quando fu preso, prima che i file del compito 3 entrassero nell'indice; al commit `1d5b946` il cancello ne conta **1911**, e il comando di casa che lo riproduce è `git ls-files | wc -l` (e col #139 chiuso sono **1 921**, perché quel conto cresce a ogni cantiere). **Il verdetto non cambia, zero è zero**, ma un numero che non si riproduce col comando di casa invecchia da solo: va letto come «i file tracciati di quel giorno», non come una costante — con la soglia dei trenta caratteri messa apposta perché i banchi del repo assegnano credenziali finte e corte e un cancello che le chiamasse chiavi urlerebbe al lupo finché nessuno lo guarda più. |
| 13 | **LO STATO VERO, oggi** (sostituisce la riga 11 qui sopra, che va letta come **RIGA SUPERATA** per la parte «resta il pezzo in mezzo»). **Onda C CHIUSA** (3/3). **ONDA D CHIUSA DAVVERO**: il metro (**#130**), il duello nel nastro (**#131**), i cinque canali (**#132**), il giudice (**#133**), il sigillo (**#134**), la sfida di carta (**#135**), la classifica degli amici (**#136**), l'abbinamento per punti e il sospetto (**#137**) e **la staffetta (#138)**. Il verificatore differito ha adesso **tutti e quattro i pezzi**: la capacità che produce i cinque verdetti, il tubo che li porta all'occhio di chi gioca, la conseguenza nel database, e **il processo che li mette in fila**. «La classifica si ripulisce da sola» ha smesso di essere una promessa: c'è il codice che la esegue, e si lancia con `node strumenti/staffetta.js`. **Onda E** (live 1v1) non aperta. **Seguiti aperti, nessuno bloccante**: **i tocchi indipendenti dallo schermo** (dal #133, il più grosso — e adesso costa anche un contesto di browser per misura, 1077 ms, a ogni giro della staffetta), **la sfida di carta a 7 e a 11** e **la sfida di carta non ha un giudice** (#135), **SAVE.diff riscritta a 1 da ogni sfida** (PRE-ESISTENTE), i due del #136, i tre del #137 (**il pavimento del mazzo non si può ricontrollare fuori dal database**, **l'SQL di `rete/` non si esegue in nessun banco**, **le righe a `verificata = 0` non scadono mai** — e questo terzo il #138 lo **addolcisce senza chiuderlo**: il taccuino della staffetta impedisce di rimacinarle, ma nel database restano aperte in eterno), più i tre nuovi del #138: **la staffetta non gira da nessuna parte da sola** (non c'è un CI che la lanci: è un processo che qualcuno deve far partire), **PostgREST non si interroga in nessun banco** (di `bancoVero` si misura la forma), e **nove asserzioni di `_q-staffetta` non hanno un falso che le condanni** — **RETTIFICA A EDIZIONI (22 settembre 2026, voce #139)**: quelle due affermazioni sono **sbagliate come scritte**, e lo stesso commit che le ha scritte le scrive giuste altrove. **(a)** le asserzioni senza falso sono **QUATTORDICI** su quarantadue, non nove: il «nove» è il conto del compito 2 trascinato nella riga finale, e **fa sembrare la rete più fitta di com'è**. L'elenco misurato sta in `MANUALE.md` §A e nella testa di `strumenti/_q-staffetta.js`: **A1 A2 A3 A5 · D2 D5 · E3 E4 · F1 F2 F3 F4 · G2 G5**. **(b)** PostgREST **si interroga**, ed è il gruppo G aggiunto dal compito 3 dello stesso cantiere: quel che è finto è il **server che risponde**, non l'interrogazione — la formula giusta è quella del `MANUALE`, «**PostgREST si interroga ma è finto**», e la differenza conta, perché il buco che resta è la firma della funzione nello schema vero, non il filo. Più INV-06/07, **#118** (MIND v2), **#123**, la rosa canonica nelle sfide, il tetto empirico della fase `power`, la tastiera nel nastro (#131), il tetto delle 40.000 righe, il nome di un uomo di rosa non validato, la rosa che il server rilegge viva (#132), più i fuori-mandato **#89, #97, #99, #101-#106, #109, #115** |
| 14 | **#139 CANTIERE CHIUSO — LA FINESTRA CHE CAMBIA (un CRITICO dell'onda D: un ONESTO poteva essere accusato)** (quattro compiti dal merge-base `1d5b946`; spec `docs/superpowers/specs/2026-09-22-finestra-che-cambia-design.md`, piano `docs/superpowers/plans/2026-09-22-finestra-che-cambia.md`). Trovato dalla **revisione d'insieme**, e non dentro a un cantiere solo: sta nella **giuntura** fra il #133 (che ha scoperto il canale dei pixel) e il #137/#138 (che hanno dato al verdetto la forza di togliere punti). **IL DIFETTO**: il #133 aveva messo nel nastro la misura della finestra (riga di tipo 10), **ma una volta sola**, in `Sfida.gioca` prima di `startMatch`, mentre il gestore di `resize` resta vivo per tutta la partita e ricuoce `SCALE`/`OX`/`OY`, e `checkOrientation` ferma **solo il portrait**. La guardia verificava che il nastro e il giudice **dicessero la stessa cosa**, non che quella cosa **fosse stata vera dall'inizio alla fine**. **MISURATO**, due bracci che cambiano una cosa sola (stesso seme 20260801, stesso copione di dita, stesse rose): a finestra FERMA il tabellone dice 3-4 e il giudice dice **TORNA** (3-4 in 8819 passi); con la finestra che perde 60 px a metà partita restando orizzontale il tabellone dice 1-2, la riga 10 dice ancora `915x412` — la misura di **partenza** — e il giudice dice **NON TORNA** (3-4 in 8631 passi). È un **onesto**: gli è comparsa la barra dell'URL. `NON TORNA` è l'unico verdetto che muove punti, ne toglie a **due persone**, alza un `sospetto` che **non decade mai** e **chiude la riga per sempre** (`and verificata = 0`). Nell'APK è mitigato (fullscreen + `configChanges`) ma non chiuso; servito come **PWA in un browser di telefono la barra dell'URL è il caso normale**, e sta proprio nella banda dei 56-90 px. **LA CURA — astensione, non accusa**: `Reg.schermo(w,h)` scrive la misura **a ogni cambio** (e `resize()` la chiama in fondo, quando SCALE/OX/OY e i pulsanti sono già quelli nuovi), e `vagliaNastro` si rifiuta con **`INCOMPLETO / schermo-cambiato`** su ogni nastro che porti **più di una misura distinta** — *distinte* e non righe: una finestra che va e torna lascia tre righe e due misure. **Prima di `schermo-diverso`**, perché uno schermo diverso si ripara aprendo la finestra giusta e uno **cambiato non si ripara in nessuna finestra**; e al posto di `schermo` il giudice restituisce `schermi`, così la staffetta non va a cercare un telefono che non esiste e la regola della «finestra negata» del #138 non scatta. **IL RESIZE A RAFFICA NON RIEMPIE IL NASTRO**: 24 eventi su 6 misure → **6 righe**, grazie a due guardie in fila (quella di `resize()` e quella di `Reg.schermo`, che serve perché `RESIZE_FORZA` fa girare `resize()` anche a finestra immutata). **IL COSTO, DICHIARATO**: con **12 px** di cambio la partita non si muove di un passo (stesso 3-4, stessi 8819 passi, nastro identico) e oggi dice `TORNA`, domani dirà «non lo so». Si paga volentieri — un onesto non confermato non perde niente, un onesto accusato perde i punti, il sospetto e la riga per sempre — e **una soglia in pixel sarebbe un'opinione** su un motore caotico. La strada che renderebbe quel nastro *giudicabile* (rigiocare il cambio) chiede di spezzare l'invariante `VW === innerWidth`: è il seguito grosso già aperto dal #133, **«i tocchi indipendenti dallo schermo»**, e non è una toppa. **IL GIOCO DI CHI GIOCA NON CAMBIA DI UN PASSO** (i tre bracci finiscono con gli stessi passi e lo stesso tabellone di prima: 8819 / 7643 / 8693); l'alternativa brutale — trattare il resize come il portrait — è stata **scartata**: punirebbe chi gioca per una cosa che fa il suo telefono. **`MOTORE_V` RESTA 2, misurato e non opinato**: quattro sfide vere registrate sul gioco di `main` più la fixture congelata, ognuna giudicata dal gioco vecchio **e** dal nuovo, **5 su 5 identici** in verdetto, causa, gol e passi. **I NASTRI VECCHI NON SI ROMPONO**: ne portano *una* di righe di tipo 10, quindi una misura, e si giudicano esattamente come oggi; la prima riga resta nel punto esatto di prima. **IL BANCO**: `_q-finestra.js` **20/20**, nato a **8/20**, a bracci — FERMA, CAMBIA, **TORNA** (la finestra che va e ritorna) e RAFFICA — in batteria con `conta:true` (`lento:true`). **Il terzo braccio esiste per un falso solo**: senza di lui `_crit-finestra-estremi` passava con **diciotto verdi su venti**. E **D4** è la prova che nessuno pensa a scrivere: lo stesso nastro con **tre attesi diversi** deve dare tre volte la stessa risposta, perché l'astensione è una proprietà del **nastro** e non del conto — prima della cura, col punteggio che la rigiocata produce, il giudice diceva `TORNA` su un nastro inverificabile. **CINQUE FALSI** con la lista misurata di ciò che mordono: `sorda` 10, `cieca` 7, `accusa` 6, `estremi` 3 (e **solo lui morde B5**), `raffica` **1 su 20**. Reti di sicurezza tutte identiche al numero; batteria intera a gruppi, tutti i cancelli che contano verdi. **Più tre rettifiche documentali** dello stesso genere — affermazioni superate lasciate dove qualcuno le legge per riprendere: il «nove» della riga 13 (sono **quattordici**), «PostgREST non si interroga» (si interroga: **finto è il server che risponde**), e il conto dei file tracciati (**1 898 → 1 911**, col comando che lo riproduce). |
| 15 | **LO STATO VERO, oggi** (sostituisce la riga 13 qui sopra, che va letta come **RIGA SUPERATA** per il conto delle asserzioni senza falso, per PostgREST e per il conto dei file tracciati). **Onda C CHIUSA** (3/3). **ONDA D CHIUSA**, e adesso anche **rivista d'insieme**: il metro (**#130**), il duello nel nastro (**#131**), i cinque canali (**#132**), il giudice (**#133**), il sigillo (**#134**), la sfida di carta (**#135**), la classifica degli amici (**#136**), l'abbinamento e il sospetto (**#137**), la staffetta (**#138**) e **la finestra che cambia (#139)** — il primo cantiere nato da una revisione *fra* cantieri invece che dentro a uno, e il primo che cura un modo di **accusare un innocente** invece di aggiungere una capacità. Il verificatore differito ha tutti e quattro i pezzi, e adesso l'unico verdetto che muove punti non può più uscire per colpa di una barra del browser. **Onda E** (live 1v1) non aperta. **Seguiti aperti, nessuno bloccante**: **i tocchi indipendenti dallo schermo** (dal #133, il più grosso — e il #139 lo rende **più pesante, non meno**: da adesso ogni nastro con la finestra mossa resta `INCOMPLETO` finché quel lavoro non è fatto, e quelli sono i nastri di chi gioca in un browser di telefono), **la sfida di carta a 7 e a 11** e **la sfida di carta non ha un giudice** (#135), **SAVE.diff riscritta a 1 da ogni sfida** (PRE-ESISTENTE), i due del #136, i tre del #137 (**il pavimento del mazzo non si può ricontrollare fuori dal database**, **l'SQL di `rete/` non si esegue in nessun banco**, **le righe a `verificata = 0` non scadono mai** — e il #139 ne aggiunge **per disegno**: un nastro con la finestra mossa resta aperto per sempre, ed è il prezzo dichiarato dell'astensione), i tre del #138 (**la staffetta non gira da nessuna parte da sola**, **PostgREST si interroga ma è finto**, e **quattordici** asserzioni di `_q-staffetta` senza un falso che le condanni), più il nuovo del #139: **il resize durante un REPLAY non si misura** (in rilettura il registro non scrive, quindi non c'è niente da dire, ma nessun banco lo dichiara con un numero). Più INV-06/07, **#118** (MIND v2), **#123**, la rosa canonica nelle sfide, il tetto empirico della fase `power`, la tastiera nel nastro (#131), il tetto delle 40.000 righe, il nome di un uomo di rosa non validato, la rosa che il server rilegge viva (#132), più i fuori-mandato **#89, #97, #99, #101-#106, #109, #115** |

## La giornata del 21 settembre, in breve

| | cosa |
|---|---|
| 2 | **#131 CANTIERE CHIUSO — IL DUELLO ENTRA NEL NASTRO (P0 dell'onda D)** (sette compiti dal merge-base `7fbe9bf`; dossier `docs/superpowers/specs/2026-09-21-duello-nastro-dossier.md`, spec `…-duello-nastro-design.md`, piano `docs/superpowers/plans/2026-09-21-duello-nastro.md`). Cantiere di MOTORE, tutto per ancore (`strumenti/_toppa-duello-*.js`). **IL DIFETTO**: i pointer del duello erano appesi a `#duel`/`#powerWrap` e scavalcavano le quattro porte del registratore; restavano il marchio di tipo 5 e `fermaReplayAlDischetto`. Misurato: fra il **43% e il 100%** delle sfide entra in almeno un duello, e in modalità un giocatore **ogni duello ha un umano dentro (169/169)** — il giudice differito non avrebbe potuto verificare proprio le partite decisive. **L'OROLOGIO è il contatore di `Duel.update`, non i millisecondi**, e ora è un cancello: giocando due secondi `Reg.tick` arriva a 120, poi attraversa 18 rigori e 3.919 aggiornamenti restando **un solo valore**. **FORMATO** tipo 6 `[tick, 6, ms, nDuello, passo, verbo, a, b, c]`, ancorato a `(nDuello, passo)`; i millisecondi dichiarati non letti. **IL CUORE è una disuguaglianza stretta** (`riga.passo < Duel.passo` dopo l'incremento): dal vivo il dito cade FRA due aggiornamenti, in rilettura il comando va rimesso all'INIZIO del successivo. **LE DUE TRAPPOLE**: `dentroUpdate` (il ripiego del portiere a `cpuT=3,0` gira anche col portiere UMANO — il banco costruisce apposta quel caso e verifica che nel nastro non ci sia un solo verbo 2) e la quantizzazione di `u,v` a un millesimo alla sorgente (lo stesso pixel dà mire diverse su due viste in **117 dita su 117**, scarto fino a 0,7810; prima della cura **234 mire su 234** non sopravvivevano al giro nel nastro). **IL RIPIEGO conservato e RIARMATO** sul caso vero (nastro troncato: `Reg.righeDuello(3)=false`, 2 duelli risolti su 3, il terzo non inventato). **LA RETE**: `_q-duello-impronta.js`, 44 duelli firmati a seme fisso, congelata PRIMA di toccare qualunque cosa e rimisurata a ogni compito — **non si è mossa di un numero, sette volte**. **RETTIFICA AL DOSSIER, misurata**: il mutante previsto (gancio spostato di un fotogramma) **NON è letale** — `pickZone` azzera il cursore, quindi conta solo l'INTERVALLO fra i due comandi e uno spostamento uniforme lo conserva; il falso che il dossier aveva davvero misurato sposta **un verbo solo** (`_crit-duello-scarto.js`) ed è **bocciato 3 semi su 3** (cursore 0,2875 → 0,30667). **UN ROSSO PAGATO**: la guardia «in rilettura le dita vere sono ignorate», messa al compito 4, rendeva rosso `_q-fuzzer.js` (riapplica il suo log-duelli a mano); misurato verde sul commit precedente, quindi mia — spostata al compito 5, dove **è tornato verde da solo**, conferma indipendente della rilettura. **MOTORE_V RESTA 2, MISURATO** (`_t-duello-motorev.js`, due versioni): **30 nastri senza duello su 30** registrati sul gioco di prima e rigiocati sul curato danno la stessa partita (impronta, punteggio, sorteggi); zero scartati, zero nulli; la misura è scritta accanto al numero nel sorgente, e il controllo `incompleto` è CONSERVATO (rettificato a edizioni: serve ai nastri vecchi). **CANCELLI**: `_t-duello-nastro` 1/4 → **5/5**, `_t-duello-contatore` **9/9**, `_t-duello-tacca` 3/4 → **4/4**, `_t-duello-porte` 5/8 → **8/8**, `_t-duello-rigioca` 2/4 → **7/7**, `_t-duello-motorev` **30/30**. Batteria intera a ogni compito, a gruppi: tutti i cancelli che contano VERDI. **Fuori perimetro, dichiarato**: la tastiera (il tipo 4 registrato e morto fuori dal duello) e il `pointermove` del mirino (solo disegno). **CORREZIONI DI REVISIONE APPLICATE** (21 settembre 2026, commit a parte, gioco NON toccato): la prova D di `_t-duello-contatore.js` era VUOTA (leggeva `Duel.nDuello` gia' a zero su pagina fresca) — curata sporcando il contatore prima di misurare, verificata rossa su un mutante dedicato e verde sul gioco vero; `_q-fuzzer.js`/`tutti.js` rettificati a edizioni (Reg ORA cattura il duello); `_t-duello-nastro.js`/`_t-duello-rigioca.js` dichiarano di non coprire la quantizzazione della mira; `_t-duello-impronta.js` promosso a `_q-duello-impronta.js` e registrato in batteria (44/44, impronta non mossa). Verbale completo in `MANUALE.md` §A registro, voce #131, IN CIMA |
| 3 | **LO STATO VERO, oggi** (sostituisce la riga 11 della giornata del 20 settembre, seconda parte, che va letta come **RIGA SUPERATA** per la parte «resta del mandato»). **Onda C CHIUSA** (3/3). **Onda D avviata**: il metro (**#130**) e il P0 (**#131**, il duello nel nastro) sono chiusi. **RESTA dell'onda D**: costruire il **GIUDICE vero** — il verificatore differito che rigioca il nastro di una sfida e ne conferma il punteggio. Adesso ha il metro giusto (tetto per taglia, `determinismo`/`rete`/`sfida` in batteria) e un nastro che contiene anche le partite decise dal dischetto. **Onda E** (live 1v1) non aperta. **Seguiti aperti, nessuno bloccante**: **INV-06/07**, **#118** (MIND v2), **#123**, la rosa canonica nelle sfide, il tetto empirico della fase `power`, la **tastiera nel nastro** (nuovo, dal #131: il tipo 4 è registrato ma `Reg.esegui` non ridispaccia il gestore — dentro il duello è coperto dalle tre porte avvolte, fuori no), più i fuori-mandato **#89, #97, #99, #101-#106, #109, #115** |
| 4 | **#132 CANTIERE CHIUSO — NESSUN INNOCENTE ACCUSATO** (sei compiti dal merge-base `3deb807`; spec `docs/superpowers/specs/2026-09-21-nessun-innocente-design.md`, piano `docs/superpowers/plans/2026-09-21-nessun-innocente.md`). Cantiere di MOTORE, tutto per ancore (cinque `strumenti/_toppa-*.js`, 25 ancoraggi). Il #131 ha dato al giudice qualcosa da misurare; questo gli toglie i **falsi positivi**: le strade per cui una partita ONESTA rigiocata finisce diversa. **I CANALI ERANO QUATTRO NEL PIANO, SONO CINQUE**, e il quinto è stato trovato MISURANDO (il cancello del compito 1 restava rosso su una sfida su due anche a cura applicata, e non per la mentalità). **(a) MENTALITÀ**: il bottone della pausa girava `G.ment[0]` senza guardia su `G.sfida` né su `Reg.modo` e senza scrivere nel registro — chi attacca cambia postura e il nastro non lo sa, chi GUARDA può cambiare la postura della squadra di chi l'ha attaccato. Cura: riga di **tipo 8** più `posaMentalita()` (una funzione per i due capi) più la guardia «in rilettura le dita vere sono ignorate». REGISTRATA e non impedita, dichiarato: sponde e mira guidata sono il MOTORE, la mentalità è una MOSSA. Misurato: punteggio rigiocato **0/2 identico (2-1 contro 3-2, 2-3 contro 0-1) → 2/2**. **(b) CARATTERE DAL NOME** (`G.car = [CAR_NEUTRO, caratterePer(G.oppName)]`): basta che il difensore si RINOMINI fra la partita e il replay perché gli stessi comandi muovano un'altra CPU. Cura: **indice fra -1 e 9 in CODA al tipo 7** (in coda perché la lettura è posizionale: un nastro vecchio finisce prima e ricade sul nome), e `Sfida.gioca` passa a `startMatch` lo STESSO numero che scrive. **L'ordine di `CARATTERE` è da oggi un FORMATO**: chi lo riordina alza MOTORE_V. Misurato: CPU giocata `0.75/2/1/0.9/1/1.15` contro rigiocata `1/1/1/1/1/1`, punteggio **0/2 (1-2 contro 1-6, 0-1 contro 2-3) → 2/2**. **(c) QUATTRO PORTE, QUATTRO RISPOSTE**: `loadSave` non guardava niente, `setupPlayers` copiava grezzo, `impaccaRosa` faceva `max(1,min(99,v|0))`, `startMatch` arrotondava e ripiegava su 62 — **sei casi discordi su otto**, e per `"abc"` quattro risposte diverse. In sfida si giocava con **250** e si registrava **99**. Cura: `attrRosa`, una regola sola, **ALLA SORGENTE** (la dottrina del pixel intero). Misurato: porte concordi **2/8 → 8/8**, risposte in scala **3/8 → 8/8**, punteggio **0/2 → 2/2**. **(d) IL TETTO TACEVA** (40.000 righe in silenzio) **e il nastro VUOTO passava**: il registro scrive **una riga per dito per fotogramma** (1,00 / 3,01 / 6,01 / 10,02 con uno/tre/sei/dieci dita), quindi il tetto arriva a **111 s con sei dita**, dentro una sfida col golden goal. Cura: riga di **tipo 9** e due rifiuti con la causa vera; il tetto resta 40.000 (guardia contro un ciclo, non taratura). Il cancello raggiunge il tetto **con le dita su una sfida vera**, dieci dita, in 7.295 passi — **40.002 comandi sul gioco CURATO** (il tetto più la riga di marchio), contro **40.001 sul gioco di PRIMA** (`main`, senza marchio) — **CORRETTO in revisione, 21 settembre 2026**: il verbale originale scriveva 40.001 per il curato, cifra che invece appartiene al gioco di prima; non abbassandolo. Misurato prima: troncato rigiocato **0-3 contro 1-3**; vuoto accettato, **0-4 contro 3-0**, col gioco che diceva «La squadra di chi ti ha attaccato è cambiata da allora» — l'innocente accusato, testualmente. **(e) L'AUDIO MANGIAVA I SORTEGGI**: `Audio5.init → startCrowd → noiseBuf` riempiva un secondo di campionamento con `dado()`, il generatore SEMINATO — **48.000 sorteggi** al primo `unlock()` a 48 kHz, e la frequenza la decide l'APPARECCHIO. Canale **LATENTE** (nel gioco spedito lo sblocco precede `SEME.accendi`), curato con `Math.random()`: correzione di categoria. **CINQUE FALSI COSTRUITI E TUTTI BOCCIATI** (`_crit-ment-muta`, `_crit-car-nome`, `_crit-rosa-meta`, `_crit-tronco-muto`, più l'impronta del #131): ognuno passa la prova di FORMATO e cade su quella di ESITO — è la trappola che la revisione del #131 aveva bocciato. **UN ROSSO DEL BANCO PAGATO**: `_q-rosa-scala.js` (`_t-rosa-scala.js` prima della promozione a cancello di qualità, 21 settembre 2026) iniettava il numero storto in memoria dopo l'avvio, cioè saltava la porta in esame — adesso sporca il salvataggio e RICARICA. **UN ROSSO DELLA BATTERIA DIAGNOSTICATO**: `audio` esce 3, 0 e 1 su corse diverse; misurato `--ripetuto 3` DA SOLO è 28/28 su tre corse sia sul curato sia su `main` — soffre la macchina carica, e da lì in poi gira in un gruppo suo. **MOTORE_V RESTA 2, MISURATO** (`_t-132-motorev.js`, due versioni): 30 nastri registrati sul gioco di prima (`main` `3deb807`) e rigiocati sul curato, taglia 5, 3600 passi, semi da 20260801, **30 su 30 identici** (impronta, punteggio, sorteggi); zero nulli, uno dei trenta passato dal dischetto — a differenza del banco del #131 i semi col duello NON si scartano, perché dal #131 il duello sta nel nastro. La misura è scritta accanto al numero nel sorgente. **CANCELLI**: `_t-ment-nastro` 1/6 → **6/6**, `_t-carattere-nastro` 1/4 → **4/4**, `_t-rosa-scala` 0/4 → **4/4**, `_t-nastro-tronco` 2/5 → **5/5**, impronta del duello **44/44 a ogni compito**. Batteria intera a gruppi a ogni compito, **42/42 VERDI** al compito 4. **PROMOSSI A CANCELLI DI QUALITÀ (correzione di revisione, 21 settembre 2026)**: tutti e quattro rinominati (`git mv`) in `_q-ment-nastro.js`/`_q-carattere-nastro.js`/`_q-rosa-scala.js`/`_q-nastro-tronco.js` e registrati in `strumenti/tutti.js` con `conta:true` — prima nessun cancello della batteria si sarebbe accorto di una regressione sui cinque canali, lo stesso rilievo già pagato dal #131 per il duello. **ALTRE DUE CORREZIONI DI REVISIONE**: `_t-132-motorev.js` aveva `--passi` di default a 2400 (il verbale dichiara 3600) — default corretto a 3600, `--passi` nominato nella riga `uso:`, rimisurato col comando nudo **30/30 identici, 1 dal dischetto**, stesso verdetto; e `carDif` non ricadeva nel ripiego di `Sfida.guarda` insieme a `mentAtt`/`mentDif`/`rosaAtt`/`rosaDif` — curato con `carDif = undefined;` via `strumenti/_toppa-132-cardif.js` (un'ancora), `git diff main -- CALCETTO-il-gioco.html` **319/14** (era 310/13), `_q-duello-impronta.js` **44/44** invariato. Verbale completo della correzione in `MANUALE.md` §A, voce #132, in cima. **Fuori perimetro dichiarato**: il GIUDICE vero, alzare il tetto, validare il nome di un uomo di rosa, la rosa che il server rilegge viva. Verbale completo in `MANUALE.md` §A registro, voce #132, IN CIMA |
| 5 | **LO STATO VERO, oggi** (sostituisce la riga 3 qui sopra, che va letta come **RIGA SUPERATA** per la parte «resta dell'onda D»). **Onda C CHIUSA** (3/3). **Onda D**: il metro (**#130**), il P0 (**#131**, il duello nel nastro) e i falsi positivi (**#132**, i cinque canali) sono chiusi. **RESTA dell'onda D**: costruire il **GIUDICE vero** — il verificatore differito che rigioca il nastro di una sfida e ne conferma il punteggio. Adesso ha il metro giusto, un nastro che contiene anche le partite decise dal dischetto, e cinque strade in meno per condannare un innocente. **Onda E** (live 1v1) non aperta. **Seguiti aperti, nessuno bloccante**: **INV-06/07**, **#118** (MIND v2), **#123**, la rosa canonica nelle sfide, il tetto empirico della fase `power`, la **tastiera nel nastro** (#131), più i nuovi del #132 — il **tetto delle 40.000 righe** (oggi dichiarato, non alzato), il **nome di un uomo di rosa** non validato al caricamento, e la **rosa che il server rilegge viva** invece di conservarne una copia — più i fuori-mandato **#89, #97, #99, #101-#106, #109, #115** |
| 1 | **#130 CANTIERE CHIUSO — IL METRO PRIMA DEL GIUDICE** (quattro compiti dal merge-base `d08a1e5`; spec `docs/superpowers/specs/2026-09-21-il-metro-design.md`, piano `docs/superpowers/plans/2026-09-21-il-metro.md`). Cantiere di BANCO/documenti, `git diff main -- CALCETTO-il-gioco.html` VUOTO. Prima dell'onda D (il giudice che rigioca il nastro di una sfida) due difetti del metro: il tetto INV-15 ancorato a taglia 5 e applicato a ogni taglia, e `_q-determinismo.js` (la prova di INV-01) mai registrato in batteria. **RETTIFICA A EDIZIONI del #129** (riga 11 sotto): il seme 20260924 a taglia 11, dichiarato "bloccato in freekick, difetto di gioco", NON è bloccato — misurato di nuovo, raggiunge `'end'` al fotogramma **19.502 (325,0s) con punteggio 2-1**. Il rosso era `TETTO_FOTOGRAMMI=18000` (tarato su taglia 5, partite da 90s) applicato a taglia 11, dove la partita di regolamento dura già 180s. **CURA**: `tettoFotogrammi(taglia)` sostituisce la costante unica — **18000/300s a taglia 5** (invariato), **21000/350s a taglia 7**, **27000/450s a taglia 11**, ciascuno MISURATO (non stimato per proporzione: il rapporto durata-orologio/fotogrammi-reali non è costante fra le taglie sui campioni raccolti) con lo stesso metodo del #127 — pre-rigori per taglia (`fuori/_misura-preRigori.js`, max 12252/100 partite a 7, 17136/150 a 11) + oltranza teorica 5904 (18×328, VERIFICATA taglia-indipendente: `fuori/_misura-oltranza.js` dà 276/948 tiri a 5 e 277/2882 a 11, stesso ordine del 328/46.776 storico). Test-condanna `strumenti/_t-metro-taglia.js`: rosso prima della cura (tettoFotogrammi non esiste), verde dopo; `_q-soak.js --taglia 11` passa da falso-positivo-certo a **10/10**; l'hang vero (artefatto #108) resta colto (`--bugiardo durata` ROSSO a 5 e a 11 col tetto più largo). **BATTERIA**: `determinismo` (mai registrato, provava INV-01 senza sorveglianza) entra con DUE voci — taglia 5 e taglia 11 (10/10 a entrambe, dopo la cura #129 della #98 — una sola voce a 5 avrebbe ricreato il buco) — insieme a `rete` (22/22) e `sfida` (54/54), verificati senza rete vera (server finto in memoria, nessun conflitto con `senza-rete.js`). **Le rettifiche #98 a edizioni**: sette file in `strumenti/` dichiaravano ancora la #98 (determinismo instabile a 7/11) come stato attuale — corretti (CLAUDE.md escluso per mandato del committente, dichiarato non toccato). **Batteria intera a tre gruppi** (una corsa sola eccedeva il tempo disponibile): 45 esecuzioni-cancello, TUTTI quelli che contano VERDI; `audio`/`avvio-telefono` prova nulla (nessun contesto audio/telefono in questo banco, dichiarato, non un rosso); `istantanea` (informativo) 45/56, stesso rumore già dichiarato da cantieri precedenti. Verbale completo in `MANUALE.md` §A registro, voce #130, IN CIMA al registro |

## La giornata del 20 settembre, in breve

| | cosa |
|---|---|
| 1 | **#125 CANTIERE CHIUSO — L'ONDA C COMINCIA (invariant-checker)** (due compiti dal merge-base `a7561d0`, primo anello dell'onda C del mandato — `_analisi/MAPPA-MANDATO.md` righe 574-641, 753-760; spec `docs/superpowers/specs/2026-09-20-invarianti-design.md`, piano `docs/superpowers/plans/2026-09-20-invarianti.md`). Nuovo banco `strumenti/_q-invarianti.js`: nove invarianti del mandato (Appendice A) verificate su partite CPU-CPU a seme fisso (20260920, taglia 5, 8 semi, 56.984 fotogrammi campionati, **9/9 verde**) — NaN/Infinity, owner valido, punteggio monotono, timeLeft monotono, durata<=13200 fotogrammi (INV-15), i cronometri-fratelli (recT/vantaggio/possOwner/possT/pulse/crowdSndT/swLock/swTimer) al riposo dopo startMatch (LA PIÙ A RISCHIO: cinque regressioni pagate a mano #86/#87/#107/#117/#122), il clamp fiato/cond in [0,100], >=2 uomini di movimento in campo, e la palla mai sotto il piano/velocità entro un tetto calibrato (1353 u/s orizzontale, 402 u/s verticale, empirico su 30 semi/222.282 fotogrammi, non teorico). Ciascuna nata rossa sul proprio bugiardo (`--bugiardo nan|owner|punteggio|timeleft|durata|fiato|movimento|ballz|ballvel`). **È UN BANCO, non codice nel motore**: legge G vivo via `__test`, zero costo a runtime, zero rischio nel motore — la mappa citava l'opzione "in-motore sotto flag" come non decisa, resta un seguito se il fuzzer lo chiederà |
| 2 | **#124 CHIUSO — il cambio di base di misura dei sorteggi, dichiarato.** `strumenti/_c3-sorteggi.js` faceva l'errore #108 (`setCpuVsCpu(true)` prima di `startMatch`, annullato in silenzio): la squadra 0 restava "umana immobile". Cura: scambio di due righe, sul modello di #121. I totali dei sorteggi cambiano perché la squadra 0 ora è CPU vera — **300.424 sorteggi (ordine vecchio) → 322.283 (ordine nuovo)** su 15 partite identiche: cambio di METODO di misura dichiarato, non un difetto; i confronti storici restano validi (simmetrici), i numeri nuovi non sono comparabili ai vecchi |
| 3 | **LA MAPPA INV-01..15 (mandato, Appendice A) dichiarata onestamente in `MANUALE.md`**: QUI (INV-02 parziale, INV-15), QUI ADATTATA (INV-03: ">=2 uomini di movimento" invece di "mai <7"), QUI parziale (INV-05 timeLeft, INV-11 clamp fiato/cond), ALTROVE (INV-01 `_q-determinismo`, INV-08 parziale `_q-regole`, INV-10 parziale `_q-umore`, INV-12 due-versioni/disegno-puro), RIMANDATA al fuzzer/soak (INV-04, INV-06, INV-07), N/A (INV-09 fuorigioco, INV-13/14 rete/submission — CALCETTO è locale). **Tre caveat a registro, candidati fuzzer**: (a) `G.swLock`/`G.swTimer` scritti solo da input umano, la prova 6 è debole su quei due campi in CPU-CPU; (b) il tetto della prova 9 (velocità palla, TETTO_VEL_PALLA=1353, margine 1,5x) è tarato empiricamente su CPU-CPU di serie, non ancorato a `TIRO_TETTO` — ancorare a `TIRO_TETTO+spin` sarebbe più difendibile, seguito; (c) INV-08 ("conteggi cartellino non decrescono") e INV-11 ("fatica non cresce durante i fermi") sono scoperte-ma-non-testate. **La non-scoperta sulla palla**: nessuna violazione P0 (z>=0 strutturale, velocità clampata dal gioco), il ramo "furto col corpo" che sembrava violare il tetto è una correzione per-frame non-volo, escluso di proposito dalla prova (lezione #112/#114) |
| 4 | **Batteria**: `_q-invarianti` registrato in `strumenti/tutti.js` (`conta:true`, modello regole/umore/cpu-ordine/accessibile, OK in 12s, corre in compagnia). Batteria intera rilanciata: **34 cancelli eseguiti in 588s di orologio (2,3 volte più veloce che in fila), 33 cancelli che contano tutti VERDI**; `audio.js` (lento, escluso dalla corsa di default) verificato a parte **28/28 VERDE** (stato dichiarato dal #122, non toccato qui); il solo informativo `istantanea.js` (non conta) **NO 46/56**, contro un riferimento del 20 agosto che era una prova NULLA — lo stesso schema già dichiarato dalle voci #113/#114/#122, non un peggioramento di questo cantiere — **«VERDE CON RISERVA»** complessivo. `git diff CALCETTO-il-gioco.html` vuoto per l'intero cantiere. Verbale completo in `MANUALE.md` §A registro, voce #125 |
| 5 | **LO STATO VERO A META' GIORNATA**: **onda B chiusa** (#117/#121/#122/#114/#113, righe della giornata del 19). **Onda C avviata**: **#125 CHIUSO** (riga 1-4, invariant-checker, primo anello) insieme al seguito **#124 CHIUSO** (riga 2, il cambio di base di misura dei sorteggi). RIGA SUPERATA dalla riga 5 della sezione "20 settembre, seconda parte" sotto (il fuzzer #126 ha girato, trovato due P0, e il cantiere dedicato #128 le ha curate) |

## La giornata del 20 settembre, seconda parte

| | cosa |
|---|---|
| 1 | **#126 (fuzzer, onda C-2) primo giro: DUE VIOLAZIONI VERE trovate** (spec/piano `docs/superpowers/specs/2026-09-20-crepe-fuzzer-design.md`, `docs/superpowers/plans/2026-09-20-crepe-fuzzer.md`). Comandi casuali deterministici via `Reg`+`Touch5` (voce #126, onda C-2, compito 1) hanno reso ROSSE due delle nove invarianti di `_q-invarianti.js` (#125): il cross-proiettile (`doCross`, dist non limitato — misurato 1433,8 u/s contro `TIRO_TETTO=860`) e il battitore espulso (`resetKickoff`, nessun controllo `out<=0` sulla scelta dell'idx1 — owner finiva su un giocatore appena espulso). Il committente ha scelto: cantiere DEDICATO (**#128**) per curare entrambe le P0 (mandato §13.3: «ogni bug ha prima un test fallito»), il fuzzer resta sospeso e riprende dopo, rebasato sul gioco curato |
| 2 | **#128 CANTIERE CHIUSO — le due crepe del fuzzer curate** (tre compiti dal merge-base `bc2d802`). **P0-1 doCross**: `Math.min(TIRO_TETTO, dist/T)` nella chiamata a `kickBall` (attrezzo `_t-crepe-docross.js`) — il cross oltre il tetto ricade prima invece di volare come un proiettile, il cross normale (399,07 u/s) invariato. **P0-2 resetKickoff**: `&& p.out<=0` sulla scelta del battitore + fallback `diMovimentoInCampo(kt)[0]` (attrezzo `_t-crepe-kickoff.js`) — un espulso non riceve mai più `G.ball.owner`, il kickoff normale invariato. Entrambe nate ROSSE sul test dedicato (prova 10/DOCROSS e 11/KICKOFF-ESPULSO di `_q-invarianti.js`), VERDI dopo la cura — **`_q-invarianti` 11/11** |
| 3 | **MOTORE_V: 1 → 2** (compito 3, attrezzo `_t-crepe-motorev.js`): le due cure cambiano l'esito di sequenze di comandi identiche (misurato: seme 20260812 diverge al fotogramma 1059 sul cross lungo; seme 20260836 dà 3-4 invece di 1-2 sul kickoff dopo espulsione) — un nastro vecchio rigiocato sul motore curato darebbe un'altra partita. `Sfida.guarda` (meccanismo della voce #107, invariato) lo rileva e rifiuta con causa vera e zero penalità, **verificato coi numeri**: `_q-regole.js` prova 13/NASTRO-VERSIONE estesa con un CASO C (nastro a MOTORE_V=1 esplicito, rigiocato sul gioco a MOTORE_V=2, rifiutato) — e un buco proprio nella prova B della stessa prova, un valore inchiodato (`===1`) che si sarebbe rotto da solo proprio oggi, trovato e chiuso (`>0`, non più legato al numero). **Due-versioni dichiarato per taglia**: doCross **16/60** (`_c3-sorteggi`, taglie 5/7/11), resetKickoff **1/120** (sequenza CPU-CPU) — diverge per costruzione, `_q-determinismo` resta **10/10** |
| 4 | **Il residuo di determinismo cross-partita: artefatto A TAGLIA 5, non un bug del gioco A QUELLA TAGLIA — a 7/11 è un canale VERO e SEPARATO, la voce #98, ora con causa isolata** (correzione di revisione: la diagnosi originaria generalizzava oltre quanto misurato). Durante la diagnosi di P0-2 la stessa coppia di semi dava partite diverse a seconda di quante ne precedevano sulla stessa pagina. Isolato **a taglia 5**: la simulazione resta deterministica rispetto a {seme, comandi} (byte-identica fresca contro dopo-N-partite, a parità di `startMatch`); il canale che sopravvive, a quella taglia, è quello dei TOCCHI (input-a-dita, documentato dal gioco stesso accanto a `Reg`, ~:13183 — la levetta e i verbi a tenuta non si azzerano da soli fra una partita e la successiva sulla stessa pagina se non lo fa chi chiama); il **soak (#127) non ne è affetto a taglia 5**; il **fuzzer (#126)**, a dita vere, dovrà azzerare `Touch5`/`Reg` fra le partite a quella taglia. **A taglia 7/11 «zero dita» non basta**: `_q-determinismo` dà **8/10** (contro 10/10 a 5) — la voce #98, pre-esistente su `main`. Causa ORA ISOLATA: `startMatch`→`setTaglia`→`rebuildCrowd` (`CALCETTO-il-gioco.html:29922-29949`) consuma `dado()`/`SEME` in proporzione al perimetro del campo sulla prima partita giocata a una data taglia (~114.026 estrazioni in più a taglia 11: `SEME.n` 114.093 contro 67), e lo stream del PRNG di gioco slitta — non è una regressione di questo cantiere. Seguito ingegneristico aperto **#129**. Consegna a #126/#127: girare a taglia 5, o dichiarare la #98 a 7/11 |
| 5 | **LO STATO VERO, oggi**: **onda B chiusa** (riga 5 della prima parte). **Onda C**: **#125 CHIUSO** e **#124 CHIUSO** (righe 1-4 prima parte), **#128 CHIUSO** (righe 1-4 di qui: le due P0 del fuzzer curate, MOTORE_V 1→2, il residuo dei tocchi diagnosticato a taglia 5, la voce #98 a 7/11 isolata). **RESTANO, in ordine**: il **fuzzer #126** (da riprendere rebasato sul gioco curato — i suoi test 9/2 tornano verdi per costruzione — completando duello, INV-04 e batteria, a taglia 5, con la nota sui tocchi da azzerare fra partite), poi il **soak #127** (1000 partite/notte, zero violazioni, a taglia 5). **#123** resta fuori onda, a registro. **Seguiti aperti residui, nessuno bloccante**: **#89, #97, #98 (causa isolata, vedi riga 4), #99, #101, #102, #103, #104, #105, #106, #109, #118, #129 (nuovo: la cura ingegneristica del PRNG di `rebuildCrowd`/`setTaglia`)** — RIGA SUPERATA dalla riga 7 sotto (il fuzzer #126 ha chiuso: duello+INV-04 confermati, batteria+verbale del compito 3, nessuna P0 nuova) |
| 6 | **Batteria**: `node strumenti/tutti.js` — **34 cancelli eseguiti in 949s di orologio (2,3 volte più veloce che in fila), i 33 che contano tutti VERDI** (`abbandono`/`audio`/`volti`/`avvio`/`avvio-telefono` esclusi dalla corsa di default); `audio.js` verificato a parte **28/28 VERDE** (stato dichiarato dal #122); il solo informativo `istantanea.js` **NO 46/56**, contro un riferimento del 20 agosto che era una prova NULLA — lo stesso schema già dichiarato da #113/#114/#122/#125, non un peggioramento di qui — **«VERDE CON RISERVA»**. `git diff CALCETTO-il-gioco.html` per l'intero cantiere: due ancore nel compito 1 (clamp doCross), due nel compito 2 (condizione+fallback resetKickoff), una nel compito 3 (MOTORE_V). Verbale completo in `MANUALE.md` §A registro, voce #128 |
| 7 | **#126 CANTIERE CHIUSO — il fuzzer di comandi, compito 3 (batteria + verbale)**, ripreso dal merge-base `f27d951` (dopo la cura #128). Compiti 1-2 (base + duello/INV-04/SAVE.rosa) confermati verdi sul gioco curato senza nessuna modifica: **`_q-fuzzer.js` 15/15**, 20 semi, 0 esclusi per duello, 150.589 fotogrammi simulati, `G.swLock` attivo in 20/20 semi (147.134 fotogrammi-tick su 150.589), INV-04 (margine 110 unità) verde su tutti i fotogrammi campionati, riproduzione (nastro+log-duelli) e determinismo cross-partita (k=1 e k=19) entrambi verdi — misurato due volte, stesso esito al bit, **~17 s** (sotto la soglia lento). Registrato in `strumenti/tutti.js` (`conta:true`, modello `regole`/`umore`/`invarianti`/`cpu-ordine`). **La rettifica a edizioni della diagnosi #128** (riga 4 sopra): il residuo cross-partita a taglia 5 non era il canale dei tocchi (`stick.ox/oy`, già sano, verificato campo per campo) ma **`SAVE.rosa`** — la rosa di carriera cresce di un attributo a ogni fine-partita per disegno; il fuzzer rigenera la rosa (`nuovaRosa()`) a ogni seme, e questo è ora scritto in chiaro alla voce #126 di `MANUALE.md`, con la vecchia diagnosi #128 lasciata in chiaro accanto e non cancellata. **NESSUNA P0 NUOVA**: le due violazioni del primo giro (cross-proiettile, battitore espulso) restano curate e confermate assenti; il fuzzer è ora una rete permanente in batteria. **Batteria intera rilanciata DUE volte**: la prima corsa (banco occupato 3,4x) ha dato **35 cancelli, un solo rosso** su `prestazione` (cronometrico, +29,4% contro +25% ammesso), **indagato e non richiuso a occhi chiusi**: rimisurato da solo su banco più libero, **3/3 VERDE** — sospetto di contesa, non ancora la prova che l'intera batteria fosse pulita. La **seconda corsa intera** (nessuna riga toccata fra le due) l'ha confermato: **35 cancelli eseguiti in 716s di orologio (2,4 volte più veloce che in fila), i 34 che contano TUTTI VERDI** — `prestazione` incluso, pulito dal primo colpo — «VERDE CON RISERVA» per il solo informativo `istantanea` (46/56, stesso schema già dichiarato da #113/#114/#122/#125/#128). `_q-fuzzer` **41s** dentro la corsa in compagnia (contesa dei 4 cancelli paralleli) contro i **~17s** misurati da solo su banco libero — resta `lento:false`. Il gioco non è mai stato toccato dal ramo. `git diff main -- CALCETTO-il-gioco.html` **VUOTO** per l'intero cantiere #126. **Domanda aperta, non risolta**: se le SFIDE portano un nastro fra due telefoni, la rosa di ciascuno può differire — va verificato se serve una rosa canonica nel nastro (come già per `sponde`/`miraGuidata`) o se è un canale nuovo, non censito. Verbale completo in `MANUALE.md` §A registro, voce #126 |
| 8 | **LO STATO VERO, oggi**: **onda C: 2 su 3 chiusi** — **#125 CHIUSO**, **#124 CHIUSO**, **#128 CHIUSO** (righe 1-6), **#126 CHIUSO** (riga 7: duello, INV-04, batteria e verbale, nessuna P0 nuova). **RESTA, ultimo anello dell'onda C**: il **SOAK #127** (1000 partite/notte, zero violazioni, a taglia 5 per la #98 — e che dovrà rigenerare `SAVE.rosa` a ogni partita come il fuzzer, non solo azzerare i tocchi: CPU-contro-CPU non usa dita, ma userebbe comunque la stessa pagina per N partite). **Seguiti aperti residui, nessuno bloccante**: **#89, #97, #98 (causa isolata), #99, #101, #102, #103, #104, #105, #106, #109, #118, #123, #129**, più la **domanda aperta sulla rosa canonica nelle sfide** (riga 7, nuova, non ancora numerata) |
| 9 | **#127 CANTIERE CHIUSO — il soak con bande, e L'ONDA C È CHIUSA (3/3)** (tre compiti dal merge-base `ddf6604`; spec/piano `docs/superpowers/{specs,plans}/2026-09-20-soak{-design,}.md`). `strumenti/_q-soak.js` guida un VOLUME di partite CPU-CPU a taglia 5 (rosa rigenerata a ogni partita come il fuzzer, ordine giusto), riusa le invarianti di `_q-invarianti.js` + INV-15 su ogni partita, e misura BANDE statistiche ancorate — complemento del fuzzer (il fuzzer stana il caso avversariale, il soak il caso raro nel volume). Campione di batteria **60 partite, 17/17 verde**, max 10.172/18.000 fotogrammi (56,5%), 32,9 s, impronta ripetibile `21f65940` (`lento:true`); il volume del mandato resta `--partite 1000` a mano (~8 min). **RICALIBRAZIONE del tetto INV-15 13.200→18.000 fotogrammi** (il valore del soak sul volume): su 1000 partite lo 0,5% superava i 220 s — NON un hang ma un raro **rigore a oltranza** legittimo (~236 s, cap 18 tiri `:18523`); tetto ricalibrato al caso peggiore MISURATO (pre-rigori 180,9 s + 18×328 fotogrammi su 46.776 tiri), l'hang resta colto (infinito, `--bugiardo durata` rosso). La ricalibrazione ha esposto e curato **due bug dei banchi**: `_q-cpu-ordine` teneva un duplicato locale del tetto (ora importa lo shared); `_q-invarianti` validava i flag senza la guardia `require.main` (crashava se requerito). **Bande** su 150 partite a taglia 5 (durata viva ~92,3 s, %0-0, gol/tiri/parate), larghezza generosa dichiarata, caso di controllo `--bugiardo bande` che condanna la sola banda toccata; divergono da `_eventi.js` (tiri 13 vs 10) perché il soak rigenera `SAVE.rosa` — dichiarato; 7/11 solo informative (#98). `git diff main -- CALCETTO-il-gioco.html` VUOTO. **L'ONDA C È CHIUSA**: invarianti (#125) + fuzzer (#126) + soak (#127), tutti in `main` — rete di robustezza permanente in batteria; 2 P0 vere trovate e curate (#128); causa #98 isolata (#129); tetto ancorato al caso peggiore reale. Verbale completo in `MANUALE.md` §A registro, voce #127 |
| 10 | **LO STATO VERO, oggi** (sostituisce la riga 8): **onda C CHIUSA, 3 su 3** — #125/#124 (prima parte), #128/#126 (righe 1-7), **#127** (riga 9). **RESTA del mandato**: onda D (competizione) ed E (live 1v1), non ancora aperte; **#118** (MIND v2). **Seguiti aperti, nessuno bloccante**: **#129** (cura del PRNG di `rebuildCrowd`/`setTaglia`, per il determinismo a 7/11 — voce #98), **INV-06/07** (validità gol/ripresa, asserzioni dedicate), **#123** (fotosensibile per-regione), la **rosa canonica nelle sfide** (domanda aperta dal #126), la nota che la fase `power` del duello non ha un tetto a livello di codice (bound empirico); più i fuori-mandato **#89, #97, #99, #101-#106, #109, #115**. Un file `CLAUDE.md` di onboarding è stato scritto durante il #126 ma lasciato **UNTRACKED** — da validare col committente prima di committarlo — RIGA SUPERATA dalla riga 11 sotto (**#129 CHIUSO, #98 CHIUSA**) |
| 11 | **#129 CANTIERE CHIUSO — LA VOCE #98 SI CHIUDE** (due compiti dal merge-base `81bb961`; spec/piano `docs/superpowers/{specs,plans}/2026-09-20-rebuildcrowd-prng{-design,}.md`). **Rettifica a edizioni della diagnosi #128**: `rebuildCrowd` era solo l'8% del consumo del PRNG a taglia 11 (~8.920 sorteggi su ~114.093), il dominante (~92%) era `paintField`/`buildFieldTex` (dentro `resize()`, dentro `setTaglia`, prima di `rebuildCrowd`); e c'erano DUE canali (SEME e Math.random-globale di `_q-determinismo.js`), non uno — un salva/ripristina scoperto sul solo `rebuildCrowd`/SEME (prima cura tentata in questo stesso cantiere) non bastava, misurato: `_q-determinismo --taglia 11` restava 8/10, identico. **Cura decisa dal committente (opzione 2)**: PRNG dedicato `DECO` per la cosmetica (folla + texture del campo), riseminato a costante fissa all'ingresso di `paintField` e `rebuildCrowd`, che smettono di leggere/scrivere sia `SEME` sia `Math.random` — cura entrambi i canali insieme. **Misurato**: `_q-determinismo` **8/10 → 10/10** a taglia 7 E a taglia 11 (10/10 confermato a taglia 5); due corse di `_q-soak --taglia 11` stesso seme → stessa impronta (`fb5d47b1`), impronta stabile a taglia piena — **RETTIFICA della revisione finale**: la prima stesura diceva «prima impossibile», ed è FALSO (misurato: anche la base `81bb961` è bit-ripetibile corsa-su-corsa, impronta `f5a869ab`); la #98 rompeva l'uguaglianza FRA partite e FRA pagine, non la ripetibilità dello stesso banco. La prova della cura resta `_q-determinismo` 8/10 → 10/10. **Non-regressione a taglia 5, bit per bit**: `_eventi.js` 30 partite, campo `crudo` del JSON byte-per-byte IDENTICO prima/dopo; `_q-invarianti` 12/12, `_q-fuzzer` 14/14, `_q-soak` 17/17, `collaudo.js` 36/36, `_q-battute.js` 11/11. Un rosso incontrato (`_q-soak --taglia 11`, INV-15, seme 20260924 bloccato in `freekick`) è **pre-esistente** anche sul gioco non curato — difetto di gioco a taglia 11 scollegato dalla cosmetica, a registro, non curato qui. **RETTIFICA A EDIZIONI (21 settembre 2026, voce #130 "il metro prima del giudice")**: questa affermazione era FALSA, misurata di nuovo (`fuori/_misura-seme-20260924.js`): il seme 20260924 a taglia 11, stesso ordine giusto, SENZA tetto, raggiunge `'end'` al fotogramma **19.502 (325,0s) con punteggio 2-1** — NON è bloccato. Il rosso era `TETTO_FOTOGRAMMI=18000`, tarato sul caso peggiore di TAGLIA 5 (partite da 90s) e applicato a taglia 11, dove la partita di regolamento dura già 180s (`durataPartita()`): non un difetto del gioco, un metro sbagliato. Curato dalla voce #130: `tettoFotogrammi(taglia)` sostituisce la costante unica (18000 a 5, invariato; 21000 a 7; 27000 a 11, misurati). **Regressione cosmetica dichiarata e voluta**: `istantanea.js` (non contato) 46/56 → 45/56, dentro il rumore fra corse che lo strumento stesso dichiara — texture e folla cambiano aspetto, è il costo accettato dell'opzione 2. **MOTORE_V resta 2**: a taglia 5 l'esito di gioco è invariato (nastri/sfide rigiocano identici, `_q-replay` 10/10 e `_q-regole` 16/16 senza alcuna modifica), e non esisteva alcun nastro affidabile a 7/11 da rompere (era proprio la #98 a impedirlo). **Batteria intera rilanciata, 41 cancelli, tutti quelli che contano VERDI** dopo aver indagato e chiuso due rossi (non ignorati): `folla.js` (seme di boot 20260728→**20260901**: la cosmetica non consuma più Math.random condiviso al boot, stesso seme vecchio → un'altra partita → la striscia di tribuna misurata non era più quella giusta per coincidenza) e `_q-volo.js` (`SEME_VOLO` 88001→**88005**, `SEME_INSEGUE` 88002→**88012**: i suoi scenari cambiano taglia 5↔7 sulla STESSA pagina, cioè proprio le transizioni che la cura rende neutre — precisazione della diagnosi: NON solo "prima partita a una taglia nuova da pagina fresca", ma OGNI transizione, comprese quelle che tornano a 5; non è un buco della non-regressione a taglia 5 di `_eventi.js`, che parte sempre da un avvio fresco). Dettaglio completo in `MANUALE.md` §A registro, voce #129. `git diff main -- CALCETTO-il-gioco.html`: `DECO` (definizione) + tre funzioni cosmetiche convertite (`paintField`, `buildGrain`, `rebuildCrowd`) — nessuna funzione di gioco toccata; i due semi ritarati vivono solo in `strumenti/`. **LA VOCE #98 (determinismo instabile a 7/11) È CHIUSA**: isolata parzialmente al #128, isolata per intero e curata al #129 — 7/11 sono ora deterministici 10/10, allo stesso titolo di taglia 5. **Seguiti aperti, nessuno bloccante**: **INV-06/07, #123**, la rosa canonica nelle sfide, il tetto empirico della fase `power`, ~~il difetto di gioco a taglia 11 (`freekick`/INV-15, appena trovato, non numerato)~~ (RETTIFICATO dalla voce #130: non era un difetto di gioco, era il tetto INV-15 flat applicato a ogni taglia — chiuso, vedi sopra), più i fuori-mandato **#89, #97, #99, #101-#106, #109, #115** |

## La giornata del 19 settembre, in breve

| | cosa |
|---|---|
| 1 | **#117 CANTIERE CHIUSO — MIND v1** (il registro dei fatti e il modello emotivo, sei compiti dal merge-base `f352af5`, prima voce dell'onda B del mandato: `_analisi/MAPPA-MANDATO.md` §2, spec `docs/superpowers/specs/2026-09-19-mind-v1-design.md`). `G.fatti` (registro passivo, 15 tipi di evento), `p.umore`/`p.nervi`/`G.spinta` (tre stati derivati dai fatti, zero `dado()`), il canale `manopolaDi(p)` (passErr/slideP/standoff modulati per giocatore, mai sull'input umano), i due canali d'occhio (mesto dai fatti, folla+banner sulla spinta). `p.celeb`/`p.mesto` erano già due stati emotivi: il MIND li rende l'espressione di stati continui, non li inventa. Banco `strumenti/_q-umore.js` nato ROSSO con la sola prova REGISTRO, arrivato a **30 prove su 30** (REGISTRO/STATI/CANALE/TETTI/TESTIMONE/SPECCHIO/INPUT-SACRO), le due versioni bugiarde (`_crit-mind-tetto.js`, `_crit-mind-muto.js`) condannate ciascuna sulla propria prova |
| 2 | **Giocabilità misurata, DENTRO BANDA**: `_eventi.js`, `f352af5` contro la punta, 300 partite CPU-CPU per versione (semi 20260803..20261102, taglia 5): gol/90s media 2,72→2,63 (**-3,3%**, ben dentro il ±20% del committente), MOMENTI DA PORTA/min -2,8%, EVENTI/min -0,3%. **Nota di metodo**: la MEDIANA di gol/90s è instabile a piccoli N (+50% a N=30, -33% a N=100, -17% a N=300, segni opposti sulla STESSA coppia di versioni) — è rumore di campionamento su un conteggio a piccoli interi, non il gioco: la MEDIA è l'indicatore stabile qui. L'hang #119 (freekick, preesistente) non si è manifestato in 860 partite campionate su nessuna delle due versioni |
| 3 | **UNA REGRESSIONE TROVATA E RIPARATA RILANCIANDO LA BATTERIA INTERA** (quinta occorrenza della lezione ricorrente, la 22 in fondo a questo file: #86/#87/#107/#112 ne avevano già pagata una). `strumenti/_q-replay.js` è uscito ROSSO sulla prova E ("registrare non cambia il gioco"): accendere il registro del nastro spostava la fisica della stessa partita a seme fisso dal fotogramma 80. Bisecato ai quattro compiti: verde su `cb23512`/`53007c5` (compiti 1-2), rosso da `8824222` (compito 3) in poi. Causa vera PRE-ESISTENTE: `G.swLock`/`G.swTimer` (isteresi del cambio-giocatore automatico) non erano MAI azzerati da `startMatch` — il sesto cronometro fratello di `G.recT`/`G.vantaggio` (già riparati il 31 agosto e a #107). Si vede solo dal compito 3 perché `manopolaDi(p)` è la prima lettura a far dipendere una decisione della CPU da quale giocatore specifico è sotto controllo umano. **Irrilevante per ogni misura CPU-CPU** (verificato: numeri di giocabilità e sorteggi identici cifra per cifra prima/dopo la cura) — riguarda solo la squadra umana di una sfida vera. Curato (`strumenti/_t-swlock-reset.js`, un ancoraggio, zero sorteggi nuovi): `_q-replay.js` torna **10/10**. L'altro rosso della stessa corsa, `audio.js` (parata del portiere → crowdLevel×8-9 jingleTick), è **PRE-ESISTENTE** su `f352af5`, scollegato dal MIND: seguito **#120** aperto, non curato qui. Batteria intera rilanciata DOPO la cura: **36 cancelli eseguiti, 33 con verdetto valido che conta, 32 verdi, 1 rosso** (`audio.js`, pre-esistente) |
| 4 | **La divergenza dei sorteggi, dichiarata per taglia** (`_c3-sorteggi.js`, `f352af5` contro la punta, per costruzione — il canale del compito 3 cambia le decisioni della CPU): taglia 5 **18/20**, taglia 7 **19/20**, taglia 11 **20/20**, totale **57/60**. `_q-determinismo --partite 4` resta **13/13** (l'invariante del multigiocatore, intatta). Verbale completo in `MANUALE.md` §A registro, voce #117 |
| 5 | **RESTANO, in ordine**: **#118 (MIND v2)** — contagio nel tempo, peso del capitano, regolazione senza intervallo, la striscia del momento post-partita, il volto che cambia, il dischetto sotto pressione, l'HUD dell'umore (il "Fuori perimetro" dello spec §6); la **voce #89** (meccaniche dei verbi, cantiere della decomposizione del 1° settembre). **Seguiti nuovi**: **#119** (l'hang del freekick, ~metà delle partite CPU-CPU a taglia 5 bloccate, cantiere dedicato), **#120** (il rosso pre-esistente di `audio.js`, mai indagato prima perché mai emerso finché la batteria intera non è stata rilanciata). **Seguiti aperti, nessuno bloccante**: **#97, #98, #99, #101, #102, #103, #104, #105, #106, #108/#110, #109, #113, #114, #115, #118, #119, #120** — RIGA SUPERATA dalle righe 6-8 sotto (#108/#110/#119/#120 chiusi, #116 nato e chiuso) |
| 6 | **#121 CANTIERE CHIUSO — pulizia #108** (seguito #108, quattro commit `1a863e5..39810c4`, **in `main`**, stesso giorno): applica la cura #108 (ordine `setCpuVsCpu`/`startMatch` — la chiamata che ARMA la CPU deve venire DOPO quella che AVVIA la partita, mai prima) ai tre banchi di batteria che la sbagliavano. Compito 1: `_q-battute.js` (5 siti) e `_q-regole.js` (15 siti) — la PROVA 1 di quest'ultimo riscritta (I4b) per reggere l'esito lecito del vantaggio-in-area con la CPU vera (gol della squadra offesa prima del rigore ritardato), **16/16** verde. Compito 2: `_q-umore.js` (5 siti, RISCHIO CONCRETO dichiarato dal progetto — le prove TETTI/TESTIMONE misuravano su una sola CPU vera invece di due), **30/30** verde, nuovi massimi osservati per le tre manopole del MIND (nessun tetto della formula superato — solo il massimo osservato cambia, vedi `MANUALE.md` voce #117 punto 3). Compito 3: nuovo banco anti-regressione `strumenti/_q-cpu-ordine.js` (**3/3**, verbale mancante fino ad oggi — aggiunto in `MANUALE.md` dalla riga 8 sotto), che interroga il COMPORTAMENTO del gioco sull'ordine delle due chiamate ad ogni corsa in batteria, invece di elencare i file noti di un censimento — **CHIUDE #110** ("il banco che non congela"). La correzione dell'ordine nei tre banchi elimina anche il sintomo diagnosticato da **#119**: con l'ordine giusto, 0 partite su 31 restano bloccate in `freekick` (diagnosi esperimento A, coerente con le partite CPU-CPU vere misurate nei compiti 1-2) — **#119 CHIUSO**. **#108 CHIUSO** su tutti e tre i banchi di batteria (restano ~22 strumenti d'archivio col vecchio ordine, fuori batteria, fuori perimetro — censimento in `MANUALE.md` voce #107, I4). Due-versioni non applicabile (nessuna cura tocca il gioco: `CALCETTO-il-gioco.html` non toccato in nessuno dei tre compiti) |
| 7 | **#122 CANTIERINO CHIUSO — spiccioli di seguito** (tre compiti dal merge-base `470149a`, chiusura di pendenze minori decisa dal committente dopo #117/#121; piano `docs/superpowers/plans/2026-09-19-spiccioli-seguiti.md`): **#120** (il rosso pre-esistente di `audio.js`, riga 3 sopra) curato NEL BANCO — lo scenario "parata del portiere" ora azzera `b.lastTouch`/`b.toccoPiede` prima di simulare (la guardia del retropassaggio, voce #107, rifiutava il tiro come autopassaggio), `audio.js` **28/28** (era 27/28); il gioco non è toccato. **#116** ("il numero fantasma", nato e chiuso nello stesso cantierino): il sottotesto di SOTTOTITOLI non promette più gol/palo che il flag `SAVE.sott` non governa — "fischio — a video". **#115**: la freccia di direzione non copre più l'arco del fiato (ordine di disegno invertito dentro `anelloComandato`, nessuna geometria toccata), con `_q-accessibile.js` esteso a un confronto DIFFERENZIALE (fiato 55/70, freccia dentro/fuori la zona accesa) che nasce rosso sul gioco PRE-#115 (diff 0,0333) e verde dopo (diff 0,0000) — un confronto assoluto con la tolleranza delle altre righe non l'avrebbe scoperto (il cuneo della freccia si restringe verso la punta, dove vive l'arco: l'overlap vero è molto più stretto dei ~53° nominali della base). Cancelli: due-versioni (`_c3-sorteggi`, base `470149a`, taglie 5/7/11) **0/60**; `_q-determinismo` **10/10** intatto; `_q-accessibile.js` **7/7**; `istantanea.js` — stesso schema OK/NO prima/dopo (47 OK, 10 NO). Attrezzi a specchio (`_t-sott-onesto.js`, `_t-freccia-fiato.js`) verificati byte per byte. Verbale in `MANUALE.md` §A registro, voce #122 |
| 8 | **LO STATO VERO, oggi** (questa riga sostituisce la riga 5: `PUNTO-DEL-LAVORO.md` era fermo al commit `48921bd`, prima delle quattro righe sopra). **Onda A CHIUSA** da tempo (#86/#85/#87/#107/#112, più le pendenze minori #120/#116/#115 appena chiuse dalla riga 7). **Onda B INIZIATA**: **MIND v1** (#117, riga 1, **in `main`**) e una prima pulizia già incassata (**#121**, riga 6, **in `main`**). **CHIUSI da questa giornata**: #108, #110, #119 (riga 6), #120, #116, #115 (riga 7). **Seguiti aperti residui, nessuno bloccante**: **#109** (il corpo del portiere che scala col campo — richiede un censimento a tutto il file), **#118** (MIND v2 — contagio nel tempo, peso del capitano, regolazione senza intervallo, la striscia del momento post-partita, il volto che cambia, il dischetto sotto pressione, l'HUD dell'umore). **#114 CHIUSO** dalla riga 9 sotto (banco fotosensibile ancorato a WCAG), nuovo seguito **#123**. **#113 CHIUSO** dalla riga 10 sotto (mira guidata a due pesi — chiude insieme al #114 la coppia di accessibilità), nuovo seguito **#124**. Restano fuori dal mandato, mai assorbiti, nessuno bloccante: **#89** (meccaniche dei verbi), **#97, #98, #99, #101, #102, #103, #104, #105, #106** |
| 9 | **#114 CANTIERE CHIUSO — il banco fotosensibile ancorato a WCAG** (tre compiti dal merge-base `ebf6bb1`, seguito del #112: spec `docs/superpowers/specs/2026-09-19-fotosensibile-wcag-design.md`, piano `docs/superpowers/plans/2026-09-19-fotosensibile-wcag.md`). `strumenti/_q-fotosensibile.js` misura ora in luminanza relativa WCAG vera (linearizzazione gamma per canale, non più byte sRGB grezzi), rileva i flash come coppie di transizioni opposte (ampiezza >=0,10, scuro <0,80, finestra 1 s, verdetto <=3 flash/s = 3 Hz — non più `PROMINENZA_MIN=1,5` tarato sul gioco), applica il criterio d'area WCAG (0,006 sr / 25% di un campo di 10°, proxy 2,7751% del canvas dalla risoluzione di riferimento 341x256@1024x768) come filtro a valle, e prova il red flash (R/(R+G+B)>=0,80 + Δu'v'>0,2, CIE 1976 UCS). **Verifica autorevole**: le sei scene reali (gol ravvicinati, sera, dischetto; moto on/off) tutte VERDI a seme fisso — nessuna supera 3 flash/s su nessun canale (il gol resta a 1 flash/s nonostante il 33% di area di picco, per il tetto strutturale della durata della festa; folla/duello restano sotto la soglia di ampiezza, un ordine di grandezza sotto 0,10); `--controllo` condanna GRANDE (4 Hz schermo intero, 5/s di picco) e RED FLASH (4 Hz, #ff0000, 5/s su entrambi i canali) ed ESENTA PICCOLO (quadratino sotto soglia d'area, 4 Hz, 15 flash rilevati e tutti esenti) — il banco discrimina, non attesta. Saturazione rossa massima misurata su tutte le tinte del gioco: **0,623**, sotto la soglia 0,80. **IL LIMITE DICHIARATO**: il banco misura sulla media whole-canvas — (a) l'area è prevalentemente formale per QUESTO gioco (folla/duello già sotto soglia di ampiezza prima del filtro); (b) resta un buco teorico generale (un lampo fra ~2,77% e ~10% dello schermo ad alta frequenza sfuggirebbe alla media, che non lo vedrebbe nemmeno come flash) — il gioco di oggi non ha una sorgente in quella fascia, ma un analizzatore WCAG completo richiederebbe il rilevamento PER-REGIONE per chiuderlo — **seguito #123**; (c) il red flash soffre dello stesso limite (b) e non ha un filtro d'area proprio. Rettifica a edizioni della frase del #112 («nessuno strobo forte oltre 3 Hz», soglia tarata) fatta in chiaro in `MANUALE.md`, con la data accanto, senza cancellare il vecchio testo. Batteria (`strumenti/tutti.js`, `fotosensibile` già registrato `conta:true` dal #112, invariato) eseguita per intero: **31/31 cancelli che contano tutti VERDI** (`fotosensibile` OK in 42s), `audio.js` (lento, escluso dalla corsa di default) verificato a parte **28/28 VERDE** (il rosso pre-esistente del #120 resta curato dal #122, nessun residuo), il solo informativo `istantanea.js` (non conta) «NO» contro un riferimento nullo del 20 agosto (pattern già noto, non un peggioramento di questo cantiere) — «VERDE CON RISERVA» complessivo. `git diff CALCETTO-il-gioco.html` vuoto in tutto il cantiere. Verbale completo in `MANUALE.md` §A registro, voce #114 |
| 10 | **#113 CANTIERE CHIUSO — la mira guidata a due pesi** (tre compiti dal merge-base `d3169d3`, seguito §9.2 del mandato; spec `docs/superpowers/specs/2026-09-19-mira-guidata-design.md`, piano `docs/superpowers/plans/2026-09-19-mira-guidata.md`), **CHIUDE INSIEME AL #114 (riga 9) LA COPPIA DI ACCESSIBILITÀ** scelta dal committente dopo il MIND/#121. Scelta **(A)** dichiarata: due pesi sullo SCOPE di `switchControlled` (il salto di controllo post-passaggio di #88), non (B) un aiuto geometrico né (C) un ibrido — la geometria tarata di #88 (cono filtrante, errore angolare del tiro, 297 tiri misurati) resta intatta. `SAVE.miraGuidata` in `{'pieno','essenziale'}`, default 'pieno' (salvataggi vecchi bit-identici), letto una volta in `startMatch`; 'essenziale' restringe il salto ai soli cross, lasciando i passaggi corti senza salto anticipato; le sfide (`Sfida.gioca`/`guarda`) forzano 'pieno' come già fanno con `sponde:'gabbia'`. **Giocabilità misurata** (seme 113001, taglia 5): col peso 'pieno' cross e passaggio saltano ENTRAMBI al fotogramma 13; con 'essenziale' il cross salta ancora al 13 (nessun nuovo fastidio), il passaggio NON salta più entro soglia (20 fotogrammi) — il controllo passa al compagno solo al fotogramma 32, per il fallback naturale pre-#88, non per la mira guidata: il fastidio previsto da #88 §4.2 sui passaggi corti è risolto senza perdere l'aiuto sul cross. **MOTORE_V NON incrementato**: PIENO-IDENTICO (`_q-mira.js`) dà **0 differenze su 60 fotogrammi, due scene** fra `fuori/base113.html` e il curato col peso 'pieno' — resta a **1**. **SFIDA-DETERMINISTICA verificata dal vivo**: con `SAVE.miraGuidata` locale='essenziale', dopo `Sfida.gioca` sintetica `G.miraGuidata` legge 'pieno' (il SAVE locale è ignorato); `_q-replay` prova B resta **verde, 120 campioni** a peso fissato. **CPU-cecità**: `switchControlled` salta già le CPU per costruzione (guardia `G.ctrl[t]<0`, da #88); il gate ufficiale pieno-contro-pieno (base113/curato) è **0/60 in ENTRAMBI gli ordini** di `setCpuVsCpu` — un confronto DIVERSO (pieno-contro-essenziale, non il gate) fatto con l'ordine sbagliato del tool aveva mostrato 58/60 al compito 1, artefatto della squadra "umana ferma" che resta dentro la guardia; **nuovo seguito #124** (correggere l'ordine dentro `_c3-sorteggi.js` stesso, sul modello della cura #121). **Batteria**: `_q-mira.js` (5/5: SCOPE, SCOPE-BASE113, PIENO-IDENTICO, MIRA-UI-STILE, MIRA-ARIA) registrato in `strumenti/tutti.js` (`conta:true`, modello `regole`/`accessibile`/`umore`/`cpu-ordine`); batteria intera rilanciata (`strumenti/tutti.js`, corsa di default): **33 cancelli eseguiti, 32 che contano tutti VERDI** (`mira` OK in 12s), `audio.js` (lento, escluso dalla corsa di default) verificato a parte **28/28 VERDE** (stato dichiarato dal #122, non toccato qui), il solo informativo `istantanea.js` (non conta) «NO» **46/56** contro il registro del 20 agosto — lo stesso schema già dichiarato dalle voci #114/#122 (il riferimento era una prova NULLA, non un peggioramento di questo cantiere) — «VERDE CON RISERVA» complessivo. `_q-determinismo --partite 4` **13/13** intatto. `git diff CALCETTO-il-gioco.html` vuoto per l'intero cantiere: il gioco non è mai stato toccato dal compito 3. Verbale completo in `MANUALE.md` §A registro, voce #113. **Seguiti aperti residui, nessuno bloccante**: **#109, #118**, più **#124** nuovo di questa riga; **restano fuori dal mandato**: **#89, #97, #98, #99, #101, #102, #103, #104, #105, #106, #123** |

## La giornata del 18 settembre, terza parte

| | cosa |
|---|---|
| 1 | **#112 CANTIERE CHIUSO — L'ONDA A SI CHIUDE** (spiccioli di UX e accessibilità, sei compiti dal merge-base `73c1c64`, ultima voce dell'onda A del mandato). Le sei cure, tutte di puro contorno (zero `dado()`, zero stato che la CPU legge): **1.** `aria-pressed` sincronizzato sui **5** interruttori `.voce.sw`, banner dichiarato in `zoneInterfaccia()`. **2.** `SAVE.vibInt` a tre valori (leggera/normale/forte), `buzz()` scala la durata — corretta in revisione una riga CSS mancante (prova **VIBRAZIONE-STILE** nuova). **3.** RIVEDI IL TUTORIAL azzera entrambi i flag (`tutorialDone`+`tutorialVisto`), sottotitolo onesto, il tutorial vero riparte alla guardia esistente. **4.** Sottotitoli degli eventi sonori (`SAVE.sott`): le chiamate mancanti aggiunte ai 4 fischi muti (inizio/fine/le due ripresa), gli altri **9 siti di codice** con banner preesistente intatti (**correzione di revisione del compito 6**: 13 `Audio5.whistle(` totali − 4 curate = 9; RIMESSA/ANGOLO/RINVIO sono un solo sito di codice con tre varianti testuali, non tre siti). **5.** L'anello del fiato: arco lime dentro `anelloComandato`, proporzionale a `p.fiato`, misurato pixel per pixel (0 / 0,419 / 0,828 a fiato 0/40/80) — **seguito #115** per i due residui (freccia che copre il lime, leggibilità della quota intermedia). **6.** Banco nuovo `strumenti/_q-fotosensibile.js`: luminanza a schermo intero su tre sorgenti (folla, dischetto, il lampo+9 raggi del gol — l'unico a coprire tutto lo schermo), sei scene tutte verdi (picco 1 lampo/finestra di 1s contro un tetto di 3), `--controllo` a 4 Hz condannato (picco 5) — la prova che il banco discrimina. Registrato in batteria insieme a `_q-accessibile.js` (7/7, mai registrato prima d'ora). **LIMITE DEL BANCO, dichiarato** (correzione di revisione del compito 6): filtra per ESCURSIONE (`PROMINENZA_MIN=1,5`) prima di contare la frequenza — cieco a strobo piu' deboli a QUALUNQUE frequenza, tarato sul gioco di oggi, non su una soglia clinica; garantisce «nessuno strobo FORTE oltre 3 Hz», non «fotosensibile-safe» in assoluto — seguito **#114** per ancorarla a WCAG/Harding |
| 2 | **UNA REGRESSIONE TROVATA E CURATA chiudendo il cantiere**: la prima esecuzione della batteria INTERA su questo ramo (nessun compito di #112 aveva `disposizione.js` fra i cancelli sorvegliati) ha trovato `disposizione.js` **ROSSO** — **correzione di revisione del compito 6**: verde sul merge-base, poi **ROSSO da fine compito 4 in poi** (compreso fine compito 5: il compito 4 ha introdotto SOTTOTITOLI e nessun compito successivo l'ha mai rimossa, quindi non poteva tornare verde da sola), fino alla cura di oggi (compito 6) — il compito 5 non aveva `disposizione.js` nel proprio elenco di cancelli da sorvegliare, e per questo non se n'è accorto (è proprio la lezione 22 sotto). Causa vera: la terza voce (SOTTOTITOLI) aggiunta alla sezione «Accessibilità» del pannello IMPOSTAZIONI l'ha resa dispari, orfana nella griglia a due colonne. Il CSS aveva già una cura generica pronta e mai applicata (`.setwrap>.sola`); curata con un ancoraggio di una riga (`strumenti/_t-sottotitoli-sola.js`), zero `dado()`, due-versioni ancora 0/60 |
| 3 | **IL DUE-VERSIONI 0/60 DELL'INTERO CANTIERE #112**, la firma del contorno: `_c3-sorteggi.js` dal merge-base `73c1c64`, taglie 5/7/11, **0 partite su 60 con un conto diverso** (567.871=567.871) — nessuna delle sei cure ha spostato un sorteggio. `_q-determinismo --partite 4` **13/13**. Verbale completo in `MANUALE.md` §A registro, voce #112, coi due Minori del compito 4 chiusi nel testo (GOL/PALO restano sempre visibili, il flag copre solo i fischi; il sottotesto del bottone può trarre in inganno, chiarito) |
| 4 | **ONDA A DICHIARATA CHIUSA.** Le cinque voci del programma approvato dal committente sono tutte curate: **#86** (vernice/proporzioni ufficiali, 7 settembre), **#85** (moviola fluida, 7 settembre), **#87** (rimesse/angoli, 18 settembre), **#107** (regole a leva corta + versione motore nel nastro, chiude anche #96, 18 settembre), **#112** (spiccioli di UX, 18 settembre). **RESTANO, in ordine**: l'**onda B** (il registro dei fatti, P0 prerequisito, e **MIND v1** — mappa completa in `_analisi/MAPPA-MANDATO.md` §2) e la **voce #89** (meccaniche dei verbi: filtrante, cross e rovesciata sul modello del paragone — cantiere della decomposizione del 1° settembre, non assorbito dal programma del mandato). Seguito nominato **#113** (MIRA GUIDATA a due pesi sull'intent-resolution di #88, mandato §9.2, 2 g — tocca il gameplay, fuori perimetro del contorno); **seguito #115** (correzione di revisione del compito 6: numerato invece che lasciato in prosa) per i due residui dall'anello del fiato (compito 5, non bloccanti): la freccia di direzione può coprire ~53° di lime nella zona accesa; la leggibilità della quota intermedia non è misurata oltre i due estremi. **Seguiti aperti, nessuno bloccante**: **#97, #98, #99, #101, #102, #103, #104, #105, #106, #108/#110, #109, #113, #114, #115** (**#96 CHIUSO** dalla voce #107, **#111 CHIUSO** dalla stessa voce) |

## La giornata del 18 settembre, seconda parte

| | cosa |
|---|---|
| 1 | **#107 CANTIERE CHIUSO** (le regole a leva corta, prima voce dell'onda A del mandato, quattro compiti dal merge-base `cabf7e4`). Il rigore legge l'area vera invece della fascia fissa `zonaCalda` (divergenza pura nella banda-y, per costruzione a ogni taglia); il portiere rifiuta il retropassaggio di un compagno (`b.toccoPiede`, 17 siti censiti + guardia, respinta-da-corpo — divergenza dichiarata 3/20-7/20-2/20 per taglia); il vantaggio esiste (finestra `VANT_T=2,5s` da moto libero, fischio ritardato dal punto salvato, cartellino in differita) con la CONTABILITÀ ARBITRALE rettificata: **~1 finestra/partita**, sfumato ~65%, **pieno 26-27%**, morti-lampo **30%→0%** (taratura F1), il sentinel curato **5%→0%** (W1, chiusura arbitrale), il cartellino che non si perde più (micro-coda), falli **~1,0/partita**. Banco `_q-regole.js` da 4 a **13/13** prove nei quattro compiti |
| 2 | **#96 CANCELLO DI PUBBLICAZIONE: CHIUSA.** Il nastro delle sfide porta `MOTORE_V` (nuova costante, nasce a 1 perché #87 e #107 hanno già cambiato il motore prima che esistesse) in un campo di testa del serializzato; `Sfida.guarda` confronta la versione PRIMA di `startMatch` e, se non combacia (o manca: nastri di prima di oggi = versione 0), chiude con la CAUSA VERA invece dell'accusa sbagliata al profilo cresciuto — zero penalità: nessuna partita si avvia, nessun punto, nessun invio al server. Prova NASTRO-VERSIONE (tredicesima di `_q-regole.js`), nata rossa 12/13 sulla base pre-cura. **L'APK SI SBLOCCA** |
| 3 | **SEGUITO #108 nato** (dalla chiusura arbitrale del compito 3, portato qui in MANUALE/PUNTO come richiesto): LA TRAPPOLA DEL BANCO CONGELATO — `setCpuVsCpu(true)` chiamato prima di `startMatch(...)` viene annullato in silenzio da `G.cpu` scritto dentro `startMatch`, e la squadra 0 resta un "umano" immobile invece di una CPU vera. **RETTIFICA (onda di correzione della revisione finale, riga 6 sotto): il censimento vero è 25 strumenti**, non dieci — COMPRESI `_q-regole.js` (15 siti a HEAD — erano 11 a `b837824`: le prove nuove di quest'onda ne hanno aggiunti 4) e `_q-battute.js` (5 siti), entrambi IN BATTERIA (`conta:true`), non solo banchi diagnostici isolati (`_c3-sorteggi.js`/`_crit8-*`/`_crit10-*` erano solo gli esempi citati, mai il conto intero). I loro confronti due-versioni RESTANO validi (simmetrici), ma nessuno di quegli scenari era mai stato CPU-CPU vero, e applicare la cura oggi farebbe cadere la PROVA 1 di `_q-regole.js` (RESA ROBUSTA dalla riga 6, I4b). Proposta di cura lato gioco (`setCpuVsCpu` a prova d'ordine) con la nota sulla comparabilità storica — **decisione del committente**, non applicata; seguito raffinato in **#110** (riga 6). La LEZIONE: un banco che congela una squadra misura il banco, non il gioco (vedi «Le regole pagate» sotto) |
| 4 | **RESTANO, in ordine**: il prossimo dell'**onda A** sono gli **spiccioli UX** (tutorial, anello del fiato, aria-label, cursore vibrazione, ~3 g) — ultima voce dell'onda A, che con #107 chiuso è quasi tutta incassata. Poi l'**onda B**: il registro dei fatti (P0, prerequisito) e **MIND v1** (mappa completa in `_analisi/MAPPA-MANDATO.md` §2). **Resta la voce #89** (meccaniche dei verbi, cantiere della decomposizione del 1° settembre, non assorbito dal programma). **Seguiti aperti, nessuno bloccante**: **#97, #98, #99, #101, #102, #103, #104, #105, #106, #108/#110, #109** (**#96 CHIUSO** dalla riga 2 sopra, **#111 CHIUSO** dalla riga 6 sotto) |
| 5 | **RETTIFICA — correzione di revisione del compito 4 (voce #107), stessa giornata**: il ri-verdetto ha trovato **due affermazioni false** nel rapporto del compito 4 — «27 cancelli che contano» e «`proporzioni` esce BANCO, pre-esistente e non una regressione». Colpevole: il BANCO, non il gioco. Lo scenario PUGNI di `strumenti/_q-proporzioni.js` teletrasportava il pallone 4000 volte senza mai azzerare `b.lastTouch`/`b.toccoPiede`, restati il tocco di un compagno del calcio d'inizio; la guardia del retropassaggio (giusta, compito 2) rifiutava quindi le mani per sempre — 0/4000. Nata in **81d8c59 (compito 2 di QUESTO cantiere)**, non pre-esistente: invisibile perché la batteria intera non era stata rilanciata fra il compito 2 e il compito 4. Cura nel banco (arma `b.lastTouch` su un avversario prima di ogni tentativo): **30/30 verde su HEAD e su `81d8c59`**, il gioco non era mai stato colpevole. Numero vero della batteria: **28 cancelli che contano, tutti verdi** (2+11+8+7), non 27. Zero `dado()` toccati (`_c3-sorteggi.js`, base compito 4 contro HEAD: 0/60). Lezione 21 in «Le regole pagate» sotto. Verbale in `MANUALE.md` §A registro, voce #107 |
| 6 | **ONDA DI CORREZIONE DELLA REVISIONE FINALE su #107** (18 settembre, un commit): **C1 CRITICO curato** — il raggio di respinta del retropassaggio (`tentaPresa`) restava dentro il cerchio della raccolta generica a taglia 11 (`P_R+B_R=7,5` contro `KICK_R*0.8=20,8`: il retropassaggio non mordeva mai a quella taglia; a 5/7 il margine era 0,2 unità, un rasoio); cura `Math.max((P_R+B_R)*dist_, KICK_R*0.8+0.5)`. **I2**: `startMatch` non azzerava `G.vantaggio` — un sentinel col cartellino pendente sopravviveva a una partita nuova e ammoniva un innocente al kickoff; curato (`G.vantaggio=null`). **I3**: `setScene` scaricava il cartellino anche con una finestra VIVA — la palla ferma in finestra dava un 5,1% "silenzioso" (nessun fischio, nessuna punizione); curato estraendo `eseguiSfumato()` e facendola richiamare anche da `pallaFuori()` PRIMA della rimessa — il fallo originario vince sulla rimessa, come la regola vera; questo chiude anche il seguito **#111** (mai rimasto aperto). **I4**: censimento vero di #108 corretto a **25 strumenti** (riga 3 sopra), e la PROVA 1 di `_q-regole.js` riscritta per non dipendere più da quell'artefatto (I4b: "scena attraversata", non "stato finale") — seguito raffinato in **#109/#110** (riga 4). **I5**: commento nuovo, il vantaggio si apre ANCHE in area (nessun codice cambiato, misurato ~0/21 casi PIENI in area). Banco `_q-regole.js` **13→16 prove** (CARD-NON-ATTRAVERSA, PALLA-FUORI-IN-FINESTRA, VANTAGGIO-IN-AREA), tutte verdi; a taglia 11 le prove 3/7 diventano verdi (C1), e SOLA la prova 6 resta rossa per un artefatto di SCALA nel banco stesso (dichiarato nel commento del file, non curato: fuori perimetro) — **RETTIFICA (ri-verdetto, micro-onda finale, 18 settembre 2026)**: questa riga diceva prima «6/8 restano rosse» a taglia 11, misurato falso due volte a seme fisso — la prova 8 è verde a quella taglia, resta rossa solo a taglia 7 (dove restano rosse sia 6 sia 8, stesso artefatto). Cancelli: `_q-regole` 16/16 (`--taglia 5`), **15/16** (`--taglia 11`, sola prova 6) e 14/16 (`--taglia 7`, prove 6/8); `_q-determinismo --partite 4` 13/13; `_q-battute` 11/11; `_q-replay` 10/10; `_q-proporzioni` 30/30; `_q-precedenza` 9/9; sorteggi dal `b837824` DIVERGONO per costruzione (7/60: taglia 5 3/20, taglia 7 2/20, taglia 11 2/20); batteria in 4 spezzoni **28 cancelli che contano, tutti verdi**. Verbale in `MANUALE.md` §A registro, voce #107 |

## La giornata del 17-18 settembre, in breve

| | cosa |
|---|---|
| 1 | **IL MANDATO È AGLI ATTI.** Il committente ha consegnato un mandato di miglioramento scritto come lo spec di un gioco 3D ambizioso (`_analisi/MANDATO-STADIUM-ROAR.md`) e ha chiarito: serve a MIGLIORARE CALCETTO, non ad aprire un gioco nuovo. Otto mappatori hanno tradotto ogni area del mandato nell'idioma di casa in `_analisi/MAPPA-MANDATO.md`: cosa c'è già (molto: determinismo a seme, identità come espressione, stati emotivi cel/mesto, IA a macchina di stati...), cosa manca, proposte con giornate stimate, esclusioni motivate. Il cantiere #87 già in corso si è rivelato ESATTAMENTE la §6.6 del mandato (rimesse/angoli/rinvii). **PROGRAMMA APPROVATO dal committente (17/9, notte)**: onda **A** (incassare subito, ~6-8 g: #87, regole a leva corta, versione motore nel nastro, spiccioli UX) → **B** (il pilastro nuovo, ~12 g: registro dei fatti + MIND v1, il modello emotivo/psicologico) → **C** (qualità permanente, ~7 g) → **D** (competizione, ~8-12 g), più **onda E** (live 1v1, lockstep-prima) in coda, con progetto d'architettura dedicato quando si aprirà |
| 2 | **#87 CANTIERE CHIUSO** (rimesse laterali, calci d'angolo, rinvio dal fondo). Cinque compiti più il verbale, dal merge-base `7ed570a` fino alla coda della revisione finale (riga 5 sotto): un interruttore solo (`SAVE.sponde`, gabbia di serie a 5/7, campo vero a scelta, **obbligatorio a 11**), scena `battuta` con fermo breve (~0,8-1,5 s) e finestra viva (hold 3 s), battitore comandato coi verbi di casa (PASSA/CROSS/FILTRANTE, TIRA spento), clip su `p.rimT` fino in moviola. Banco dedicato `_q-battute.js` nato 2/7, arrivato **11/11**, ora IN BATTERIA. GABBIA **0/40** dal merge-base (identica al bit a 5/7); a 11 **18/20 DIVERGE dichiarato** (campo vero obbligatorio, causa unica). Giocabilità (`_eventi.js` contro il pre-ramo): 11 e 5-campo-vero **entrambi 88,9%** di momenti-da-porta/minuto (soglia ≥80%), 0-0 al 5-10% (soglia ≤33%) — nessuna manopola da tarare. Batteria intera verde, **27 cancelli che contano** in 4 spezzoni. **Regressione trovata E curata dalla batteria stessa**: la riga SPONDE (compito 1) aveva riaperto il difetto storico del 28 agosto su `#btnCambiaCampo` (dietro la barra fissa della schermata GIOCA, 5/722 bersagli in `tocco.js`) mangiando gli ultimi 2 px di margine che quella toppa aveva lasciato; curata SOLO CSS (`_t-gioca-sponde-fit.js`, zero `dado()`, bersaglio del pollice 42→37 px, dichiarato in stile edizioni nel commento del gioco). Verbale in `MANUALE.md` §A registro, voce #87 |
| 3 | **Seguiti nuovi**: **#102** (piazzati evoluti — portiere che sale sul corner disperato, pressione per fase, dal paragone MINIERA scavo 7 — più la regia panoramica dell'angolo a 11); **#103** (la coda dell'angolo: gol olimpico, statistica corner in lavagnetta); **#104** (la battuta è scena di partita anche per il disegno: sei liste + `forceWinMatch` in una sola `fermoDiPartita(s)`, soglia FERMO BREVE proporzionale a `duraBattuta()`, l'HUD non deve lampeggiare a ogni rimessa); **#105** (le sponde viaggiano col nastro delle sfide, per sfide a campo vero a 5/7); **#106** (`tocco.js` misura la raggiungibilità ma non la TAGLIA del bersaglio — il 37px del compito 5 resta senza pavimento misurato). **Nota corretta (revisione finale)**: la voce **#96** (cancello di pubblicazione) copre la VERSIONE DEL MOTORE — i nastri di ieri non si rigiocano a 11/campo vero perché il motore è cambiato; il caso IMPOSTAZIONE-PER-DISPOSITIVO (due telefoni sulla stessa versione ma con SPONDE diversa in locale) è un difetto diverso, **C1**, ora curato forzando LA GABBIA nelle sfide a 5/7 (seguito #105 per quando si vorranno sfide a campo vero anche lì) |
| 4 | **RESTANO, in ordine**: il prossimo dell'**onda A** è **le regole a leva corta** (§6.7/§6.5 del mandato: il rigore legge l'area vera invece della fascia `zonaCalda`, ½ g; il portiere non prende più il retropassaggio con le mani, 1 g; il vantaggio — non ogni fallo ferma il gioco, 2 g) **+ la versione del motore nel nastro** (1 g, chiude la voce #96 e sblocca il prossimo APK) **+ gli spiccioli UX** (tutorial, anello del fiato, aria-label, cursore vibrazione, ~3 g). Poi l'**onda B**: il registro dei fatti (P0, prerequisito) e **MIND v1** (due umori e una spinta, il canale `manopole(t,p)`, i due canali d'occhio, il banco `_q-umore.js` nato rosso) — mappa completa in `_analisi/MAPPA-MANDATO.md` §2. **Resta anche la voce #89** (meccaniche dei verbi: filtrante, cross e rovesciata sul modello del paragone) — un cantiere della decomposizione del committente del 1° settembre, non assorbito dal programma del mandato (l'eventuale assorbimento lo annota a parte il controllore). **Seguiti aperti, nessuno bloccante**: **#96, #97, #98, #99, #101, #102, #103, #104, #105, #106** (RETTIFICA, sezione "18 settembre, seconda parte" sopra: **#96 CHIUSA** dalla voce #107, **#108 nato**) |
| 5 | **ONDA DI CORREZIONE DELLA REVISIONE FINALE su #87** (18 settembre): **C1 CRITICO curato** — `startMatch` leggeva sempre `SAVE.sponde` del dispositivo, mai `opts.sponde`; a 5/7 due telefoni con SPONDE diversa rigiocavano la stessa sfida su un motore diverso, e `chiudiSfida` spiegava lo scarto solo col profilo cambiato. Cura: `startMatch` onora `opts.sponde` quando presente, e le due partenze di sfida passano `sponde:'gabbia'` — le sfide a 5/7 giocano sempre la gabbia, per costruzione identica su ogni telefono (a 11 restano già allineate, il campo vero è obbligatorio). **I5 Importante curato (a metà — vedi la coda sotto)** — le tre pose di battuta (`posaBattuta`/`posaBattutaAngolo`/`posaBattutaRinvio`) lasciavano `G.battuta` pendente per sempre quando non trovavano un battitore (rose azzerate); cura centrale in `pallaFuori`: palla libera, `G.battuta=null`. Testo: 3 rilievi Importanti e cinque minori chiusi in `MANUALE.md`/`strumenti/_q-battute.js`/`strumenti/_t-gioca-sponde-fit.js` (numeri e liste disallineati fra file, nessun cambio di comportamento). Via attrezzo a àncore `strumenti/_t-sfide-sponde.js` — **due postille del ri-verdetto**: (a) l'attrezzo non è una ricetta completa di quel commit, una riga di solo testo (M3) resta un edit a mano fuori dall'attrezzo; (b) la verifica byte-per-byte prova che l'attrezzo riproduce l'edit già fatto, non che l'edit sia nato ancorato — garanzia più debole, annotata come tale. Cancelli: `_q-battute` 11/11, `_q-determinismo --partite 4` 13/13, `_c3-sorteggi` (fw-base contro HEAD, taglie 5/7/11) 0/60, `tocco.js` 722/722, `_q-precedenza` 9/9 |
| 6 | **CODA DELLA REVISIONE FINALE su #87** (18 settembre, un commit in più): il ri-verdetto ha bocciato la cura I5 della riga sopra — **bloccante**. La guardia liberava l'owner ma non riportava la palla dentro la banda: a campo vero `ballWalls()` gira ogni fotogramma sulla fisica libera del pallone, quindi il varco si ripresentava al fotogramma dopo, `pallaFuori()` veniva richiamata, la guardia riliberava di nuovo — scena `play`/`battuta` alternata a fotogrammi alterni, misurato **169 ingressi in scena battuta su 400 fotogrammi prima della cura, 1 dopo** (sonda dedicata, rosa di movimento tutta fuori). Il commento era anche falso («resta per il fermo breve»): `duraBattuta()` torna 0 con `G.battuta` nullo, e `setScene('play')` scatta subito — nessun fermo da conservare. Cura: un `clamp` riporta la palla dentro la banda (come `ballOverBar` quando "deep" è null, stesso principio non la stessa riga) PRIMA di azzerare; commento riscritto con la storia vera. Rilievo minore in coda: `_q-battute.js` PROVA 1 guadagna la quarta sotto-condizione che misura la via vera delle sfide — `startMatch(1,1,{size:11,sponde:'gabbia'})` → `campoVero===true`. Via attrezzo `strumenti/_t-battuta-ripiego.js` (1 ancoraggio). Cancelli: `_q-battute` 11/11, `_q-determinismo --partite 4` 13/13, `_c3-sorteggi` (coda precedente contro HEAD, taglie 5/7/11) 0/60, `_q-precedenza` 9/9, sonda 169→1 |

## La giornata del 7 settembre, in breve

| | cosa |
|---|---|
| 1 | **#86 CANTIERE CHIUSO.** In sette compiti, un solo commit ciascuno: il campo, il gesso, la porta e — a 11 — i corpi entrano nella scala dei campi veri (IFAB/FIFA Futsal/UISP), misurati contro `_analisi/MISURE-UFFICIALI.md` da un banco nato ROSSO 4/24 il 6 settembre e arrivato a **27/27**. Porta nel tetto ±20% a tutte le taglie, area disegnata=applicata dalla stessa costante, forma e corpi dell'11 nel tetto ±15% (diametro giocatore +11,3%, pallone +3,8%). Batteria intera verde (25 cancelli che contano, incluso `proporzioni` da oggi in batteria), sorteggi identici al bit dove il piano lo promette: `_q-determinismo` 10/10; il due-versioni COMPLESSIVO del ramo DIVERGE per costruzione (58/60 partite, 542.275→601.224 sorteggi) — dichiarato, non nascosto; le tre corse SEPARATE per taglia danno 60/60, e lo scarto della corsa combinata è il non-determinismo PRE-esistente a taglia 7 e 11 (voce #98, fuori perimetro), non una cura parziale. Verbale in `MANUALE.md` §A registro, voce #86 |
| 2 | **#85 LA MOVIOLA FLUIDA: CANTIERE CHIUSO** (con le voci #68 e #98 a bordo). Cinque compiti in commit `3c560d6..3372268` — dal banco che nasce rosso al verbale, con le due code di revisione dei compiti 2 e 4 (`d48058c`, `6beea19`); dopo diagnosi `15410bf` e piano `90fe5b9`: i cronometri dei gesti (kickT/kickB/charge/slide/dive/rove...) smettono di restare congelati nel replay mentre il corpo avanza — SCATTO **5→0** fotogrammi congelati su 16 transizioni attive, seme 20260907 — e i cinque campi di posa mancanti (contrasto/presaT/gkManiT/rinvT/recover) entrano nel campione e nel disegno (+19.800 numeri a taglia 11, ~396 kB di RAM per eccesso, mai serializzati). La cura profonda di `Touch5` in `startMatch` CHIUDE LA VOCE #68 (la prova E di `_q-replay.js` torna verde, mai più NULLA — la bisezione aveva smentito il sospetto iniziale `stick.ox/oy`: le cause vere erano `stick.active/id/dx/dy/hist` e il verbo a tenuta orfano `atti/btnTouch`) e MISURA la voce #98: a 7/11 resta **8/10 identico prima e dopo la cura** (il seme 20260803 diverge già fra due pagine fresche in CPU-contro-CPU, zero Touch5) — **#98 si RIDIMENSIONA**, non si chiude: è un difetto vero del motore, indipendente dal tocco. Batteria intera verde (26 cancelli che contano, incluso il nuovo `replay` da oggi con dentro le prove SCATTO/CAMPI/E). Sorteggi identici al bit: `_q-determinismo` 10/10; due-versioni COMPLESSIVO (base `3bced51`) **0 partite divergenti su 60** (601.370=601.370) — il primo ramo di questa voce che chiude senza dichiararne. Nota onesta sull'occhio: la cura arriva al rig ma nell'episodio campionato (fine-calcio, ampiezza piccola) la differenza a occhio è impercettibile (diff pixel quasi pari, 23,8% contro 23,77%); la verifica su un gesto ampio o in gioco vero resta aperta. Verbale in `MANUALE.md` §A registro, voce #85 |
| 3 | **#100 CURATA** (decisione del committente, 7 settembre: «allungare il rinvio in proporzione così da renderlo più realistico al calcio vero»). Il rinvio a pugno del portiere, che con l'area vera di #86 poteva cadere dentro l'area a 7 e a 11 (242 contro 268 e 361), torna sempre fuori area a ogni taglia: le due componenti orizzontali del pugno scalano per `GK_PUGNO_SCALA = VERNICE.areaProf/VERNICI[5].areaProf` (`strumenti/_t-rinvio-scala.js`), il tempo di volo resta invariato. A 5 fattore 1, **taglia identica al bit** (242-590 invariato); a 7 (1,549) **375-914**; a 11 (2,087) **505-1231** unità (23-56 m). Tre prove nuove per taglia in `strumenti/_q-proporzioni.js` (27→30, tutte verdi), RED dimostrato sul gioco pre-cura (`fuori/base-100.html`: rosso a 7 e 11, verde a 5) e GREEN sul gioco curato. Due-versioni (`_c3-sorteggi`): 0/20 identiche a 5, DIVERGE 1/20 a 7 e a 11 (l'evento è raro in CPU contro CPU: il pugno scatta solo dopo una presa forte). Verbale in `MANUALE.md` §A registro, voce #86 |
| 4 | **Restano due cantieri della stessa decomposizione** (committente, 1° settembre): **rimesse laterali e calci d'angolo** (#87), **meccaniche dei verbi** — filtrante, cross e rovesciata sul modello del paragone (#89) |
| 5 | **Cancelli/seguiti aperti, nessuno bloccante**: **#96** (pubblicazione, già cancello: il prossimo APK aspetta la versione del motore nel nastro delle sfide — copre ora anche i rami #86 e #85); **#97** (raccomandazioni non bloccanti della revisione di #88: guardia sul rinnovo, bersaglio della tenuta con prova K allargata); **#98** (RIDIMENSIONATA dal ramo #85, non chiusa: `_q-determinismo --taglia 7/11` resta 8/10 identico prima e dopo la cura Touch5 — il seme 20260803 diverge già fra due pagine fresche in CPU-contro-CPU, zero Touch5: difetto vero del motore, dossier in `_analisi/PROVA-E-DIAGNOSI.md`); **#99** (la batteria è cieca a taglia 11: portare in batteria almeno un cancello che giri a 11 e ricostruire lo strumento di spazzata che la decisione 1 di #86 assumeva esistente — in parte bloccata da #98). **La voce #68 esce dai misteri: CHIUSA** dal ramo #85 (cura `Touch5.azzera()` in `startMatch`, voce #85 sopra) |

## La giornata del 6 settembre, in breve

| | cosa |
|---|---|
| 1 | **#88 LA PULSANTIERA: CANTIERE CHIUSO.** In nove compiti, un solo commit ciascuno: la faccia dei dischi dipende dal possesso (`squadraDelPallone()`) invece che da una soglia geometrica, le celle si spengono invece di travestirsi da un altro verbo, il comando segue il destinatario dichiarato del passaggio, il raddoppio è una tenuta. Le sei soglie del progetto approvato tutte VERDI su `strumenti/_q-volo.js` (11/11): TIRA nel volo del cross 9%→100%, faccia bugiarda in 6 s d'inseguimento 3→0, comando al destinatario 38→17 fotogrammi (0,63 s→0,28 s), volée 0→1, celle accese che rifiutano l'atto 147→0 (e la direzione opposta, prova G, 0/361), furti a dita vere invariati (19/20 e 17/20 prima, 17/20 e 17/20 dopo). Batteria intera verde (24 cancelli che contano), sorteggi identici al bit: `_q-determinismo` 10/10, `_c3-sorteggi` 0 partite divergenti su 60, `_crit10-sorteggi` verde. Verbale completo in `MANUALE.md` § A registro, voce #88 |
| 2 | **Restano quattro cantieri della stessa decomposizione** (decisa dal committente il 1° settembre, `docs/superpowers/specs/2026-09-01-pulsantiera-contesto-design.md` §7), nessuno ancora iniziato: **vernice del campo** in scala ufficiale (#86), **residuo della moviola** — i cronometri dei gesti non interpolati (#85), **rimesse laterali e calci d'angolo** (#87), **meccaniche dei verbi** — filtrante, cross e rovesciata rifatti sul modello del paragone (#89) |
| 3 | **#96 CANCELLO DI PUBBLICAZIONE** (rischio 6 del §6 dello spec): la fusione di #88 può avvenire ora, ma il **prossimo APK non deve partire** finché il nastro delle sfide non porta la versione del motore, o il messaggio di `chiudiSfida` non dichiara entrambe le cause possibili (profilo cambiato **oppure** motore cambiato) — le sfide si rigiocano da {seme, taglia, gol, nastro}, quindi i nastri registrati col motore di ieri non si riproducono più, e oggi quel messaggio attribuisce lo scarto solo al profilo cambiato |

## La giornata del 1 settembre, in breve

| | cosa |
|---|---|
| 1 | **LA MINIERA** (`_analisi/MINIERA-FCM.md`): su mandato del committente («non reinventare la ruota»), sei minatori hanno estratto le soluzioni del concorrente come fatti liberi — divisioni, formato ASSALTO, ritmo del ritorno, comandi, gioco aereo, schermi — con righe di prova e gradi. **Il percorso scende da ~26-27 a ~19-20 giornate.** Quattro rettifiche già scritte nella mappa delle differenze |
| 2 | **#82 CURATA e verificata**: la porta del ri-armo sul cambio di lato (`_t-isteresi-disco.js`). Sonda 11/20 → ~18/20 con zero falliti per sfarfallio; `giocata` 20/20; `_q-riarmo` 7/7 ×3; duelli a seme fisso identici al bit prima/dopo. Verbale: `PROGETTO-ISTERESI-DISCO.md` §8 |
| 3 | **Sei banchi accusavano l'innocente**, cinque per rotture PREESISTENTI: giocata (scivolata accreditata all'uomo sbagliato dopo l'autoswitch), precedenza (taglio delle costanti + finto di Reg), riarmo (campionatore a blocchi, garante del raddoppio, perno del portatore). Tutti riparati con verbale |
| 4 | **#72 pronta a partire**: `_t-aereo.js` scritto e provato a vuoto (8 ancoraggi, stadi A+B); fotografia PRIMA presa (`fuori/aereo-prima-*.json`, cross 0,3/0,0/0,0) |
| 5 | La direttiva del committente è in memoria permanente (`minare-il-paragone`): prima di progettare, scavare il pacchetto di paragone |

## Il prossimo passo, in ordine

1. ~~La #72~~ FATTA in sei stadi (1 settembre, sera): cross 0,3/0,0/0,0 →
   1,0/1,0/0,2, batteria verde; il residuo (bersaglio [2,6], «con uomo» 0)
   è la voce #84 — l'appuntamento del cross, progetto a parte.
2. ~~Onda 1~~ I TRE CONTENUTI SONO DENTRO (1 settembre, notte), in
   ordine 2→1→3 come da progetto: l'ABBANDONO vale 0-3 a tavolino in
   torneo e stagione (banco 14/14); le DIVISIONI a 3 fasce × 3 con premi
   a formula, pavimento di fascia e schermata in bacheca (banco 18/18,
   _q-meta col modello aggiornato 80/80); RECORD con la data + la
   seconda mensola da 15 a 24 trofei (banco 12/12). I tre banchi sono in
   batteria (tutti.js). RESTA dell'onda: la misura del tasso
   monete/partita (§6.8 — prerequisito della taratura FINE dei premi),
   la giuria dei dieci minuti sul rischio-frustrazione, e il collaudo a
   mano su telefono.
3. Taccuino del campetto (~2,5 g, ora che il contenuto 3 c'è) e ASSALTO
   (~6 g, onda 2 — la scala ha un posto dove versare).
4. Play Console: bloccata alla creazione dell'account (2FA + 25 € +
   identità sono solo del committente); AAB 1402079 pronto.

**Ramo:** `main`. **Il gioco adesso:** ~2,36 MB, `targetSdk` **36** (da oggi
Play rifiuta le app nuove sotto Android 16 — verificato e schivato il giorno
stesso). **Cancelli:** batteria verde, `verifica.py` 48/48.

## La giornata del 29-31 agosto, in breve

| | cosa |
|---|---|
| 1 | **Il MANUALE** (`MANUALE.md`): 269 voci censite dal codice da undici lettori, con appendice di **98 incoerenze** — 10 curate lo stesso giorno |
| 2 | **La STAGIONE moriva alla seconda apertura** (TypeError) e per quel crash RISULTATI DELL'ULTIMA GIORNATA non era mai esistito: curata |
| 3 | **Il duello dal dischetto era un fermo-immagine** (0 campioni su 2408 cambiati in 700 ms): ora ha un orologio di solo disegno (22/2408) |
| 4 | **Sette «TORNA AL MENU» bugiardi**, «quattro dischi» che erano cinque, «Z contrasta» che era la scivolata, trofei e podio disonesti nei testi: parole vere |
| 5 | **I cinque cronometri** che sopravvivevano a `startMatch` (voce #65): azzerati, distribuzioni identiche al byte su 100+100 partite |
| 6 | **`giocata.js`** sbagliava 2-3 volte su 10 in entrambe le versioni: tre difetti del banco curati (due orologi, comando dal rilascio, portatore ripuntato) — 70/70 su dieci corse, e in pausa fallisce ancora come deve |
| 7 | **`folla.js`** divideva per un fondo a fase fissa: mediato su 8 fasi, 9,6% identico su entrambe le versioni |
| 8 | **Pacchetto Play**: `SCHEDA-STORE.md` coi testi pronti, privacy in `rete/public/privacy.html` (da deployare), icona 512, fotografie in `fuori/store/`, `PUBBLICARE.md` coi passi del giorno |
| 9 | Le lezioni di metodo sono in memoria persistente (`concludere-solo-da-misure`): oggi hanno pagato quattro volte, due contro di me |

## La giornata del 26-27 agosto, in otto righe

| | cosa | misura |
|---|---|---|
| 1 | **L'11 contro 11 non aveva un difetto d'intelligenza: aveva un difetto di OROLOGIO.** Sei cure d'IA bocciate in fila con lo stesso profilo erano l'indizio. Novanta secondi su un campo largo il doppio sono mezza partita | 0-0 dal **50% al 22%** (100 partite); i gol *al minuto* restano identici |
| 2 | Il **tiro non aspetta più il difensore**: la carica durava 0,30-0,46 s anche a contatto, e il marcatore arrivava durante | precisione VERA 7 → 11%, parate 0,38 → 0,49 |
| 3 | **Cinque toppe ferme dal 20 agosto** entrano: torneo e stagione a 7 e a 11, la licenza OFL a bordo, il salvataggio che sopravvive, l'audio che tace in tasca, zero nomi di concorrenti | il cancello del meta-gioco: «il chiodo del 5v5 è saltato» |
| 4 | **Le reti vanno a chi le fa** (erano tutte all'uomo di indice 1) | 20 gol su **4** marcatori invece che su 1 |
| 5 | **La lavagnetta dice tutto quello che il saldo incassa** (i premi dei trofei si pagavano fuori lista) | partite col conto storto: 4 su 14 → **0** |
| 6 | **Il passaggio si mira col dito** (L1.4), e la linea di guida lo promette | `_q-linea` 5/5, `_q-l16` 6/6, `giocata` 7/7 |
| 7 | **L'avvio era già a posto**: lo sforo veniva dal banco rallentato 4× | **1861 ms** sul telefono vero contro un tetto di 2000 |
| 8 | **Due strumenti ciechi e uno rumoroso** trovati e curati: il contrasto delle divise (dispersione 0,86 su soglia 3,00), quattro banchi che sceglievano il disco per raggio, `_q-riarmo` che oscilla su due prove | tutti verificati ancora verdi sul gioco di ieri |

---

## Il verdetto, e come si muove

Il committente ha chiesto: **giuria di 26 giudici, il nostro 8-9 e i competitor 3-4.**

| momento | voto | note |
|---|---|---|
| sei appelli piatti | 6,4 | prima dell'interrogatorio |
| dopo 4 onde | 7,1 | luce, figure, leggibilità, scene madri |
| dopo la correttiva | 7,3 | 6 difetti su 10 spariti |
| dopo l'onda 6 | 7,9 | la sera, il campo vissuto, il quadro pieno |
| **dopo la passata del pollice** | **8,2** | il gioco impara a segnare |

Il giudice quantifica **~8,6** con le voci che restano, e mette il 9 dietro due
cose sole: **le pose** e **il centro del quadro**.

Le altre due lenti, lo stesso giorno: **7,3** (chi gioca col pollice, era 6,4) e
**6,2** (l'ancoraggio ai concorrenti). Il gioco vale molto più fotografato che
giocato — ed è la frase che ha riordinato tutto il lavoro:

> «Il gioco vince su tutto quello che si può fotografare e perde su quello che
> si prova col pollice.»

---

## La scoperta che vale più di tutte: in questo gioco non si segnava

Misurato su 50 partite a semi dichiarati con `strumenti/_eventi.js`, che è il
**primo strumento del progetto che misura il gioco invece dell'immagine**:

```
prima:  ZERO gol su azione in 50 partite · 73% di 0-0 al 90' · 67% ai rigori
        11 tiri a partita, 79 palloni vaganti — il caos c'era, mancava la conseguenza
dopo:   1,42 gol a partita · 20% di 0-0 · 18% ai rigori
        parate 1 -> 3 · legni 0,40 -> 0,96 · momenti da porta al minuto 0,91 -> 3,26
```

**La causa era aritmetica.** Con l'attrito `b.vx *= 0.35^dt` un pallone percorre
al massimo `v0/1,0498` unità. Il tiro partiva a 313 u/s → 298 unità di corsa,
contro una distanza mediana di tiro di 326: **non arrivava in porta nemmeno a
campo vuoto**, zero volte su ventisette. E il tiro perfetto mirava al palo con
l'effetto a giro nello stesso verso della mira: 18 arrivi su 19 fuori dallo
specchio, per costruzione. Più due difetti del portiere: armava il tuffo su una
*distanza* invece che su un *tempo*, e mentre si rialzava il suo corpo spariva.

L'equità **migliora**: lo squilibrio fra le due squadre scende da +0,330 a
+0,195 reti, perché la lotteria dei rigori decideva due partite su tre.

---

## Undici strumenti ciechi trovati in due giorni

La regola di casa era «uno strumento che attesta invece di misurare è peggio di
nessuno strumento», e valeva dieci casi. Adesso ventuno. **Quattro delle sei
critiche più dure della giuria erano artefatti dei nostri strumenti.**

| # | strumento | la bugia |
|---|---|---|
| 11 | `prestazione.js` | confronta con un riferimento preso a banco libero: col server di sviluppo del committente acceso segnava +100% dove il costo vero era +11%. Cura: **misura appaiata**, i due file alternati sullo stesso banco nello stesso minuto |
| 12 | `silhouette.js` | chiedeva a un corpo disteso visto di fronte di essere più largo che alto: servirebbe un uomo lungo 1,62 m in orizzontale. 12 combinazioni su 120 irraggiungibili, tetto vero 108 |
| 13 | `folla.js` | sommava forma **e traslazione** con segno opposto: la folla salta di 10 unità su una finestra alta 38, e l'anello di tribuna esce dal bordo alto mentre le braccia entrano dal basso |
| 14 | `silhouette.js` (bis) | dichiarava «8 su 10 nominabili» mentre tre **provini ciechi** con persone diverse ne riconoscevano da zero a due |
| 15 | `scatta.js` (HUD) | forzava il possesso «così la foto non dipende da chi ha la palla»: ha mostrato alla giuria otto fotogrammi in cui i pulsanti dicevano TIRA senza palla |
| 16 | `scatta.js` (fine) | fabbricava il tabellino con `forceGoal`: ha ingannato la giuria **due volte in direzioni opposte** — prima «tre conclusioni in novanta secondi», poi «ogni tiro entra, precisione 100%» |
| 17 | `istantanea.js` (campioni) | **gli otto istanti erano sei.** Pescati uniformi, potevano cadere vicinissimi; e il passo era `max(0, bersaglio − orologio)`, quindi un istante spostato avanti per uscire da una celebrazione faceva cadere il successivo **alle sue spalle**, passo zero, stessa tela rimisurata. Non identica: i due ridisegni del riferimento avevano intanto mosso la camera di due fotogrammi senza aggiornare la trasformazione letta dallo strumento — **è per questo che «nell'istante 8 la palla non c'era»**. Il cronometro stampato tornava indietro (t=39,0 s dopo t=40,1 s) |
| 18 | `istantanea.js` (palla) | l'**atteso** era il numero falso: deduceva il raggio da `B_R` (8) mentre il gioco disegna a `B_R × B_DIS = 10,72` e lo dichiara in `__test.pallaRaggio()`, hook scritto apposta e mai letto. I 48 px erano la palla. Ma il sospetto era fondato per costruzione: il buco perdonato valeva 3,2 unità dove il gioco ne lascia 2,6 |
| 19 | `istantanea.js` (denominatore) | «erba vuota» misurata sul **quadro intero**, dove sotto l'interfaccia erba non ce n'è né può essercene. Corretto sull'area giocabile: **il tetto si alza, non si abbassa** — lo stesso quadro passa da 29,2% a 33,4%, e il caso peggiore da 41,7% a 47,8%, due punti dal tetto dove ne aveva otto |
| 20 | `avvio.js` (colonna «analisi») | la sua intestazione dice: «ANALISI = navigationStart → DOMContentLoaded. È il costo di **leggere e compilare** il file: la fase che cresce col peso». Ma DOMContentLoaded non scatta a compilazione finita: scatta quando ogni script in linea ha finito di **eseguire**. Misurato spezzando quel numero in quattro (`strumenti/_sonda-avvio.js`): leggere e compilare sono il **15,6%**. Il tetto in kilobyte, che ha governato il progetto per settimane, sorvegliava un sesto della stanza |
| 22 | `istantanea.js` (misura 7) | **il cancello nato da un rilievo vero, che non misura il rilievo.** Il tetto è irraggiungibile per aritmetica: una figura rompe 1,98 celle, quindi stare sotto il 40% chiede **9,7 figure in un riquadro di 273×123 unità** dove il 5v5 ne ha dieci in tutto il campo. Non conta i corpi: una manovra che porta i corpi al centro da 12 a 21 lascia il numero **identico al decimale**. E ha un verde falso — il «fotogramma manifesto» al 6,3% è prato deserto che passa perché la tinta del manto sotto i fari è 89° contro una soglia a 90 |
| 21 | `_identita.js` | **la stessa ferita, entrata da una seconda porta.** Nato copiando il campionamento di `istantanea.js`, ne ha ereditato il difetto insieme al resto: istanti 4 e 5 tutti e due al secondo 38 con la stessa coppia peggiore al decimale, 7 e 8 tutti e due al 49. Sei fotogrammi che si dichiaravano otto, e uno «0 su 52 giocatori» che contava due volte tredici uomini. Corretto, il difetto delle divise era **peggiore** di quanto dichiarato (coppia peggiore 5,5 e non 9,3) e il guadagno più grande. **Una ferita chiusa in un file e lasciata aperta in quello accanto è una ferita aperta** |

Più due nati e riparati in giornata: `tutti.js` misurava tempi su un banco ancora
caldo, e l'attrezzo dei nomi dichiarava successo senza aver misurato nulla.

I tre casi di `istantanea.js` hanno una lezione in comune che vale da sola:
**un banco che si guarda allo specchio trova cose che nessun cancello trova.**
Adesso lo strumento misura la propria indipendenza — differenza media assoluta di
luminanza fra ogni coppia di istanti, 94.348 campioni per coppia — e nel banco
c'è un **gemello sporco** permanente: lo stesso fotogramma rimisurato dopo i
ridisegni del riferimento, cioè esattamente la coppia che il banco produceva da
sé. Vale 5,12 contro una soglia di 7,0, e viene riconosciuto. La coppia più
vicina fra istanti veri adesso vale 9,29 con il 35% di quadro cambiato; con il
campionamento vecchio la corsa **fallisce** e dice quali coppie sono la stessa
foto.

**Il metodo che li ha trovati**: il **provino cieco**. Una persona che non
conosce la chiave di risposta e a cui è vietato cercarla. Costa un minuto, e ha
spostato il lavoro tre volte in direzioni che nessuna misura indicava.

---

## Il metodo che va veloce

`strumenti/tutti.js`: tutti i cancelli **insieme**, quattro alla volta.
**357 secondi invece di 960.** Ma il guadagno vero è un altro: eseguendoli
*tutti* ha scoperto in un colpo tre cancelli rossi che nessuno cercava.

Tre cose che rendono onesto quel comando:
- **l'impronta del file prima e dopo**: se cambia, il referto è **NULLO**. Un
  lavoratore che scrive mentre un verificatore misura produce due numeri che
  descrivono file diversi, e nessuno se ne accorge;
- **i cancelli cronometrici girano da soli**, dopo, a campo libero: `giocata` e
  `avvio` misurano tempi e sotto contesa bocciano il gioco per un ritardo che
  non è suo;
- **la misura del carico**: cronometra un lavoro aritmetico fisso e lo confronta
  col più veloce mai visto. Sopra 1,5× declassa il verdetto da condanna a
  sospetto.

E la forma di lavoro che ha funzionato: **diagnosi in parallelo, scrittura in
fila.** N specialisti in sola lettura, ognuno su un difetto, che consegnano una
toppa cerca/sostituisci provata su una copia fuori dal repo e che *si rifiuta di
scrivere* se un ancoraggio non si trova esattamente una volta. Poi le applico in
fila. Prima: tre ore per giro.

---

## Cosa resta per il 9, in ordine di resa (parole del giudice)

| | | decimi |
|---|---|---|
| 1 | **Le pose che dicono il verbo — ed è UN VERBO SOLO.** Misurato su 13.560 fotogrammi di verbo: `tira` 3,3% di fotogrammi illeggibili, `scivola` 6,1%, `esulta` 39,1% (ma vive sulla quota, 72-79 px), **`para` 63,3%** con 8,1 px di asse sagittale. La causa in una riga: **il portiere si tuffa lungo la bocca della porta, la porta è verticale sullo schermo, e la vista non ruota mai.** Ed è **la posa**, non la camera: la clip `tuffo` ha dz 1,382 contro dy 0,605, mentre `cielo` e `pugno` hanno dy 1,56-1,59 e si leggono sempre. Le tre strade già chiuse con numeri: il beccheggio (σ2 non contiene l'elevazione; e a 16° la figura si stringe del 22,5% e il tuffo si accorcia al 43-54%), l'ombra (due provini ciechi: 1 su 10, poi 0 su 10; zero pixel sopra 3:1 contro l'erba), e l'ingrandimento (il rapporto gesto/tratto è **invariante di scala**, verificato a k da 0,5 a 3). In lavorazione: riscrivere il tuffo sull'asse della quota | 1,5 |
| 2 | ~~L'11 contro 11 che non tira mai~~ **CHIUSO in due tempi.** Primo tempo (23 ago): l'attrito non sapeva quanto è grande il campo — `k = −ln(0,35) = 1,0498` per unità percorsa dà corsa massima 819 unità su un campo lungo 2300, cioè il 36% contro il 71% del 5v5; e i «sette tiri a partita» erano rigori. Secondo tempo (26 ago): **non era l'intelligenza, era il cronometro.** Sei cure di IA bocciate una dopo l'altra con lo stesso profilo (i tiri calano, i gol seguono) erano l'indizio: 90 secondi su un campo largo il doppio sono metà partita, perché le gambe restano 168 u/s e l'attraversata costa 13,7 s contro 6,8. Col gioco **spedito**, cambiando **solo** la durata, lo 0-0 passa dal 50% al 25% — e i gol al minuto restano identici (0,43 contro 0,42). Nasce `durataPartita()`: 90 · 126 · 180 s, cioè i tre valori che il menu già offriva. Cancello: 0-0 al **23%** contro la soglia 33 | fatto |
| 3 | **Il centro del quadro**: cancello nuovo, rosso in 5 istanti su 8 (43-75% contro un tetto del 40%) | 0,4 |
| 4 | ~~**L'avvio**~~ **CHIUSO il 26 agosto sul TELEFONO VERO: 1861 ms contro un tetto di 2000, 7 avvii buoni su 7, dispersione 14,3% (OnePlus A6003).** Lo sforo di 870 ms era misurato sul banco a 4×, che è un telefono che non esiste: `avvio-telefono.js` misura da icona premuta a pallone toccabile sul dispositivo, e passa. Resta valida la cura già applicata (il manto si cuoceva due volte, la seconda da `document.fonts.ready` dentro l'avvio della partita) | fatto |
| 5 | **HUD e bussola** che non mangino mai il protagonista (restano due casi nell'11v11) | 0,2 |
| 6 | La coda: la palla al petto invece che ai piedi, la palla che si separa dallo stato durante il tiro, l'etichetta «AVVERSARIO IN 2 9"» spezzata | 0,3 |

E una cosa fuori elenco, che il giudice chiama il traguardo vero: **il fermo
immagine casuale dell'azione che valga quanto la lavagnetta di fine partita.**

---

## I cancelli (ultima esecuzione completa)

```
collaudo 36/36 · misura 7/7 · senza-rete 6/6 · equità 4/4 (0,000 al bit)
giocata 8/8 (da solo: è cronometrico) · folla 4/4 · seme · gabbia · volti · silhouette
avvio: ~10 s a 4× contro 2 s — aperto
prestazione: solo in modo appaiato (--contro HEAD); il modo assoluto non è credibile qui

istantanea.js, su otto istanti davvero indipendenti — 46 misure su 56:
  erba senza soggetti 4/8   palla 8/8   altezza figura 8/8   temperatura 8/8
  ombre 5/8   centro a sera 7/8   CENTRO ABITATO 6/8  (era 3/8 col metro rotto)
banco degli strumenti: 11 prove su 11, VERDE per la prima volta
```

**Il totale è sceso da 48 a 46 e non è una regressione: è severità.** Quel 48
conteneva un cancello del terzo centrale che nessuna inquadratura poteva
superare, e una misura dell'erba comprata dallo stesso difetto di tinta —
`hueMin: 90` buttava via il 9,95% del manto e **il 50,1% dei pixel sotto la
pozza dei fari**. Portata a 60 (l'unico avvallo dell'istogramma sta a 58-60° e
il pavimento tiene il 99,1% del manto), l'erba vuota vera esce: **57-63%
dell'area giocabile contro un tetto di 50**. È un difetto nuovo *visibile*, non
un difetto nuovo. Da ri-giustificare: quel tetto del 50% era tarato sulla
definizione di prato vecchia, più stretta.

Nessun totale di questo strumento si confronta riga per riga con quelli di ieri:
il campionamento stratificato ha spostato tutti gli otto bersagli, e la famiglia
di tinta del prato è cambiata. Si confrontano i verdetti, non le colonne.

## Come si riprende

```bash
cd C:/Users/Utenteee/Desktop/GitHub/games
node strumenti/tutti.js --tutto --insieme 4      # tutta la batteria, in parallelo
node strumenti/giocata.js --tutte                 # da solo: cronometrico
node strumenti/_eventi.js                         # il gioco, non l'immagine
node strumenti/prestazione.js --contro HEAD       # l'unico confronto onesto qui
python android/costruisci.py && python android/verifica.py
```

## Le regole pagate

1. Le passate correttive rendono più di quelle creative.
2. Un solo «peggio» è bloccante.
3. I cancelli li esegue chi giudica, non chi lavora.
4. **Uno strumento che attesta invece di misurare è peggio di nessuno
   strumento** — ventuno casi. E quando ne ripari uno, **cerca subito la
   stessa ferita negli strumenti che l'hanno copiato**: `_identita.js` aveva
   ereditato il campionamento difettoso di `istantanea.js` insieme al resto.
5. Un agente morto non è un via libera: i workflow si fermano su null.
6. Il metro si rettifica con data e fonte quando la realtà lo supera.
7. Prima di accusare il metro, interrogalo.
8. **Prima di chiamare regressione un numero rosso, misura quanto valeva
   sull'ultimo commit.** Costa trenta secondi; tre diagnosi su quattro, in una
   giornata, non erano regressioni.
9. **Un cancello che approssima un giudizio umano va tarato contro giudizi
   umani veri, ripetuti, da persone che non conoscono la risposta.**
10. **Il cancello viene superato per la via più corta, e la cosa che
    rappresentava non arriva.** «Palla trovabile» ottenuto accendendo la palla;
    «contrasto 3:1» ottenuto sbiancando la maglia invece di spostarne la tinta —
    e quel blocco ha impedito alla sera di arrivare per mesi.
11. **Guarda dove campiona il cancello.** «Erba vuota 8/8» valeva sul quadro
    intero; sul terzo centrale, dove cade l'occhio, il prato è vuoto per due terzi.
12. **Misura il gioco, non solo l'immagine.** Sedici strumenti guardavano
    fotografie: nessuno si era accorto che non si segnava mai.
13. **Fai misurare al banco la propria indipendenza.** Otto campioni erano sei, e
    nessuna delle sette misure poteva accorgersene: guardavano tutte il
    contenuto, nessuna guardava *se stessa*. Il difetto si è visto solo quando lo
    strumento ha confrontato i propri campioni fra loro.
14. **Guarda il denominatore, non solo la soglia.** «Erba vuota» si misurava
    anche sotto l'interfaccia, dove erba non può esserci. La guardia giusta non è
    un numero ma una struttura: il numeratore non si tocca, così dichiarare più
    interfaccia fa **salire** la percentuale. Una dichiarazione che può solo
    nuocere a chi la fa non ha bisogno di essere creduta.
15. **Un numero con la dispersione fuori soglia non si scrive da nessuna
    parte.** L'avvio ha portato per due giorni l'etichetta «~10 s contro un
    tetto di 2 s», e a banco scarico vale **2870 ms con dispersione 2,2%**.
    Le tre misure precedenti — 10203, 8872, 6140 — erano tutte il banco. Il
    guaio non è stato misurare male: è stato **trascrivere** un numero che
    lo strumento aveva già dichiarato non valido, e poi ragionarci sopra.
17. **Un provino cieco con una persona sola non risolve una differenza
    piccola.** Due giudici, lo stesso foglio, la stessa chiave: il primo dà
    la posa nuova 1 su 10 contro 4 su 10 della vecchia, il secondo 6 su 10
    contro 5. In tutto hanno nominato 5 tuffi contro 11 — la varianza **fra
    le persone** è più grande della differenza fra le cose confrontate. Con
    un solo giudice avremmo scritto «peggiorata» o «migliorata» a seconda di
    chi capitava. Il referto giusto è «questa misura non sa distinguerle»,
    e va detto con la forbice accanto.
18. **Un provino cieco ha bisogno di un testimone.** Nel foglio a sola ombra
    dieci celle su venti erano capsule lisce, che per costruzione non possono
    dire niente. La persona le ha dichiarate tutte e dieci «non nominabile»:
    solo per questo le altre dieci risposte valgono qualcosa. Senza testimone,
    «zero verbi riconosciuti» e «la persona non ci teneva» sono lo stesso
    referto. E va detto in anticipo che alcune celle potrebbero non essere
    nominabili, se no si inventa — nel primo provino, senza avvertenza, sono
    uscite due sole parole per venti immagini.
19. **Un falso troppo gentile non prova niente.** Il primo anello dipinto per
    ingannare il cancello metteva lo zoccolo nero sul varco: nessun raggio ci
    arrivava, e il cancello «passava» la prova senza averla affrontata. Un falso
    va costruito nel *caso peggiore*, con la stessa geometria del gioco.
20. **Un banco che congela una squadra misura il banco, non il gioco.**
    `setCpuVsCpu(true)` chiamato prima di `startMatch(...)` viene annullato
    in silenzio da `G.cpu` scritto dentro `startMatch` stesso: la squadra 0
    restava un "umano" immobile per tutta la partita, e un vantaggio pieno
    misurato a 0/260 (o 0% su 260 finestre) non era una proprietà del
    gioco — era l'assenza di un avversario vero. La contabilità arbitrale
    vera (ordine giusto, `startMatch` poi `setCpuVsCpu`) dava 26-27%, non
    zero. L'idioma sbagliato viveva, committato, in dieci strumenti
    (voce #107, seguito #108): un confronto due-versioni fatto sotto lo
    stesso banco congelato resta valido perché simmetrico, ma lo scenario
    che misura non era mai stato quello dichiarato.
21. **Un teletrasporto che non azzera lo stato derivato eredita l'ultimo
    evento vero, e un banco può restare bloccato lì per sempre.** Lo
    scenario PUGNI di `strumenti/_q-proporzioni.js` (voce #107) chiamava
    `startMatch` una volta e poi spostava il pallone addosso al portiere
    4000 volte di fila senza mai riscrivere `b.lastTouch`/`b.toccoPiede`:
    restavano il tocco di piede di un compagno del calcio d'inizio, e la
    guardia del retropassaggio (giusta, nata nel compito 2 della stessa
    voce) rifiutava le mani ad ogni singolo tentativo — 0/4000, su tutte
    le taglie, per sempre, perché nessun evento nuovo toccava mai quello
    stato. Il gioco non era colpevole (30/30 verde anche sul commit dove
    la guardia è nata, `81d8c59`, col banco curato). E una seconda
    lezione dentro la prima: **«pre-esistente» va verificato contro
    l'ORIGINE, non contro il commit immediatamente precedente.** Il
    compito 4 aveva confrontato l'uscita rossa solo con la propria base
    (`700f775`, già dopo la guardia) e l'aveva dichiarata pre-esistente
    al cantiere — falso: la guardia, e quindi il guasto, erano nati
    DENTRO lo stesso cantiere, due compiti prima, e nessuno aveva
    rilanciato la batteria intera nel frattempo per accorgersene.
22. **In un cantiere a più compiti, rilanciare la batteria INTERA a ogni
    compito, non solo i cancelli nominati dal piano.** Una regressione
    di contorno colpisce un banco che il compito in corso non ha motivo
    di guardare, e resta invisibile fino alla batteria di chiusura — non
    un caso isolato: **almeno cinque occorrenze nel programma**: **#86**
    (il collaudo/i diritti FIFA), **#87** (il banco del tocco), **#107**
    (le proporzioni — è la lezione 21 sopra, il sottocaso "pre-esistente
    verificato contro il commit sbagliato"), **#112** (`disposizione.js`,
    riga 2 sopra: rosso da fine compito 4, mai rilanciato fino alla
    batteria di chiusura del compito 6), **#117** (`_q-replay.js` prova
    E, nato al compito 3: `G.swLock`/`G.swTimer` mai azzerati da
    `startMatch`, il sesto cronometro fratello di `G.recT`/`G.vantaggio`
    — trovato e riparato al compito 6, vedi la voce #117 in cima a questo
    file). La lezione 21 copre solo quel sottocaso specifico; questa è la
    lezione generale che lo contiene: la batteria intera, non i soli
    cancelli che il piano del compito nomina, è l'unico modo di
    accorgersi di una regressione che cade fuori dal proprio elenco.
